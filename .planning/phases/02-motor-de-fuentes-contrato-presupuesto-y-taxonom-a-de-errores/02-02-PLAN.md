---
phase: 02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/backend/nyanko_api/http.py
  - apps/backend/nyanko_api/sources/engine.py
  - apps/backend/nyanko_api/sources/registry.py
  - apps/backend/tests/test_source_budget.py
  - apps/backend/tests/test_source_engine.py
autonomous: true
requirements: [SRC-05, SRC-06, SRC-07]

must_haves:
  truths:
    - "ROADMAP criterio 3: un parse que encuentra 0 resultados lanza `SourceParseError`, y un fallo de fuente no pisa una lista de capitulos buena ya cacheada."
    - "ROADMAP criterio 4: dos consumidores simultaneos de la misma fuente, reader/prefetch simulado y cola de descargas simulada, beben del mismo bucket y el ritmo agregado no supera el presupuesto declarado."
    - "ROADMAP criterio 5: el fetcher generico aplica `headers` declarados por la fuente sin conocer nombres de fuente."
    - "D-05/D-06: el motor crea un `RateLimitedClient` por fuente al registrar y lo inyecta; los llamadores no poseen el presupuesto."
    - "D-07: `requests_per_minute` declarado por la fuente se acota a un techo global antes de construir el bucket."
    - "D-08: las peticiones de lectura tienen prioridad sobre descargas cuando compiten por el mismo bucket."
  prohibitions:
    - "Trampa A: no reimplementar `RateLimitedClient`; ya existe en `apps/backend/nyanko_api/http.py` y se reutiliza o se extiende ahi."
    - "D-06: un modulo de fuente no importa `httpx`, `requests`, `urllib` ni `aiohttp` directamente."
    - "D-10: una fuente nunca reintenta por su cuenta; reintenta el motor mediante `retry_with_backoff` existente."
    - "D-11: un error nunca sobrescribe una lista de capitulos buena en cache."
    - "D-09: un parse que produce 0 resultados no devuelve `[]`; lanza `SourceParseError`."
    - "deferred: no persistir cache a SQLite, no circuit breaker y no separar bucket API-vs-CDN."
---

<objective>
Cambiar el dueño real del presupuesto: de clientes/modulos a motor de fuentes. El motor registra fuentes,
crea un bucket por fuente con `RateLimitedClient`, inyecta el fetcher, aplica headers declarados,
traduce errores y mantiene una cache buena en memoria sin persistirla.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-CONTEXT.md
@.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `apps/backend/nyanko_api/sources/__init__.py` | plan 01 | Indice del paquete con imports explicitos y `SOURCES` escrito a mano |
| `apps/backend/nyanko_api/sources/contract.py` | plan 01 | Contrato versionado de fuente |
| `SOURCE_API_VERSION` | `sources/contract.py` | Entero de version de API, valor inicial `1` |
| `Source` | `sources/contract.py` | `Protocol` runtime-checkable con `search()`, `chapters()` y `pages()` |
| `SourceCapabilities` | `sources/contract.py` | `@dataclass(frozen=True, slots=True)` con capacidades, headers y ritmo declarado |
| `SourceSeries` | `sources/contract.py` | Tipo de dominio propio de fuente |
| `SourceChapter` | `sources/contract.py` | Tipo de dominio propio de capitulo |
| `SourcePage` | `sources/contract.py` | Tipo de dominio propio de pagina |
| `apps/backend/nyanko_api/sources/errors.py` | plan 01 | Familia de errores del motor |
| `SourceError` | `sources/errors.py` | Base `RuntimeError` |
| `SourceNetworkError` | `sources/errors.py` | Error de red reintentable por el motor |
| `SourceParseError` | `sources/errors.py` | Error de parseo no reintentable |
| `SourceRateLimitError` | `sources/errors.py` | Error de rate limit tipado |
| `SourceNotFoundError` | `sources/errors.py` | Serie/capitulo inexistente |
| `SourceUnsupportedError` | `sources/errors.py` | Operacion no soportada |
| `apps/backend/nyanko_api/sources/registry.py` | modificado | Registro que crea buckets por fuente al registrar |
| `SourceRegistry` | `sources/registry.py` | Clase de registro versionado y dueño de instancias aceptadas |
| `SourceRegistration` | `sources/registry.py` | Estado `ok`/`rejected` con motivo |
| `build_source_registry` | `sources/registry.py` | Builder explicito que recibe dependencias del motor |
| `apps/backend/nyanko_api/sources/engine.py` | nuevo | Motor/fetcher, cache y traduccion de errores |
| `SourceEngine` | `sources/engine.py` | Clase dueña de llamadas, cache y recuperacion de errores |
| `DefaultSourceFetcher` | `sources/engine.py` | Implementacion concreta del `SourceFetcher` Protocol de `contract.py`; aplica headers y prioridad |
| `RateLimitedClient.request(..., priority=...)` | `nyanko_api/http.py` | Extension minima para prioridad lectura/descarga sin copiar limiter |
| `apps/backend/nyanko_api/sources/local_archive.py` | plan 01 | Primer adapter real |
| `LocalArchiveSource` | `sources/local_archive.py` | Fuente minima de carpetas locales |
| `GET /api/sources` | `apps/backend/nyanko_api/main.py` en plan 03 | Endpoint REST plano para estado de fuentes |
| `apps/backend/tests/test_sources.py` | plan 01 | Conformidad, versionado, import-guard y `LocalArchiveSource` |
| `apps/backend/tests/test_source_budget.py` | nuevo | Presupuesto compartido, techo global, headers y prioridad |
| `apps/backend/tests/test_source_engine.py` | nuevo | Cache buena y errores |
| `apps/backend/tests/test_source_api.py` | plan 03 | Endpoint REST y persistencia sin URLs |
| `apps/backend/tests/test_packaged_sources.py` | plan 03 | PyInstaller onedir con fuentes no vacias |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hacer que el registro cree un bucket por fuente y lo inyecte</name>

  <read_first>
    - `apps/backend/nyanko_api/sources/engine.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/nyanko_api/sources/registry.py` - fichero modificado.
    - `apps/backend/tests/test_source_budget.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/nyanko_api/http.py` - analog exacto; contiene `RateLimitedClient`, `_observe_budget`, `_LoopState` por event loop y `retry_with_backoff`.
    - `apps/backend/tests/test_http.py` - analog de tests de ritmo acumulativo y `real_rate_limit_sleep`.
    - `apps/backend/tests/conftest.py` - fixture autouse `_fast_rate_limit_sleep` y fixture `real_rate_limit_sleep`.
    - `apps/backend/nyanko_api/anilist.py`, `apps/backend/nyanko_api/kitsu.py`, `apps/backend/nyanko_api/myanimelist.py` - anti-patron de singleton por modulo que esta fase no debe copiar.
    - `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-PATTERNS.md` - seccion `engine.py` y Trampa A/B.
  </read_first>

  <action>
    Crear `SourceEngine` y `DefaultSourceFetcher` como implementacion concreta del `SourceFetcher`
    Protocol definido en plan 01. En `SourceRegistry.register()` o en `build_source_registry`,
    al aceptar una fuente, construir exactamente un `RateLimitedClient` para esa fuente con
    `requests_per_minute = min(source.capabilities.requests_per_minute, SOURCE_RATE_LIMIT_CEILING)` y
    `max_concurrency` acotado. Inyectar un `SourceFetcher` que use ese mismo cliente en la instancia
    de la fuente. El fetcher aplica `source.capabilities.headers` en cada request sin ramas por nombre
    de fuente. No crear otro limiter en `sources/`; cualquier cambio de ritmo/prioridad vive en
    `apps/backend/nyanko_api/http.py`.
  </action>

  <acceptance_criteria>
    - Asercion de fuente: `apps/backend/nyanko_api/sources/engine.py` importa `RateLimitedClient` desde `nyanko_api.http` y no define otra clase de limiter.
    - Asercion negativa: `rg "class .*Rate.*Limit|asyncio.sleep|next_slot|Semaphore" apps/backend/nyanko_api/sources` no encuentra una reimplementacion de ritmo en `sources/`.
    - Asercion de comportamiento: dos consumidores simulados de la misma fuente comparten el mismo `RateLimitedClient` observado por identidad o por calendario agregado.
    - Trampa B: `tests/test_source_budget.py` debe pedir explicitamente el fixture `real_rate_limit_sleep`; sin ese fixture, `_fast_rate_limit_sleep` autouse haria que el test pasara vacio.
    - Trampa B verificable: el test de ritmo graba sleeps acumulativos; si el executor duplica el bucket por consumidor, aparecen delays duplicados/intercalados y el test falla de forma medible.
    - Asercion de techo: una fuente fake que declara `requests_per_minute = 600` queda acotada a `SOURCE_RATE_LIMIT_CEILING`.
    - Asercion de headers: una fuente fake con `headers = {"Referer": "https://source.test/", "User-Agent": "NyankoSourceTest"}` produce una request del fetcher con esos headers sin `if source.name`.
    - Comando: `cd apps/backend; python -m pytest tests/test_source_budget.py tests/test_http.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Añadir prioridad lectura antes que descarga sin copiar el limiter</name>

  <read_first>
    - `apps/backend/nyanko_api/http.py` - fichero modificado; leer `RateLimitedClient.request`, `_LoopState` y el sleep fuera del semaforo.
    - `apps/backend/tests/test_http.py` - analog de aserciones de calendario acumulativo y deadlines distintos.
    - `apps/backend/tests/test_source_budget.py` - fichero modificado.
    - `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-PATTERNS.md` - D-08 y aviso sobre modificar el reparto de huecos.
  </read_first>

  <action>
    Extender `RateLimitedClient.request()` y helpers `get()`/`post()` con keyword `priority: int = 0`
    o equivalente minimo. El reparto de huecos debe usar una cola de admision con prioridad dentro de
    `http.py`: por ejemplo, un waiter heap `(-priority, sequence, future/request)` protegido por
    `asyncio.Condition` dentro de `_LoopState`, o una estructura equivalente que pueda reordenar
    waiters antes de asignar `deadline`. Mantener `_LoopState` por event loop, `retry_with_backoff`,
    y el sleep fuera del semaforo. El semaforo sigue siendo solo tope de peticiones en vuelo, no
    mecanismo de ritmo. Un kwarg `priority` que entra a la misma cola FIFO de `state.lock` no cumple
    D-08.
  </action>

  <acceptance_criteria>
    - Asercion de fuente: `RateLimitedClient.request` acepta `priority` sin crear un segundo limiter.
    - Asercion de fuente: el `await asyncio.sleep(...)` del pacing sigue fuera de cualquier `async with state.semaphore`.
    - Asercion de comportamiento: el test invierte el orden de llegada; las descargas simuladas se encolan primero y quedan esperando en una barrera/Condition comprobada, luego entran las lecturas, y aun asi los deadlines mas tempranos pertenecen a lectura.
    - Asercion de dientes: una implementacion FIFO que acepta `priority` pero no reordena waiters falla el test de inversion de llegada.
    - Asercion de regresion: los tests existentes de `tests/test_http.py` que validan `_LoopState`, techo y calendario acumulativo siguen verdes.
    - Comando: `cd apps/backend; python -m pytest tests/test_http.py tests/test_source_budget.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementar cache buena y traduccion de errores</name>

  <read_first>
    - `apps/backend/nyanko_api/sources/engine.py` - fichero modificado.
    - `apps/backend/tests/test_source_engine.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/nyanko_api/sources/errors.py` - familia de errores del plan 01.
    - `apps/backend/nyanko_api/http.py` - `retry_with_backoff` y mapa de errores `httpx`.
    - `apps/backend/tests/test_persisted_urls.py` - helper `assert_no_persisted_urls` y guardia FND-05.
    - `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-PATTERNS.md` - seccion cache en memoria sin analog.
  </read_first>

  <action>
    En `SourceEngine`, implementar cache en memoria `dict[tuple[str, str], list[SourceChapter]]` para
    listas de capitulos por `(source_name, source_id)`. Escribir la cache solo despues de un resultado
    valido; en `SourceError`, si ya existe lista buena para la clave, devolverla sin sobrescribirla, y
    si no existe, relanzar. Traducir errores de `RateLimitedClient`/`httpx`: 429 a
    `SourceRateLimitError`, 404 a `SourceNotFoundError`, red/timeout a `SourceNetworkError`. No
    reintentar `SourceParseError`; un HTTP 200 con HTML de reto Cloudflare debe terminar en
    `SourceParseError`, no `[]`.
  </action>

  <acceptance_criteria>
    - Asercion de comportamiento: test cachea una lista buena de capitulos y luego simula HTTP 200 con cuerpo HTML/reto Cloudflare que causa `SourceParseError`; `SourceEngine.chapters()` devuelve la lista cacheada intacta.
    - Asercion de comportamiento: si no hay cache previa, el mismo `SourceParseError` se relanza y no se guarda `[]`.
    - Asercion negativa: no hay escritura a SQLite ni llamadas a `database.set_cache` para esta cache.
    - Asercion negativa: fuentes fake usadas en tests no contienen retry propio; los reintentos de red quedan en `RateLimitedClient`/`retry_with_backoff`.
    - Asercion de fuente: cualquier comentario `ponytail:` de la cache nombra el techo conocido: dict de proceso sin TTL/LRU, persistencia diferida a Fase 8.
    - Comando: `cd apps/backend; python -m pytest tests/test_source_engine.py tests/test_source_budget.py -q -x`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS nivel 1. Bloquear cualquier amenaza `high`; no aceptar mitigaciones pendientes dentro de esta fase.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Denial of Service | `SourceCapabilities.requests_per_minute` -> `RateLimitedClient` | high | mitigate/block | Una fuente declara un presupuesto absurdo para banear la IP. Mitigacion: techo global D-07 al construir el bucket y test que falla si 600/min supera `SOURCE_RATE_LIMIT_CEILING`. |
| T-02-02 | Tampering | Modulos de fuente | high | mitigate/block | Una fuente accede a red por su cuenta y salta el presupuesto. Mitigacion: fetcher inyectado D-06 y test import-guard del plan 01; este plan no añade imports de red en fuentes. |
| T-02-03 | Information Disclosure | `LocalArchiveSource` ids/rutas | high | mitigate/block | Path traversal al resolver paginas/capitulos locales. Mitigacion: el plan 01 valida roots; este plan no debe convertir ids locales en URLs ni rutas persistidas. |
| T-02-04 | Information Disclosure | Cache/persistencia | high | mitigate/block | Persistir URL o respuesta de fuente violaria FND-05. Mitigacion: cache solo en memoria y persistencia futura solo como `(source_name, source_id)` con `assert_no_persisted_urls`. |
</threat_model>

<verification>
1. `cd apps/backend; python -m pytest tests/test_source_budget.py tests/test_source_engine.py tests/test_http.py -q -x`.
2. `rg "class .*Rate.*Limit|asyncio.sleep|next_slot|Semaphore" apps/backend/nyanko_api/sources` no muestra un limiter duplicado en `sources/`.
3. Revisar que `tests/test_source_budget.py` pide `real_rate_limit_sleep`; sin ese fixture el test seria verde vacio por `_fast_rate_limit_sleep`.
</verification>

<success_criteria>
- SRC-05 cubierto por headers como dato aplicados por fetcher generico.
- SRC-06 cubierto por bucket unico por fuente, compartido por consumidores simultaneos.
- SRC-07 cubierto por `SourceParseError` y cache buena que sobrevive errores.
</success_criteria>

<output>
Create `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-02-SUMMARY.md` when done.
</output>
