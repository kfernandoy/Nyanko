---
phase: 02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/nyanko_api/sources/__init__.py
  - apps/backend/nyanko_api/sources/contract.py
  - apps/backend/nyanko_api/sources/errors.py
  - apps/backend/nyanko_api/sources/registry.py
  - apps/backend/nyanko_api/sources/local_archive.py
  - apps/backend/tests/test_sources.py
autonomous: true
requirements: [SRC-04, SRC-05, SRC-07]

must_haves:
  truths:
    - "ROADMAP criterio 1: una fuente con `api_version != SOURCE_API_VERSION` se rechaza al registrar, queda visible con `status == \"rejected\"` y `rejection_reason`, y el sidecar sigue arrancando."
    - "ROADMAP criterio 2: `apps/backend/tests/test_sources.py` parametriza sobre `SOURCES` reales, incluye `LocalArchiveSource`, y falla si una fuente registrada no cumple el `Source` Protocol."
    - "ROADMAP criterio 5: cada fuente declara `headers` (`Referer`/`User-Agent`) y `requests_per_minute` como dato; no existe logica `if source.name == ...` fuera de la fuente para esos headers."
    - "D-01: el Protocol Source expone exactamente `search()`, `chapters(serie)` y `pages(capitulo)` - ni un metodo mas - y `SourceCapabilities` (frozen dataclass) es donde cada fuente declara lo que NO hace, en vez de llenar el Protocol de `NotImplementedError`. Nada de popular/latest/series-detail."
    - "D-03: los tipos que devuelve una fuente (`SourceSeries` / `SourceChapter` / `SourcePage`) son propios del motor y viven SEPARADOS de los modelos Pydantic del tracker en `models.py`. Una serie de una fuente no es una entrada de AniList; tenerlas separadas hace imposible escribir por accidente el desastre que la Fase 4 existe para prevenir (leer el capitulo 12 de la serie A y escribir progreso en la B)."
    - "D-12: el mensaje al usuario depende del TIPO de error, no de `str(exc)`: sin conexion (`SourceNetworkError`) -> clave `reintentar`; fuente rota / sitio cambio (`SourceParseError`) -> clave `actualizar_la_fuente`; te estan limitando (`SourceRateLimitError`) -> clave `esperar`. Es un mapeo tipo->clave de mensaje, no un sistema de i18n."
    - "D-13: `SOURCE_API_VERSION` es un entero y el registro exige coincidencia exacta."
  prohibitions:
    - "D-15: no usar autodiscovery de fuentes con `pkgutil`, `importlib` ni `__subclasses__()`; la lista `SOURCES` se escribe a mano."
    - "D-06: un modulo de fuente no importa `httpx`, `requests`, `urllib` ni `aiohttp` directamente."
    - "D-09: un parse que produce 0 resultados lanza `SourceParseError`; nunca devuelve `[]`."
    - "D-04: la app no persiste URLs de fuente; solo `(source_name, source_id)` opacos."
    - "D-14: una fuente rechazada no desaparece de la lista ni tumba el sidecar."
    - "D-02/deferred: `LocalArchiveSource` no lee CBZ, no hace ordenacion natural y no interpreta `ComicInfo.xml`."
---

<objective>
Crear la base del motor de fuentes: contrato versionado, errores tipados, registro explicito y
`LocalArchiveSource` minima. Este plan no implementa fuentes online ni ningun consumidor de lectura.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-CONTEXT.md
@.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `apps/backend/nyanko_api/sources/__init__.py` | nuevo | Indice del paquete con imports explicitos y `SOURCES` escrito a mano |
| `apps/backend/nyanko_api/sources/contract.py` | nuevo | Contrato versionado de fuente |
| `SOURCE_API_VERSION` | `sources/contract.py` | Entero de version de API, valor inicial `1` |
| `Source` | `sources/contract.py` | `Protocol` runtime-checkable con `search()`, `chapters()` y `pages()` |
| `SourceFetcher` | `sources/contract.py` | `Protocol` minimo del fetcher inyectado; la implementacion concreta nace en `sources/engine.py` en plan 02 |
| `SourceCapabilities` | `sources/contract.py` | `@dataclass(frozen=True, slots=True)` con capacidades, headers y ritmo declarado |
| `SourceSeries` | `sources/contract.py` | Tipo de dominio propio de fuente, separado de modelos Pydantic del tracker |
| `SourceChapter` | `sources/contract.py` | Tipo de dominio propio de capitulo |
| `SourcePage` | `sources/contract.py` | Tipo de dominio propio de pagina |
| `apps/backend/nyanko_api/sources/errors.py` | nuevo | Familia de errores del motor |
| `SourceError` | `sources/errors.py` | Base `RuntimeError` |
| `SourceNetworkError` | `sources/errors.py` | Error de red reintentable por el motor |
| `SourceParseError` | `sources/errors.py` | Error de parseo no reintentable; 0 resultados cae aqui |
| `SourceRateLimitError` | `sources/errors.py` | Error de rate limit tipado |
| `SourceNotFoundError` | `sources/errors.py` | Serie/capitulo inexistente |
| `SourceUnsupportedError` | `sources/errors.py` | Operacion no soportada por capacidades |
| `source_error_action` | `sources/errors.py` | Mapeo minimo D-12 de tipo de `SourceError` a clave de accion/mensaje para UI |
| `apps/backend/nyanko_api/sources/registry.py` | nuevo | Registro de fuentes aceptadas y rechazadas |
| `SourceRegistry` | `sources/registry.py` | Clase de registro versionado |
| `SourceRegistration` | `sources/registry.py` | Registro de estado `ok`/`rejected` con motivo |
| `build_source_registry` | `sources/registry.py` | Builder explicito; se reutiliza y se completa en planes 02/03 |
| `apps/backend/nyanko_api/sources/local_archive.py` | nuevo | Primer adapter real |
| `LocalArchiveSource` | `sources/local_archive.py` | Fuente minima de carpetas locales |
| `apps/backend/nyanko_api/sources/engine.py` | nuevo en plan 02 | Motor/fetcher, cache en memoria y traduccion de errores |
| `SourceEngine` | `sources/engine.py` | Clase dueña del fetcher, cache y llamadas a fuentes |
| `GET /api/sources` | `apps/backend/nyanko_api/main.py` en plan 03 | Endpoint REST plano para estado de fuentes |
| `apps/backend/tests/test_sources.py` | nuevo | Conformidad, versionado, import-guard y `LocalArchiveSource` |
| `apps/backend/tests/test_source_budget.py` | nuevo en plan 02 | Presupuesto compartido y prioridad |
| `apps/backend/tests/test_source_engine.py` | nuevo en plan 02 | Cache buena y errores |
| `apps/backend/tests/test_source_api.py` | nuevo en plan 03 | Endpoint REST y persistencia sin URLs |
| `apps/backend/tests/test_packaged_sources.py` | nuevo en plan 03 | PyInstaller onedir con fuentes no vacias |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir contrato versionado y errores tipados</name>

  <read_first>
    - `apps/backend/nyanko_api/sources/contract.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/nyanko_api/sources/errors.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/tests/test_sources.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/nyanko_api/providers.py` - analog exacto para `MediaProvider`, `ProviderCapabilities`, tipos de dominio y builder.
    - `apps/backend/nyanko_api/anilist.py` - analog de `AniListError(RuntimeError)` con payload.
    - `apps/backend/nyanko_api/kitsu.py` - analog de error minimo y `raise ... from error`.
    - `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-PATTERNS.md` - seccion `contract.py` y `errors.py`.
  </read_first>

  <action>
    Crear `SOURCE_API_VERSION = 1`, `SourceCapabilities`, `SourceSeries`, `SourceChapter`,
    `SourcePage`, `SourceFetcher` Protocol y `Source` Protocol. El Protocol tiene atributos de clase `name: str`,
    `display_name: str`, `api_version: int`, `capabilities: SourceCapabilities` y solo los metodos
    async `search(query: str, limit: int = 20)`, `chapters(series: SourceSeries | str)` y
    `pages(chapter: SourceChapter | str)`. Crear `SourceError`, `SourceNetworkError`,
    `SourceParseError`, `SourceRateLimitError`, `SourceNotFoundError` y `SourceUnsupportedError`
    como jerarquia deliberada de `RuntimeError`, usando `from error` al envolver excepciones.
    Crear un mapeo minimo `source_error_action(error: SourceError) -> Literal["reintentar",
    "actualizar_la_fuente", "esperar"]` o equivalente: `SourceNetworkError` -> `reintentar`,
    `SourceParseError` -> `actualizar_la_fuente`, `SourceRateLimitError` -> `esperar`.
    No agregar `popular`, `latest`, `series_detail` ni metodos espejo de Mihon.
  </action>

  <acceptance_criteria>
    - Asercion de fuente: `apps/backend/nyanko_api/sources/contract.py` contiene exactamente `SOURCE_API_VERSION = 1`.
    - Asercion de fuente: `class Source(Protocol)` esta marcado con `@runtime_checkable`.
    - Asercion de fuente: el Protocol define `search`, `chapters` y `pages`; `rg "popular|latest|series_detail" apps/backend/nyanko_api/sources/contract.py` no encuentra metodos del contrato.
    - Asercion de fuente: `SourceCapabilities` usa `@dataclass(frozen=True, slots=True)` y contiene datos para `headers` y `requests_per_minute`.
    - Asercion de fuente: `SourceSeries`, `SourceChapter` y `SourcePage` no importan ni heredan de `nyanko_api.models`.
    - Asercion de comportamiento: una fuente fake que devuelve `[]` al parsear 0 resultados debe tener test que espera `SourceParseError`, no lista vacia.
    - Asercion de comportamiento: tests de `source_error_action` o equivalente verifican `SourceNetworkError -> "reintentar"`, `SourceParseError -> "actualizar_la_fuente"` y `SourceRateLimitError -> "esperar"`; ningun renderer/API debe depender de `str(exc)` para decidir el mensaje.
    - Comando: `cd apps/backend; python -m pytest tests/test_sources.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear registro explicito con rechazadas visibles</name>

  <read_first>
    - `apps/backend/nyanko_api/sources/__init__.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/nyanko_api/sources/registry.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/tests/test_sources.py` - fichero modificado.
    - `apps/backend/nyanko_api/providers.py` - analog exacto de `ProviderRegistry` y `build_provider_registry`.
    - `apps/backend/nyanko_api/detectors/__init__.py` - analog exacto de imports explicitos y `__all__`.
    - `apps/backend/nyanko-api.spec` - confirma que PyInstaller congela submodulos in-tree; la trampa es runtime autodiscovery.
    - `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-CONTEXT.md` - D-13, D-14, D-15.
  </read_first>

  <action>
    Crear `SOURCES = [LocalArchiveSource]` con imports explicitos en `sources/__init__.py`.
    Crear `SourceRegistry`, `SourceRegistration` y `build_source_registry`. `register()` rechaza una
    fuente con `api_version != SOURCE_API_VERSION` guardandola como `status == "rejected"` con
    `rejection_reason`, y captura errores de carga/instanciacion para reportarlos sin abortar el
    arranque. Duplicados siguen lanzando `ValueError("Source already registered: ...")` como bug del
    autor. Exponer metodos separados para fuentes `all()` aceptadas y `status()`/`registrations()`
    con aceptadas + rechazadas. No usar `pkgutil`, `importlib`, `__subclasses__()` ni recorrido de
    paquetes.
  </action>

  <acceptance_criteria>
    - Asercion de fuente: `apps/backend/nyanko_api/sources/__init__.py` contiene `from .local_archive import LocalArchiveSource` y `SOURCES = [LocalArchiveSource]`.
    - Asercion negativa: `rg "pkgutil|importlib|__subclasses__" apps/backend/nyanko_api/sources` no encuentra autodiscovery de fuentes.
    - Asercion de comportamiento: una fuente fake con `api_version = SOURCE_API_VERSION + 1` aparece en `SourceRegistry.registrations()` con `status == "rejected"` y motivo que nombra la version.
    - Asercion de comportamiento: una fuente fake que lanza al instanciarse queda en la lista como rechazada y no interrumpe `build_source_registry`.
    - Asercion de comportamiento: registrar dos fuentes con el mismo `name` lanza `ValueError` y no oculta el duplicado.
    - Comando: `cd apps/backend; python -m pytest tests/test_sources.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementar LocalArchiveSource minima y guardia de imports de red</name>

  <read_first>
    - `apps/backend/nyanko_api/sources/local_archive.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/nyanko_api/sources/__init__.py` - fichero modificado.
    - `apps/backend/tests/test_sources.py` - fichero modificado.
    - `apps/backend/nyanko_api/providers.py` - analog de clase concreta con atributos de clase y dependencia en `__init__`.
    - `apps/backend/nyanko_api/scanner.py` - analog de paseo de carpetas y extensiones como `frozenset`.
    - `apps/backend/nyanko_api/database.py` - `get_library_folders()` como fuente de carpetas existentes.
    - `apps/backend/tests/test_persisted_urls.py` - filosofia de guardia runtime sin listas a mano y helper `assert_no_persisted_urls`.
    - `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-PATTERNS.md` - secciones `local_archive.py` e import-guard.
  </read_first>

  <action>
    Crear `LocalArchiveSource` con `name = "local_archive"`, `display_name`, `api_version =
    SOURCE_API_VERSION` y `capabilities = SourceCapabilities(search=False, headers={}, requests_per_minute=1)`.
    Su `__init__` recibe un objeto tipado como `SourceFetcher` Protocol aunque no use red, y recibe carpetas de biblioteca ya
    existentes. Implementar solo listar capitulos de una carpeta y listar imagenes de cada capitulo
    con `IMAGE_EXTENSIONS = frozenset(".jpg .jpeg .png .webp .gif .avif".split())`. Usar ids opacos
    relativos al root validado; rechazar traversal o ids que resuelvan fuera del root con
    `SourceNotFoundError` o `SourceParseError`. No guardar rutas absolutas como identidad y no crear
    persistencia nueva. En `test_sources.py`, añadir import-guard AST que descubre cada fuente desde
    `SOURCES`/registry y falla si su modulo importa `httpx`, `requests`, `urllib` o `aiohttp`.
  </action>

  <acceptance_criteria>
    - Asercion de comportamiento: `LocalArchiveSource.chapters()` lista subcarpetas de un root de biblioteca y devuelve `SourceChapter` con `source_id` opaco, no ruta absoluta.
    - Asercion de comportamiento: `LocalArchiveSource.pages()` lista solo extensiones de `IMAGE_EXTENSIONS`.
    - Asercion de comportamiento: un archivo `.cbz` dentro de una carpeta de capitulo NO aparece en `LocalArchiveSource.pages()` porque no pertenece a `IMAGE_EXTENSIONS`.
    - Asercion de comportamiento: con paginas `1.jpg`, `2.jpg`, `10.jpg`, el orden devuelto es lexicografico `1.jpg`, `10.jpg`, `2.jpg`; la ordenacion natural queda para Fase 3.
    - Asercion de comportamiento: `ComicInfo.xml` dentro de una carpeta de capitulo se ignora y no aparece como pagina.
    - Asercion de seguridad: un id con `..` o que resuelva fuera del root de biblioteca lanza un `SourceError` tipado y no devuelve archivos.
    - Asercion de guardia: el test AST recorre el modulo de cada fuente registrada con `inspect.getsourcefile(type(source))`, no una lista manual.
    - Asercion negativa: el import-guard falla si una fuente registrada importa `httpx`, `requests`, `urllib` o `aiohttp`.
    - Comando: `cd apps/backend; python -m pytest tests/test_sources.py tests/test_persisted_urls.py -q -x`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS nivel 1. Bloquear cualquier amenaza `high`; no aceptar mitigaciones pendientes dentro de esta fase.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Denial of Service | `SourceCapabilities.requests_per_minute` | high | mitigate/block | La fuente puede declarar un presupuesto absurdo para banear la IP del usuario. Este plan crea el dato; el plan 02 debe acotarlo con el techo global de D-07 antes de instanciar `RateLimitedClient`. |
| T-02-02 | Tampering | Modulos de fuente | high | mitigate/block | Una fuente podria importar red por su cuenta y saltarse el presupuesto. Mitigacion: inyeccion de fetcher en el contrato + test AST de import-guard contra `httpx`, `requests`, `urllib`, `aiohttp`; el bloqueo real/trust gate queda fuera por D-06/Fase 6. |
| T-02-03 | Information Disclosure | `LocalArchiveSource` | high | mitigate/block | Path traversal al listar carpetas del usuario. Mitigacion: resolver ids opacos bajo roots de biblioteca, rechazar salidas del root y cubrirlo con tests de traversal. |
| T-02-04 | Information Disclosure | Persistencia de fuente | high | mitigate/block | Persistir una URL viola FND-05. Mitigacion: ids opacos de D-04 y tests que llamen `assert_no_persisted_urls` despues de escrituras en planes posteriores. |
</threat_model>

<verification>
1. `cd apps/backend; python -m pytest tests/test_sources.py -q -x`.
2. `cd apps/backend; python -m pytest tests/test_persisted_urls.py -q -x`.
3. `rg "pkgutil|importlib|__subclasses__" apps/backend/nyanko_api/sources` no muestra autodiscovery de fuentes.
</verification>

<success_criteria>
- SRC-04 cubierto por version exacta y rechazadas visibles.
- SRC-05 cubierto por headers/ritmo como dato del contrato.
- SRC-07 cubierto por `SourceParseError` para 0 resultados.
</success_criteria>

<output>
Create `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-01-SUMMARY.md` when done.
</output>
