---
phase: 02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - apps/backend/nyanko_api/models.py
  - apps/backend/nyanko_api/main.py
  - apps/backend/tests/test_source_api.py
  - apps/backend/tests/test_packaged_sources.py
autonomous: true
requirements: [SRC-04, SRC-05]

must_haves:
  truths:
    - "ROADMAP criterio 1: `GET /api/sources` reporta fuentes `ok` y `rejected` con motivo; una fuente rota no tumba el sidecar."
    - "ROADMAP criterio 5: la respuesta de fuente expone capacidades/headers como dato, sin conocimiento especial de cada fuente en el endpoint."
    - "ROADMAP criterio 6: en build empaquetado PyInstaller onedir, la lista de fuentes registradas no esta vacia gracias a imports explicitos y `SOURCES`."
    - "El endpoint se implementa como `@app.get(\"/api/sources\")` plano, siguiendo `/api/providers`; no introduce `APIRouter` ni `include_router`."
    - "El registry de fuentes se construye una vez al arrancar el sidecar y se mantiene vivo; el endpoint no llama `build_source_registry()` por peticion porque el registry posee buckets."
    - "D-16: una fuente que carga bien pero falla siempre en uso se reporta con su tipo de error y sigue viva - decide el usuario, la app no la apaga sola. Verificable: una fuente registrada que lanza en cada llamada sigue apareciendo con `status == \"ok\"` (cargo bien) y sus fallos se reportan por tipo; nada la desactiva automaticamente."
    - "Criterio 1 / UI: en Fase 2, `se reporta en la UI` significa que la superficie REST `GET /api/sources` entrega el estado para que el renderer lo muestre; la pantalla React queda para Fase 3+."
  prohibitions:
    - "D-15: nada de autodiscovery por `pkgutil`/`importlib`; el build empaquetado debe usar imports explicitos."
    - "D-14: una fuente rechazada no desaparece de la lista ni tumba el sidecar."
    - "D-04: la app nunca persiste una URL de fuente; solo `(source_name, source_id)`."
    - "No crear `APIRouter`/`include_router`; el backend actual es plano."
    - "No reconstruir `SourceRegistry` dentro de `GET /api/sources` ni en cada request."
    - "deferred: no instalar fuentes de terceros, no trust gate, no calibrar circuit breaker hasta Fase 7 y no ampliar contrato con popular/latest/series-detail; lo diferido es solo la calibracion del circuit breaker, no el comportamiento base D-16 de mantener viva una fuente cargada."
---

<objective>
Exponer el motor de fuentes al renderer y cerrar la trampa de empaquetado: fuentes aceptadas y rechazadas
son visibles, el registry vivo no se reconstruye por request, y el onedir de PyInstaller conserva una
lista no vacia por imports explicitos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-CONTEXT.md
@.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `apps/backend/nyanko_api/sources/__init__.py` | plan 01 | Indice del paquete con imports explicitos y `SOURCES` escrito a mano |
| `apps/backend/nyanko_api/sources/contract.py` | plan 01 | Contrato versionado de fuente |
| `SOURCE_API_VERSION` | `sources/contract.py` | Entero de version de API, valor inicial `1` |
| `Source` | `sources/contract.py` | `Protocol` runtime-checkable con `search()`, `chapters()` y `pages()` |
| `SourceCapabilities` | `sources/contract.py` | `@dataclass(frozen=True, slots=True)` con capacidades, headers y ritmo declarado |
| `SourceSeries` | `sources/contract.py` | Tipo de dominio propio de fuente |
| `SourceChapter` | `sources/contract.py` | Tipo de dominio propio de capitulo |
| `SourcePage` | `sources/contract.py` | Tipo de dominio propio de pagina |
| `apps/backend/nyanko_api/sources/errors.py` | plan 01 | Familia de errores del motor |
| `SourceError` | `sources/errors.py` | Base `RuntimeError` |
| `SourceNetworkError` | `sources/errors.py` | Error de red reintentable por el motor |
| `SourceParseError` | `sources/errors.py` | Error de parseo no reintentable |
| `SourceRateLimitError` | `sources/errors.py` | Error de rate limit tipado |
| `SourceNotFoundError` | `sources/errors.py` | Serie/capitulo inexistente |
| `SourceUnsupportedError` | `sources/errors.py` | Operacion no soportada |
| `apps/backend/nyanko_api/sources/registry.py` | plan 01/02 | Registro aceptadas/rechazadas y buckets por fuente |
| `SourceRegistry` | `sources/registry.py` | Clase de registro versionado |
| `SourceRegistration` | `sources/registry.py` | Estado `ok`/`rejected` con motivo |
| `build_source_registry` | `sources/registry.py` | Builder explicito de registry vivo |
| `apps/backend/nyanko_api/sources/engine.py` | plan 02 | Motor/fetcher, cache y traduccion de errores |
| `SourceEngine` | `sources/engine.py` | Clase dueña de llamadas, cache y recuperacion de errores |
| `DefaultSourceFetcher` | `sources/engine.py` | Implementacion concreta del `SourceFetcher` Protocol de `contract.py`; aplica headers y prioridad |
| `RateLimitedClient.request(..., priority=...)` | `nyanko_api/http.py` | Extension minima para prioridad lectura/descarga |
| `apps/backend/nyanko_api/sources/local_archive.py` | plan 01 | Primer adapter real |
| `LocalArchiveSource` | `sources/local_archive.py` | Fuente minima de carpetas locales |
| `SourceCapabilitiesResponse` | `nyanko_api/models.py` | Modelo Pydantic de capacidades para REST |
| `SourceInfo` | `nyanko_api/models.py` | Modelo Pydantic con `status` y `rejection_reason` |
| `GET /api/sources` | `apps/backend/nyanko_api/main.py` | Endpoint REST plano para estado de fuentes |
| `apps/backend/tests/test_sources.py` | plan 01 | Conformidad, versionado, import-guard y `LocalArchiveSource` |
| `apps/backend/tests/test_source_budget.py` | plan 02 | Presupuesto compartido, techo global, headers y prioridad |
| `apps/backend/tests/test_source_engine.py` | plan 02 | Cache buena y errores |
| `apps/backend/tests/test_source_api.py` | nuevo | Endpoint REST, registry vivo y persistencia sin URLs |
| `apps/backend/tests/test_packaged_sources.py` | nuevo | PyInstaller onedir con fuentes no vacias |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exponer estado de fuentes con endpoint plano</name>

  <read_first>
    - `apps/backend/nyanko_api/models.py` - fichero modificado.
    - `apps/backend/nyanko_api/main.py` - fichero modificado.
    - `apps/backend/tests/test_source_api.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/nyanko_api/providers.py` - analog de `ProviderCapabilitiesResponse`, `ProviderInfo` y `/api/providers`.
    - `apps/backend/nyanko_api/sources/registry.py` - `SourceRegistry` y `SourceRegistration` de planes 01/02.
    - `apps/backend/nyanko_api/sources/contract.py` - `SourceCapabilities`.
    - `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-PATTERNS.md` - secciones `models.py` y `main.py`.
  </read_first>

  <action>
    En `models.py`, crear `SourceCapabilitiesResponse` y `SourceInfo`. `SourceInfo` incluye
    `name`, `display_name`, `status: Literal["ok", "rejected"]`, `rejection_reason: str | None` y
    `capabilities: SourceCapabilitiesResponse | None`. En `main.py`, construir el registry de fuentes
    una vez al arrancar el sidecar o en estado de app, junto a las dependencias existentes; no en el
    handler. Añadir `@app.get("/api/sources", response_model=list[SourceInfo])` plano, modelado sobre
    `/api/providers`, que lee el registry vivo y devuelve aceptadas + rechazadas.
  </action>

  <acceptance_criteria>
    - Asercion de fuente: `models.py` contiene `class SourceCapabilitiesResponse(BaseModel)` y `class SourceInfo(BaseModel)`.
    - Asercion de fuente: `main.py` contiene `@app.get("/api/sources"` y no introduce `APIRouter` ni `include_router`.
    - Asercion negativa: dentro de la funcion del endpoint `/api/sources` no aparece `build_source_registry(`.
    - Asercion de comportamiento: test con `TestClient` recibe `LocalArchiveSource` con `status == "ok"`.
    - Asercion de comportamiento: test inyecta/usa una fuente rechazada y comprueba que aparece con `status == "rejected"` y `rejection_reason`, sin 500.
    - Asercion de ciclo de vida: dos llamadas a `/api/sources` usan el mismo objeto `SourceRegistry` o no recrean buckets; el test falla si el endpoint reconstruye el registry por request.
    - Asercion de comportamiento D-16: una fuente registrada que carga bien pero lanza `SourceParseError` en cada llamada sigue apareciendo en `/api/sources` con `status == "ok"`; nada la desactiva automaticamente.
    - Asercion de alcance D-12: `/api/sources` no inventa una superficie de errores de uso ni manda `str(exc)` pelado al renderer; el mapeo tipo->clave queda unit-tested en `02-01` mediante `source_error_action`.
    - Comando: `cd apps/backend; python -m pytest tests/test_source_api.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Probar PyInstaller onedir sin autodiscovery</name>

  <read_first>
    - `apps/backend/tests/test_packaged_sources.py` - crear si no existe; es el fichero modificado.
    - `apps/backend/nyanko-api.spec` - `hiddenimports += collect_submodules('nyanko_api')`.
    - `apps/backend/nyanko_api/sources/__init__.py` - `SOURCES` explicito del plan 01.
    - `apps/backend/nyanko_api/main.py` - endpoint `/api/sources`.
    - `apps/backend/tests/test_sources.py` - tests de no-autodiscovery en dev.
    - `.planning/ROADMAP.md` - criterio 6 de Fase 2.
  </read_first>

  <action>
    Crear `tests/test_packaged_sources.py` con un fixture `scope="session"` que construye el onedir de
    prueba ejecutando `pyinstaller nyanko-api.spec --distpath <tmp_dist> --workpath <tmp_build>` desde
    `apps/backend`, y luego lanza ese exe recien construido. Si por coste local se decide usar un exe
    existente, el test debe tener freshness guard que FALLA, nunca skippea, cuando
    `apps/backend/dist/nyanko-api/nyanko-api.exe` es mas viejo que cualquier archivo bajo
    `apps/backend/nyanko_api/`. Lanzar el exe con `NYANKO_DATA_DIR=<tmp_data_dir>` y un puerto
    explicitamente libre; leer el puerto real desde `<tmp_data_dir>/port` porque `sidecar.py` puede
    caer a un puerto efimero. Consultar `GET /api/sources` en ese puerto, afirmar lista no vacia, y
    afirmar identidad del proceso empaquetado usando data dir fresco/pid del proceso lanzado. Nunca
    asumir 8765 y nunca conectar contra un sidecar dev ya vivo.
  </action>

  <acceptance_criteria>
    - Asercion de build: el test construye el onedir en un `tmp_dist` con `pyinstaller nyanko-api.spec`, o falla por freshness guard si intenta usar un exe existente mas viejo que `apps/backend/nyanko_api/`; un skip no cuenta como verde para este criterio.
    - Asercion de aislamiento: el proceso empaquetado se lanza con `NYANKO_DATA_DIR` apuntando a un temp dir unico y con un puerto explicitamente libre; el test lee el puerto real desde `<tmp_data_dir>/port` y no asume 8765.
    - Asercion de identidad: el endpoint consultado pertenece al proceso recien lanzado, demostrado por pid/proceso vivo y data dir fresco; un sidecar dev escuchando en 8765 no puede hacer pasar el test.
    - Asercion de comportamiento: `GET /api/sources` contra el puerto leido de `<tmp_data_dir>/port` devuelve longitud `> 0`.
    - Asercion de dientes: si `SOURCES` queda vacio en el build empaquetado, `tests/test_packaged_sources.py` falla; no pasa por mirar el arbol de desarrollo ni por conectarse al sidecar dev.
    - Asercion negativa: `rg "pkgutil|importlib" apps/backend/tests/test_packaged_sources.py` no encuentra autodiscovery.
    - Comando: `cd apps/backend; python -m pytest tests/test_packaged_sources.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard de no persistencia nueva de fuentes</name>

  <read_first>
    - `apps/backend/tests/test_source_api.py` - fichero modificado.
    - `apps/backend/tests/test_persisted_urls.py` - helper `assert_no_persisted_urls` y guardia FND-05.
    - `apps/backend/nyanko_api/database.py` - rutas y tablas existentes; no crear excepciones nuevas `_local`/`path`.
    - `apps/backend/nyanko_api/sources/local_archive.py` - ids opacos de `LocalArchiveSource`.
    - `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-CONTEXT.md` - D-04.
  </read_first>

  <action>
    Dejar una guardia explicita de no-regresion: Fase 2 no modifica `database.py` ni crea tablas o
    columnas de fuente, por tanto no hay escritura nueva que ejercitar. El test debe afirmar que la
    implementacion de Fase 2 no introduce columnas nuevas para URLs/rutas de fuente y que cualquier
    futuro vinculo documentado sigue siendo solo `(source_name, source_id)`. No añadir allowlist por
    sufijo `path` o `_local`, y no eximir URLs del sidecar. Usar el presupuesto restante de ejecucion
    en la Task 2 de build empaquetado.
  </action>

  <acceptance_criteria>
    - Asercion de fuente: Fase 2 no modifica `apps/backend/nyanko_api/database.py` para crear persistencia de fuentes.
    - Asercion negativa: no se añade ninguna columna nueva de fuente que almacene valores `http://`, `https://`, rutas absolutas, ni sufijos allowlisted `path`/`_local`.
    - Asercion de fuente: los planes/tests nombran explicitamente `source_name` y `source_id` como unica identidad persistible futura; no persisten URL ni ruta absoluta.
    - Comando: `cd apps/backend; python -m pytest tests/test_source_api.py tests/test_persisted_urls.py -q -x`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS nivel 1. Bloquear cualquier amenaza `high`; no aceptar mitigaciones pendientes dentro de esta fase.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Denial of Service | Catalogo REST de fuentes | high | mitigate/block | Una fuente declara presupuesto absurdo y el endpoint la presenta como normal. Mitigacion: el registry vivo ya aplica techo D-07 en plan 02 antes de exponer `SourceInfo`. |
| T-02-02 | Tampering | Modulos de fuente empaquetados | high | mitigate/block | Una fuente usa red directa y salta presupuesto. Mitigacion: import-guard del plan 01 cubre fuentes registradas; el test onedir comprueba fuentes explicitas, no discovery runtime. |
| T-02-03 | Information Disclosure | `LocalArchiveSource` en REST | high | mitigate/block | Path traversal o rutas locales filtradas al renderer. Mitigacion: endpoint devuelve ids opacos/capacidades, no rutas absolutas; tests del plan 01 cubren salida del root. |
| T-02-04 | Information Disclosure | DB y FND-05 | high | mitigate/block | Persistir URL de fuente o sidecar. Mitigacion: D-04 exige `(source_name, source_id)` y `assert_no_persisted_urls` despues de escrituras. |
</threat_model>

<verification>
1. `cd apps/backend; python -m pytest tests/test_source_api.py tests/test_packaged_sources.py -q -x` y el resultado de `tests/test_packaged_sources.py` no puede ser `skipped`; si no puede construir o probar un onedir fresco debe fallar.
2. `cd apps/backend; python -m pytest tests/test_persisted_urls.py -q -x`.
3. `rg "APIRouter|include_router" apps/backend/nyanko_api/main.py` no muestra routers nuevos.
4. `rg "build_source_registry" apps/backend/nyanko_api/main.py` confirma que el builder no esta dentro del handler `/api/sources`.
</verification>

<success_criteria>
- SRC-04 cubierto por fuente rechazada visible y arranque vivo.
- SRC-05 cubierto por capacidades/headers expuestos como dato.
- Criterio 6 del ROADMAP cubierto por test contra PyInstaller onedir.
</success_criteria>

<output>
Create `.planning/phases/02-motor-de-fuentes-contrato-presupuesto-y-taxonom-a-de-errores/02-03-SUMMARY.md` when done.
</output>
