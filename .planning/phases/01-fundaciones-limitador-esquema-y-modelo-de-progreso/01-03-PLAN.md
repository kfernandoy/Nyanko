---
phase: 01-fundaciones-limitador-esquema-y-modelo-de-progreso
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - apps/backend/tests/test_persisted_urls.py
  - apps/backend/scripts/check_persisted_urls.py
autonomous: true
requirements: [FND-05]

must_haves:
  truths:
    - "Un test de guardia falla si CUALQUIER columna persistida empieza por http — no una lista escrita a mano, sino todas las columnas de texto que la BD tenga en ese momento."
    - "La guardia cubre automáticamente las columnas nuevas del esquema v8 y las que añada cualquier fase futura, sin que nadie tenga que acordarse de actualizarla."
    - "Toda columna en la lista blanca lleva una justificación escrita de por qué guarda una URL remota legítima."
    - "Ninguna columna *_local puede estar en la lista blanca, nunca."
  artifacts:
    - apps/backend/tests/test_persisted_urls.py
    - apps/backend/scripts/check_persisted_urls.py
  key_links:
    - "PRAGMA table_info sobre sqlite_master ↔ el esquema v8 — la guardia se entera sola de las columnas nuevas; una lista literal no."
    - "check_persisted_urls.py ↔ la copia migrada de verify_real_db_migration.py — la guardia se corre contra datos reales, no solo contra fixtures."
    - "REMOTE_URL_ALLOWLIST ↔ *_local — la única regla que impide que la lista blanca se use para silenciar el bug que ya ocurrió."
---

<objective>
Una guardia que falle si cualquier columna persistida empieza por `http`, extendida a las columnas
nuevas del esquema v8 (FND-05).

Purpose: este proyecto **ya perdió todas las portadas de la biblioteca** persistiendo el puerto
efímero dentro de una URL (`http://127.0.0.1:8765/assets/...`). Cuando el sidecar arrancó en otro
puerto, la biblioteca se quedó sin portadas para siempre y no se curó sola. El reader de la Fase 3 es
una superficie diez veces mayor: cada página de cada capítulo. La guardia tiene que existir **antes**
que el reader, y tiene que ser del tipo que no se puede olvidar de actualizar.
Output: un test que se entera solo de las columnas nuevas, y un script que corre contra la BD real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/scripts/check_stale_asset_ports.py
@.planning/phases/01-fundaciones-limitador-esquema-y-modelo-de-progreso/01-02-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 03)

| Símbolo | Fichero | Qué es |
|---------|---------|--------|
| `tests/test_persisted_urls.py` | nuevo | El test de guardia de FND-05 |
| `test_no_persisted_column_starts_with_http` | `tests/test_persisted_urls.py` | Recorre TODAS las columnas de texto de TODAS las tablas vía `sqlite_master` + `PRAGMA table_info` |
| `REMOTE_URL_ALLOWLIST` | `tests/test_persisted_urls.py` | `frozenset[tuple[str, str]]` de `(tabla, columna)` que guardan URLs remotas legítimas — cada entrada con su justificación |
| `scripts/check_persisted_urls.py` | nuevo | La misma guardia, ejecutable contra cualquier fichero de BD (incluida la real) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: La guardia genérica — sin lista de columnas que mantener</name>

  <read_first>
    - `apps/backend/scripts/check_stale_asset_ports.py` — ENTERO. Es la guardia que existe hoy, y su
      limitación es justo lo que hay que arreglar: `COLS = ("cover_image_local", "banner_image_local")`
      es una **lista escrita a mano**. Una columna nueva no entra en ella sola, y el esquema v8 acaba de
      añadir una. Ese es el fallo de diseño a corregir, no el código a copiar tal cual (la forma del
      script sí: docstring con el porqué, `main() -> int`, `sys.exit(main())`).
    - `apps/backend/nyanko_api/database.py` líneas 175-217 — `media_details_cache`. Ahí conviven columnas
      que guardan URLs **remotas legítimas** (`cover_image`, `banner_image`, `site_url` — el CDN del
      proveedor) con las que guardan **rutas locales** (`cover_image_local`, `banner_image_local`). La
      guardia tiene que distinguirlas, y esa distinción es la lista blanca.
    - `apps/backend/nyanko_api/database.py` líneas 399-420 — `_migrate_asset_urls_to_relative`: la
      migración que ya recorta las URLs absolutas ya escritas. La guardia es lo que impide que vuelvan.
    - `apps/backend/nyanko_api/database.py` líneas 14-271 — el `SCHEMA` completo, para entender qué
      tablas hay. **No lo transcribas a una lista**: la guardia lo descubre en runtime.
    - `.planning/phases/01-fundaciones-limitador-esquema-y-modelo-de-progreso/01-02-SUMMARY.md` — qué
      columnas añadió realmente el esquema v8.
  </read_first>

  <files>apps/backend/tests/test_persisted_urls.py</files>

  <behavior>
    - Sobre una BD inicializada y sembrada de filas, el test recorre **todas** las tablas de
      `sqlite_master` y **todas** sus columnas, y falla si alguna fila guarda un valor de texto que
      empiece por `http`.
    - Una columna en `REMOTE_URL_ALLOWLIST` queda exenta.
    - Sembrar `media_details_cache.cover_image_local` con `http://127.0.0.1:8765/assets/x.jpg` hace
      **fallar** el test. Ese es exactamente el bug que ya ocurrió.
    - Sembrar `media_details_cache.cover_image` (CDN remoto, en la lista blanca) con `https://...` **no**
      hace fallar el test.
    - Añadir una tabla o una columna nueva al esquema y guardar en ella una URL absoluta hace fallar el
      test **sin haber tocado el test**.
  </behavior>

  <action>
    Escribe `tests/test_persisted_urls.py`. La guardia descubre el esquema en runtime: enumera las tablas
    con `SELECT name FROM sqlite_master WHERE type='table'`, y para cada una saca sus columnas con
    `PRAGMA table_info(...)`. Para cada columna no exenta, ejecuta un `SELECT` que busque filas cuyo
    valor `LIKE 'http%'`. Cero listas de columnas escritas a mano — eso es lo que hace que la guardia
    cubra el esquema v8 y todo lo que venga después **sin que nadie se acuerde de actualizarla**. La
    guardia que hay que actualizar a mano es la guardia que un día no se actualiza.

    `REMOTE_URL_ALLOWLIST` es un `frozenset` de tuplas `(tabla, columna)`. Deriva su contenido
    **corriendo la guardia** contra una BD real ya poblada (usa la copia migrada que produjo el plan
    01-02) y justificando **cada** acierto uno por uno: o es una URL remota legítima (el CDN del
    proveedor, el `siteUrl` de AniList, el RSS de un `torrent_source`) y entra en la lista blanca con su
    motivo escrito al lado, o es un bug y se arregla. No metas nada en la lista blanca «para que pase».

    Dos exclusiones que **no** son lista blanca y hay que tratar aparte, diciéndolo en un comentario:
    las columnas `*_json` y `cache.payload` guardan payloads JSON del proveedor que contienen URLs
    remotas dentro, pero el valor de la columna no *empieza* por `http` (empieza por `{` o `[`), así que
    la guardia no las toca. Si algún día una de ellas empezara por `http`, querríamos enterarnos.

    **La regla dura**: ninguna columna cuyo nombre acabe en `_local` o en `_path` puede estar en la lista
    blanca. Esas son, por definición, rutas locales, y son exactamente las dos que se llevaron por delante
    las portadas. Afírmalo como un test propio, para que la lista blanca no pueda usarse nunca para
    silenciar la reincidencia del bug que este proyecto ya sufrió.
  </action>

  <acceptance_criteria>
    - `tests/test_persisted_urls.py` NO contiene ninguna lista literal de nombres de tabla ni de columnas
      a inspeccionar: las obtiene de `sqlite_master` y `PRAGMA table_info` en runtime. (La lista blanca
      sí es literal — es una lista de *exenciones*, no de cobertura.)
    - El test cubre las columnas del esquema v8 sin nombrarlas: añadir una columna de texto nueva al
      `SCHEMA` y sembrarla con `http://...` hace fallar el test sin editarlo. **Demuéstralo**: hazlo,
      observa el fallo, deshaz el cambio, y deja constancia en el SUMMARY.
    - Sembrar `media_details_cache.cover_image_local` con `http://127.0.0.1:8765/assets/x.jpg` hace fallar
      el test con un mensaje que nombra la tabla, la columna y el número de filas afectadas.
    - `REMOTE_URL_ALLOWLIST` tiene una justificación escrita por entrada.
    - `test_allowlist_never_covers_local_columns`: existe y afirma que ninguna entrada de la lista blanca
      tiene un nombre de columna acabado en `_local` ni en `_path`.
    - `python -m pytest tests/test_persisted_urls.py -q` en verde.
  </acceptance_criteria>

  <verify>
    <automated>cd apps/backend && python -m pytest tests/test_persisted_urls.py -q</automated>
  </verify>

  <done>La guardia cubre todo el esquema por construcción, y no se puede desactivar metiendo una columna `_local` en la lista blanca.</done>
</task>

<task type="auto">
  <name>Task 2: La misma guardia, contra la BD real</name>

  <read_first>
    - `apps/backend/scripts/check_stale_asset_ports.py` — la forma del script (ruta de la BD por
      `%APPDATA%`, `mode=ro`, salida legible, código de salida).
    - `apps/backend/scripts/verify_real_db_migration.py` — el script del plan 01-02. La copia migrada a
      v8 que produce es contra la que hay que correr esta guardia.
    - `tests/test_persisted_urls.py` — la guardia que acabas de escribir. La lógica de detección se
      comparte; no la escribas dos veces.
  </read_first>

  <files>apps/backend/scripts/check_persisted_urls.py</files>

  <action>
    Escribe `scripts/check_persisted_urls.py`: la misma guardia, ejecutable contra un fichero de BD
    cualquiera (por defecto, la de producción en `%APPDATA%\app.nyanko.desktop\nyanko.sqlite3`), abierta
    siempre en `mode=ro`. Imprime, por tabla y columna, cuántas filas empiezan por `http`, y sale con 1
    si hay alguna fuera de la lista blanca.

    El test de la Task 1 corre contra una BD sintética en CI (siempre verde, siempre rápido). Este script
    corre contra los **datos reales**, que es donde el bug ocurrió de verdad. Los dos hacen falta: un
    fixture no habría encontrado nunca las portadas rotas.

    La detección y la lista blanca son una sola implementación compartida entre el test y el script — que
    diverjan es cómo la guardia acaba diciendo cosas distintas según quién la llame.

    Corre el script contra la **copia migrada a v8** del plan 01-02 y pega la salida en el SUMMARY.
    `check_stale_asset_ports.py` se queda donde está: sigue siendo útil (compara contra el puerto vivo,
    que es una comprobación distinta), y borrarlo no es trabajo de esta fase.
  </action>

  <acceptance_criteria>
    - `python scripts/check_persisted_urls.py <ruta-de-la-copia-v8>` sale con código 0.
    - La salida enumera las columnas de la lista blanca que sí traen URLs remotas (con recuentos), para
      que el humano vea que la guardia está mirando de verdad y no pasando en vacío.
    - La BD real **no se modifica**: solo se abre `mode=ro`.
    - La lógica de detección y la lista blanca no están duplicadas entre el test y el script: una importa
      de la otra.
    - La salida completa está pegada en `01-03-SUMMARY.md`.
  </acceptance_criteria>

  <verify>
    <automated>cd apps/backend && python -m pytest tests/test_persisted_urls.py -q && python scripts/check_persisted_urls.py</automated>
  </verify>

  <done>La guardia está probada contra la BD real de 33 MB migrada a v8, no solo contra un fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proceso del sidecar → BD persistida | El puerto del sidecar es **efímero** (cae a uno libre si el 8765 está ocupado). Todo lo que se persista con ese puerto dentro queda muerto en el siguiente arranque. |
| BD persistida → renderer | Lo que el renderer recibe como URL de asset. Una URL con host/puerto dentro es un enlace roto permanente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of Service | columnas de asset persistidas | high | mitigate | **Ya ocurrió**: la biblioteca entera se quedó sin portadas al persistir `http://127.0.0.1:<puerto-efímero>/assets/...`. No se curó sola. El reader de la Fase 3 multiplica la superficie por diez (cada página de cada capítulo). Mitigación: guardia genérica por `PRAGMA table_info` (Task 1) que cubre el esquema v8 y todo lo futuro sin mantenimiento, más el script contra datos reales (Task 2). |
| T-03-02 | Tampering | `REMOTE_URL_ALLOWLIST` | medium | mitigate | La lista blanca es el mecanismo por el que un ejecutor futuro, con prisa, silencia un acierto real («lo meto en la lista y pasa»). Mitigación: regla dura de que ninguna columna `*_local` / `*_path` puede estar en la lista blanca, afirmada por su propio test, y justificación escrita obligatoria por entrada. |
| T-03-03 | Information disclosure | `check_persisted_urls.py` | low | accept | La salida del script imprime nombres de tabla/columna y recuentos de una BD local del propio usuario, en su propia máquina. Sin URLs completas ni tokens. Aceptado. |

**Sin instalación de paquetes en este plan** — solo stdlib (`sqlite3`, `os`, `sys`). No aplica el Package Legitimacy Gate; no hay `T-03-SC`.
</threat_model>

<verification>
1. `cd apps/backend && python -m pytest -q` — suite completa en verde.
2. `python scripts/check_persisted_urls.py` contra la copia v8 — código 0, salida pegada en el SUMMARY.
3. La demostración de que la guardia tiene dientes (columna nueva sembrada con `http://` → el test falla sin editarlo) está documentada en el SUMMARY.
4. `ruff check tests/test_persisted_urls.py scripts/check_persisted_urls.py` limpio.
</verification>

<success_criteria>
- FND-05: un test de guardia falla si **cualquier** columna persistida empieza por `http`, incluidas las nuevas del esquema v8 — y sin que nadie tenga que acordarse de añadirlas.
- La guardia se ha ejercitado contra la BD real, no solo contra un fixture.
- La lista blanca no puede usarse para silenciar una columna `_local`.
</success_criteria>

<output>
Create `.planning/phases/01-fundaciones-limitador-esquema-y-modelo-de-progreso/01-03-SUMMARY.md` when done
</output>
