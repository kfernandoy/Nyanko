---
phase: 01-fundaciones-limitador-esquema-y-modelo-de-progreso
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/specs/progress-model.md
  - apps/backend/nyanko_api/database.py
  - apps/backend/nyanko_api/progress.py
  - apps/backend/nyanko_api/main.py
  - apps/backend/tests/test_progress.py
  - apps/backend/tests/test_database.py
  - apps/backend/scripts/verify_real_db_migration.py
autonomous: true
requirements: [FND-04, FND-06]

must_haves:
  truths:
    - "El capítulo 10.5 se guarda con su decimal intacto en local y llega al proveedor como 10."
    - "La guarda monotónica compara contra el progreso del TRACKER (remote_library_entries.progress), no contra el local — y lo hace por construcción, vía Database.tracker_progress, no por convención."
    - "Sin valor del tracker conocido, la guarda falla cerrado: no propone subir nada."
    - "progress_before guarda el valor QUE EL TRACKER TENÍA — no un 0 de relleno: el undo lo escribe de vuelta en el proveedor."
    - "La pareja (progress, chapter_progress) tiene una regla de divergencia decidida y escrita, porque progress tiene cinco escritores que no tocarán chapter_progress."
    - "La migración a v8 corre contra una copia de la BD real de producción, dispara el backup, y sale con integrity_check ok y los mismos recuentos por tabla."
  artifacts:
    - docs/specs/progress-model.md
    - apps/backend/nyanko_api/progress.py
    - apps/backend/scripts/verify_real_db_migration.py
  key_links:
    - "CANONICAL_SCHEMA_VERSION ↔ _requires_canonical_migration ↔ _backup_before_migration — subir la constante es lo que dispara el backup. Es el único rollback que existe."
    - "progress.next_progress ↔ Database.tracker_progress ↔ remote_library_entries.progress — el lector es lo que hace que 'compara contra el tracker' sea construcción y no convención."
    - "progress.effective_chapter ↔ database.py:2604 — el sync del tracker mueve `progress` sin tocar `chapter_progress`; la reconciliación al leer es lo que absorbe esa divergencia."
    - "main.py:2517 y main.py:3001 ↔ main.py:3792 — enqueue_mutation sin progress_before deja al undo sin nada que restaurar; y CON un progress_before equivocado, el undo escribe ese número equivocado en el tracker real."
---

<objective>
Escribir, decidir y migrar el modelo de progreso: capítulo decimal en local, `floor()` al proveedor,
guarda monotónica contra el tracker, `progress_before` en cada sync (FND-04) — y ejercitar la
migración a schema v8 contra una copia de la BD real de producción (FND-06).

Purpose: `library_entries.progress` es `INTEGER NOT NULL DEFAULT 0` y el capítulo 10.5 no tiene dónde
ir. Cambiar la semántica de `progress` **después** de que los usuarios hayan escrito filas es migrar
una biblioteca real de 2.774 entradas y 25.740 episodios. Esta es la última ventana barata.
Output: `docs/specs/progress-model.md` (la decisión, escrita), schema v8 aditivo, un módulo puro
`progress.py` que la Fase 5 consumirá, y un script que prueba la migración contra la BD de verdad.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/backend/nyanko_api/database.py
</context>

## Artifacts this phase produces (Plan 02)

| Símbolo | Fichero | Qué es |
|---------|---------|--------|
| `docs/specs/progress-model.md` | nuevo | La decisión escrita: por qué columna aditiva y no rebuild de tabla |
| `CANONICAL_SCHEMA_VERSION = 8` | `nyanko_api/database.py` | Sube de 7 → 8. Es lo que dispara `_backup_before_migration` |
| `library_entries.chapter_progress` | schema (columna nueva) | `REAL` — el capítulo local con decimal (10.5). `NULL` para anime |
| `nyanko_api/progress.py` | nuevo módulo | Puro, sin I/O, sin dependencias del proyecto |
| `progress.to_provider(chapter)` | `nyanko_api/progress.py` | `float → int` por `floor()`. 10.5 → 10 |
| `progress.next_progress(...)` | `nyanko_api/progress.py` | La guarda monotónica. Devuelve el valor a enviar, o `None` si no hay que enviar nada |
| `progress.effective_chapter(progress, chapter_progress)` | `nyanko_api/progress.py` | La reconciliación de la pareja: `chapter_progress` solo vale si `floor(chapter_progress) == progress`; si no, cae a `float(progress)` |
| `Database.set_chapter_progress(...)` | `nyanko_api/database.py` | Escribe `chapter_progress` (REAL) y `progress` (INTEGER, floored) en la misma transacción. **No** es el único escritor de `progress` — hay cinco más |
| `Database.tracker_progress(media_id, account_id)` | `nyanko_api/database.py` | Lee `remote_library_entries.progress` — el valor **del tracker**. Es lo que alimenta la guarda monotónica |
| `scripts/verify_real_db_migration.py` | nuevo script | Copia la BD real, migra, comprueba `integrity_check` + recuentos + backup |
| `tests/test_progress.py` | nuevo | Tabla de casos del modelo de progreso |

<tasks>

<task type="auto">
  <name>Task 1: Escribir la decisión, y luego el schema v8 que la implementa</name>

  <read_first>
    - `apps/backend/nyanko_api/database.py` líneas 155-174 — `library_entries` (`progress INTEGER NOT NULL DEFAULT 0`) y `remote_library_entries` (`progress INTEGER NOT NULL DEFAULT 0`, el espejo del tracker).
    - `apps/backend/nyanko_api/database.py` líneas 23-34 — `playback_events`, con `progress_before` / `progress_after` INTEGER.
    - `apps/backend/nyanko_api/database.py` líneas 273 y 292-366 — `CANONICAL_SCHEMA_VERSION = 7` e `initialize()`. Fíjate en la forma: el esquema es idempotente (`CREATE TABLE IF NOT EXISTS` + una lista de `_add_column`), y la versión se inserta al final. **Este es el patrón de migración de la casa. Síguelo.**
    - `apps/backend/nyanko_api/database.py` líneas 557-584 — `_requires_canonical_migration()` y **`_backup_before_migration()`**. YA EXISTEN. `initialize()` llama al backup si la versión aplicada es < `CANONICAL_SCHEMA_VERSION`. **No escribas un backup nuevo**: subir la constante a 8 es, por sí solo, lo que arma el backup en la próxima BD de producción que se abra.
    - `apps/backend/nyanko_api/database.py` líneas 502-555 — `_migrate_torrent_filters`. Es el ÚNICO precedente de rebuild de tabla en el árbol (rename → create → copy → drop). Léelo para saber lo que estás rechazando.
    - `apps/backend/nyanko_api/database.py` línea 117 — `episodes.episode_number` ya es `REAL`. El proyecto ya sabe guardar números de episodio decimales; es `library_entries.progress` quien se quedó en INTEGER.
  </read_first>

  <files>docs/specs/progress-model.md, apps/backend/nyanko_api/database.py</files>

  <action>
    **Primero el documento** (`docs/specs/progress-model.md`, en español, corto). FND-04 dice que el
    modelo está *escrito*; escribirlo después del código es escribir una justificación, no una decisión.
    Tiene que dejar fijado:

    - **Dos números, no uno.** `library_entries.progress` (INTEGER) es *lo que el tracker tiene o va a
      tener*: los proveedores solo aceptan enteros (`$progress: Int!`). `library_entries.chapter_progress`
      (REAL, nuevo, `NULL` para anime) es *lo que el usuario leyó de verdad*: 10.5. `floor()` es la
      función que va del segundo al primero, y solo se aplica al cruzar hacia el proveedor.
    - **Por qué columna aditiva y NO rebuild de `library_entries`.** SQLite no tiene `ALTER COLUMN TYPE`;
      cambiar `progress` a REAL exige el rebuild de 12 pasos sobre 2.774 filas vivas, y devolvería
      `10.0` donde hoy la API devuelve `10` — un cambio de tipo visible desde el renderer, en la ruta que
      comparte con el anime, para nada. Un `ALTER TABLE ADD COLUMN` es aditivo, no reescribe una sola
      fila existente, y por construcción **no puede alterar los recuentos por tabla** que el criterio de
      éxito 3 exige comparar. Escribe esto como decisión, con el rebuild nombrado y rechazado.
    - **La guarda monotónica compara contra el tracker.** El valor de referencia es
      `remote_library_entries.progress` (el espejo del proveedor), no `library_entries.progress` (el
      local, que la UI ya movió de forma optimista). Comparar contra el local es cómo se empuja un `1`
      encima de una serie terminada.
    - **`progress_before` se graba en cada sync**, porque es lo único que hace posible el deshacer.

    - **La divergencia de la pareja, decidida (sección obligatoria).** `library_entries.progress` **ya
      tiene cinco escritores** en el árbol y ninguno va a tocar `chapter_progress`:
      `database.py:1226`, `:1369`, `:2153` (inserts desde la lista remota) y sobre todo
      **`database.py:2604`** (`UPDATE library_entries SET progress = ?` — el sync del tracker) y
      `:2631`. En cuanto el usuario edite su progreso en la web de AniList, o desde otro dispositivo, el
      sync moverá `progress` y `chapter_progress` quedará obsoleto. Escribir «`set_chapter_progress` es
      el único escritor autorizado de la pareja» sería **falso el primer día**. La decisión, que la Fase
      5 hereda y no tiene que adivinar:

      > `progress` (INTEGER) es **siempre** autoritativo. `chapter_progress` (REAL) solo es válido
      > mientras `floor(chapter_progress) == progress`. Si no cuadran, el tracker se movió por debajo y
      > `chapter_progress` es basura: se ignora y se cae a `float(progress)`.

      Esta regla se **evalúa al leer**, no se mantiene al escribir. Por eso no hay que tocar los cinco
      escritores (ni acordarse del sexto que alguien añada en la Fase 5): la reconciliación es una
      función pura, `progress.effective_chapter(progress, chapter_progress)`, y es imposible que se
      quede desincronizada porque no hay nada que sincronizar. Un invariante que hay que mantener en
      cinco sitios es un invariante que se rompe; uno que se deriva al leer, no.

    **Luego el esquema.** En `database.py`: sube `CANONICAL_SCHEMA_VERSION` de 7 a 8, añade
    `chapter_progress REAL` a la definición de `library_entries` dentro de `SCHEMA`, y añade la llamada
    `self._add_column(connection, "library_entries", "chapter_progress", "REAL")` en `initialize()`,
    junto a las de `started_at` / `completed_at` que ya están ahí. Nada más. Sin columnas especulativas
    para la Fase 4 ni para la 5 — el vínculo fuente↔tracker es esquema de la Fase 4, y meterlo aquí es
    inventarse un modelo que aún no se ha discutido.

    Añade `Database.set_chapter_progress(media_id, chapter)`: escribe `chapter_progress` (el REAL) y
    `progress` (el `floor()`) **en la misma transacción**. Es el único escritor que mantiene la pareja
    *coherente* — pero explícitamente **no** es el único escritor de `progress`, y el modelo no puede
    fingir que lo es.

    Añade también `Database.tracker_progress(media_id, account_id) -> int | None`: lee
    `remote_library_entries.progress`, que es el valor **del tracker**. Precedente literal del SELECT en
    `database.py:2147`. Es una línea, y es lo que convierte «la guarda compara contra el tracker» de
    convención en construcción: sin este lector, `next_progress` recibe su `tracker_progress` de
    dondequiera que el llamador de la Fase 5 decida, que es exactamente el fallo que la Fase 4 existe
    para prevenir.
  </action>

  <acceptance_criteria>
    - `docs/specs/progress-model.md` existe y nombra explícitamente: las dos columnas y sus tipos, la
      tabla contra la que compara la guarda monotónica (`remote_library_entries`), y el rebuild de tabla
      como alternativa **considerada y rechazada** con su motivo.
    - `docs/specs/progress-model.md` contiene una sección de **divergencia** que (a) nombra los cinco
      escritores existentes de `library_entries.progress` por fichero:línea, (b) declara `progress` como
      autoritativo, y (c) declara `chapter_progress` válido solo si `floor(chapter_progress) == progress`.
      El documento **no** afirma en ningún sitio que `set_chapter_progress` sea el único escritor de
      `progress` — porque es falso.
    - `Database.tracker_progress(media_id, account_id)` existe y lee de `remote_library_entries`.
      Comprobación: devuelve el valor sembrado en `remote_library_entries.progress` y **no** el de
      `library_entries.progress` cuando los dos difieren.
    - `python -c "from nyanko_api.database import CANONICAL_SCHEMA_VERSION; assert CANONICAL_SCHEMA_VERSION == 8"` pasa.
    - Sobre una BD nueva: `PRAGMA table_info(library_entries)` incluye `chapter_progress` con `type` = `REAL`.
    - Sobre una BD v7 preexistente con filas: tras `initialize()`, `chapter_progress` existe, es `NULL` en
      todas las filas viejas, y `SELECT COUNT(*) FROM library_entries` **no ha cambiado**.
    - `set_chapter_progress(media_id, 10.5)` deja `chapter_progress = 10.5` **y** `progress = 10` en la
      misma fila. Leer `chapter_progress` devuelve `10.5`, no `10`.
    - `initialize()` sobre una BD v7 no vacía crea un fichero `*.backup-v8-*.sqlite3` junto a la BD.
    - `python -m pytest tests/test_database.py -q` en verde.
  </acceptance_criteria>

  <verify>
    <automated>cd apps/backend && python -m pytest tests/test_database.py -q</automated>
  </verify>

  <done>El modelo está escrito y decidido; v8 es aditivo y el backup se dispara solo por subir la constante.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: progress.py — la guarda monotónica, pura y con su tabla de casos</name>

  <read_first>
    - `docs/specs/progress-model.md` — el documento que acabas de escribir. Es la especificación de este módulo.
    - `apps/backend/nyanko_api/main.py` líneas 3480-3520 y 3620-3660 — el camino de confirmación de
      reproducción de anime: cómo se calcula `previous_progress` y cómo se pasan `progress_before` /
      `progress_after` a `update_playback_event`. Es el precedente del comportamiento que la Fase 5
      reutilizará para manga.
    - `apps/backend/nyanko_api/main.py` líneas 2510-2520 y 2995-3005 — **los dos huecos**: aquí se llama a
      `enqueue_mutation` pasando `progress_after=...` **sin** `progress_before`.
    - `apps/backend/nyanko_api/main.py` línea 3792 — `restored_progress = last_event["progress_before"]`.
      Ese es el deshacer. Con `progress_before` a NULL, no restaura nada.
    - `apps/backend/nyanko_api/database.py` líneas 163-174 — `remote_library_entries`: `progress` es el
      valor del tracker y `account_id` lo cualifica por cuenta.
  </read_first>

  <files>apps/backend/nyanko_api/progress.py, apps/backend/tests/test_progress.py, apps/backend/nyanko_api/main.py</files>

  <behavior>
    Tabla de casos de `next_progress(chapter, tracker_progress, tracker_status)` — escríbela **antes**
    que el código, como `pytest.mark.parametrize`:

    - `chapter=10.5, tracker=9` → devuelve `10` (floor, y sube).
    - `chapter=11.0, tracker=10` → devuelve `11`.
    - `chapter=10.5, tracker=10` → devuelve `None` (floor(10.5) == 10 == el tracker: no hay nada que subir; subir 10 otra vez es ruido).
    - `chapter=9.0, tracker=12` → devuelve `None` (regresión: la guarda monotónica muerde).
    - `chapter=10.5, tracker=None` → devuelve `None` (**falla cerrado**: sin valor del tracker no se
      escribe a ciegas. Que la Fase 5 tenga que ir a buscarlo es exactamente lo que queremos).
    - `chapter=1.0, tracker=24, status="COMPLETED"` → devuelve `None` y señala relectura (no empuja un
      `1` encima de una serie terminada; SYN-04 decidirá la UX de `REPEATING` en la Fase 5, aquí solo
      **no se escribe**).
    - `chapter=0.5, tracker=0` → devuelve `None` (floor(0.5) == 0 == el tracker).
    - `to_provider(10.5) == 10`, `to_provider(10.0) == 10`, `to_provider(1) == 1`.

    Y la reconciliación decidida en la Task 1, `effective_chapter(progress, chapter_progress)`:

    - `progress=10, chapter_progress=10.5` → `10.5` (cuadran: `floor(10.5) == 10`).
    - `progress=12, chapter_progress=10.5` → `12.0` (el tracker se movió por debajo — p. ej. el sync de
      `database.py:2604` — y `chapter_progress` es basura).
    - `progress=10, chapter_progress=None` → `10.0` (anime, o manga que nunca pasó por el reader).
  </behavior>

  <action>
    Crea `nyanko_api/progress.py`: módulo **puro**, sin `sqlite3`, sin `httpx`, sin imports del proyecto.
    Dos funciones: `to_provider(chapter: float) -> int` (que es `math.floor` con un tipo honesto) y
    `next_progress(...)`, que devuelve el entero a enviar al proveedor o `None` cuando no hay que enviar
    nada. Que devuelva `None` en vez de lanzar es deliberado: «no hay que subir nada» es un resultado
    normal, no un error; el llamador de la Fase 5 lo trata como «no encolar».

    Puro y testeable significa que la Fase 5 no puede reimplementar esta lógica dentro de un endpoint
    — que es exactamente como acabaría embadurnada entre el reader y el sync y sin testear en ninguno de
    los dos. Este módulo es la única definición.

    Escribe `tests/test_progress.py` con la tabla de casos de arriba como `parametrize`. Los casos
    primero, el módulo después.

    **Y cierra los dos huecos de `main.py`** (`:2517` y `:3001`): encolan una mutación de progreso
    pasando `progress_after` **sin** `progress_before`. Lee el progreso previo **del tracker** y pásalo.
    El precedente exacto de dónde sacarlo está en el camino de anime que ya funciona: `main.py:3469` y
    `:3604` hacen `previous_progress = details.list_entry.progress if details.list_entry else 0` — el
    valor del proveedor, no el local.

    **Grabar el número equivocado es peor que no grabar nada.** El deshacer de `main.py:3792` hace
    `restored_progress = last_event["progress_before"]` y lo **escribe de vuelta en el tracker**. Un
    `progress_before = 0` puesto de relleno para que la columna no sea NULL convierte el botón de
    deshacer en «pon mi progreso real de AniList a cero». `progress_before` es el valor que el tracker
    tenía, o no es nada.
  </action>

  <acceptance_criteria>
    - `nyanko_api/progress.py` no importa `sqlite3`, ni `httpx`, ni ningún módulo de `nyanko_api`.
      Comprobación: `python -c "import ast,sys; src=open('nyanko_api/progress.py').read(); mods=[n.module or '' for n in ast.walk(ast.parse(src)) if isinstance(n,ast.ImportFrom)]+[a.name for n in ast.walk(ast.parse(src)) if isinstance(n,ast.Import) for a in n.names]; bad=[m for m in mods if m.startswith(('nyanko_api','sqlite3','httpx'))]; assert not bad, bad"`
    - `tests/test_progress.py` cubre **los ocho casos** de `next_progress` y **los tres** de
      `effective_chapter`, vía `parametrize`.
    - `next_progress(chapter=10.5, tracker_progress=None, ...)` devuelve `None` — falla cerrado.
    - **`progress_before` guarda el valor DEL TRACKER, y el test lo distingue del local.** Siembra
      `remote_library_entries.progress = 7` y `library_entries.progress = 3` (valores **distintos**, y
      ninguno de los dos `0`), recorre el endpoint de edición de entrada, y afirma que la fila resultante
      de `playback_events` tiene **`progress_before == 7`**. Un `assert progress_before is not None`
      **no vale**: lo pasa un `0` de relleno, y ese `0` es lo que el deshacer de `main.py:3792`
      escribiría en el AniList real del usuario. Igualmente, `progress_before == 3` (el local) debe hacer
      fallar el test — por eso los valores sembrados tienen que diferir.
    - `python -m pytest tests/test_progress.py tests/test_database.py -q` en verde.
    - `ruff check nyanko_api/progress.py` limpio.
  </acceptance_criteria>

  <verify>
    <automated>cd apps/backend && python -m pytest tests/test_progress.py tests/test_database.py -q && ruff check nyanko_api/progress.py</automated>
  </verify>

  <done>La guarda vive en un módulo puro, compara contra el tracker, falla cerrado, y `progress_before` se graba en todos los caminos.</done>
</task>

<task type="auto">
  <name>Task 3: Migrar una copia de la BD real (FND-06)</name>

  <read_first>
    - `apps/backend/scripts/check_stale_asset_ports.py` — ENTERO. Es el precedente de script de
      verificación de la casa: abre la BD real en `mode=ro`, imprime lo que encuentra, y sale con código
      1 si falla. Copia esa forma (docstring que explica el porqué, `main() -> int`, `sys.exit(main())`).
    - `apps/backend/nyanko_api/database.py` líneas 292-366 (`initialize`), 557-584
      (`_requires_canonical_migration`, `_backup_before_migration`).
    - La BD real de producción está en `%APPDATA%\app.nyanko.desktop\nyanko.sqlite3` (~33 MB, hoy en
      `schema_migrations` v7). La de desarrollo, en `apps/backend/data/nyanko.sqlite3`.
  </read_first>

  <files>apps/backend/scripts/verify_real_db_migration.py</files>

  <action>
    Escribe `scripts/verify_real_db_migration.py`. Acepta la ruta de la BD como argumento y por defecto
    la de producción (`%APPDATA%\app.nyanko.desktop\nyanko.sqlite3`). Hace, en este orden:

    1. **Copia** la BD a un directorio temporal. **NUNCA** migra la original — abre siempre la original
       en `mode=ro` para el censo previo. Si el script puede escribir sobre la BD real del usuario,
       está mal escrito.
    2. Censo previo sobre la copia: `PRAGMA integrity_check`, `MAX(version)` de `schema_migrations`, y
       `COUNT(*)` de **todas** las tablas (enumeradas con `sqlite_master`, no una lista escrita a mano —
       una lista a mano deja de cubrir la tabla que alguien añada mañana).
    3. Corre `Database(copia).initialize()`. Esa es la migración: la de verdad, la del arranque.
    4. Censo posterior: `integrity_check` de nuevo, versión, y `COUNT(*)` de todas las tablas otra vez.
    5. Comprueba y **falla con código 1** si: `integrity_check` no es `ok`; la versión no es 8; el
       recuento de alguna tabla cambió; no apareció el fichero `*.backup-v8-*` junto a la copia; o
       `chapter_progress` no está en `library_entries`.
    6. Imprime la tabla comparativa antes/después. El humano tiene que poder leer el resultado.

    Los recuentos se **comparan antes contra después sobre la misma copia**. No los escribas en el
    código: el ROADMAP dice 2.761/25.727 y la BD real de hoy trae 2.774/25.740 — ya ha crecido. Un
    recuento horneado es un test que se rompe solo, y que además no prueba lo que hace falta probar (que
    la migración no perdió filas).

    Corre el script contra la copia de la BD real y pega su salida en el SUMMARY. Es la evidencia de
    FND-06: un fixture no vale, y la salida es lo que lo demuestra.
  </action>

  <acceptance_criteria>
    - `python scripts/verify_real_db_migration.py` sale con código 0 contra una copia de
      `%APPDATA%\app.nyanko.desktop\nyanko.sqlite3`.
    - La salida muestra `integrity_check: ok` **antes y después**.
    - La salida muestra, tabla por tabla, el recuento antes y después, e **iguales** — con
      `library_entries` y `episodes` entre ellas, con sus valores reales de hoy (no los del ROADMAP).
    - La salida confirma `schema_migrations: 7 → 8` y la aparición del fichero `*.backup-v8-*.sqlite3`.
    - La BD original **no se ha modificado**: su `mtime` y su tamaño son los mismos antes y después de
      correr el script, y su `MAX(version)` de `schema_migrations` sigue en 7 hasta que la app real
      arranque. Compruébalo y déjalo escrito.
    - La enumeración de tablas sale de `sqlite_master`, no de una lista literal en el código.
    - La salida completa del script está pegada en `01-02-SUMMARY.md`.
  </acceptance_criteria>

  <verify>
    <automated>cd apps/backend && python scripts/verify_real_db_migration.py</automated>
  </verify>

  <done>La migración v8 está probada contra la BD real de 33 MB, con backup, integridad y recuentos idénticos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código de migración → BD real del usuario | 33 MB, 2.774 entradas de biblioteca, 25.740 episodios, irreemplazables. El backup pre-migración es el **único** rollback que existe. |
| Nyanko → tracker (AniList/MAL/Kitsu) | Lo que cruza aquí se escribe en la lista **real** del usuario y `pending_mutations` lo reintenta de forma duradera. Un valor malo no se «deshace» solo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `Database.initialize` / migración v8 | high | mitigate | Una migración que corrompa o pierda filas destruye la biblioteca real. Mitigación: migración **aditiva** (`ALTER TABLE ADD COLUMN`, sin rebuild), `_backup_before_migration` disparado por la subida de `CANONICAL_SCHEMA_VERSION`, y `verify_real_db_migration.py` con `integrity_check` + recuentos por tabla antes/después sobre una **copia** (Task 3). |
| T-02-02 | Tampering | `progress.next_progress` | high | mitigate | Una guarda que compare contra el progreso **local** (que la UI ya movió optimistamente) puede empujar un valor inferior sobre el tracker y **sobrescribir el progreso real del usuario** en su AniList — con `pending_mutations` reintentándolo de forma duradera. Mitigación: la guarda compara contra `remote_library_entries.progress`, y devuelve `None` (falla cerrado) cuando el valor del tracker es desconocido. Cubierto por la tabla de casos. |
| T-02-03 | Tampering | `progress_before` incorrecto → undo destructivo | high | mitigate | Elevado de `medium`/Repudiation tras la revisión. No es solo que falte la traza: `main.py:3792` **escribe `progress_before` de vuelta en el tracker**. Un `0` de relleno (que un `assert NOT NULL` aceptaría tan contento) convierte el botón de deshacer en «pon a cero mi progreso real de AniList» — `pending_mutations` lo reintentaría además de forma duradera. Mitigación: los dos call sites (`main.py:2517`, `:3001`) toman el valor **del tracker** (precedente: `main.py:3469`/`:3604`), y el test siembra tracker=7 / local=3 y afirma `progress_before == 7`, de modo que ni el `0` ni el valor local lo pasan. |
| T-02-06 | Tampering | divergencia `progress` ↔ `chapter_progress` | medium | mitigate | `library_entries.progress` tiene cinco escritores (`database.py:1226`, `:1369`, `:2153`, `:2604`, `:2631`) y ninguno tocará `chapter_progress`; en cuanto el sync del tracker (`:2604`) mueva `progress`, la pareja diverge y el reader podría reanudar por un capítulo que el usuario ya no tiene. Mitigación: `progress` es autoritativo y `chapter_progress` solo vale si `floor(chapter_progress) == progress`; la regla se evalúa **al leer** (`progress.effective_chapter`), así que ningún escritor —presente o futuro— puede romperla por omisión. |
| T-02-04 | Tampering | `verify_real_db_migration.py` | high | mitigate | Un script de verificación que abra la BD **real** en modo escritura la migra sin backup del usuario y sin querer. Mitigación: la original solo se abre `mode=ro`; toda escritura ocurre sobre una copia en un temporal; criterio de aceptación explícito de que `mtime`/tamaño/versión de la original no cambian. |
| T-02-05 | Denial of Service | copia de 33 MB en temporal | low | accept | La copia ocupa disco durante la verificación. Es un script manual del desarrollador, no un camino de runtime. Aceptado. |

**Sin instalación de paquetes en este plan** — solo stdlib (`sqlite3`, `math`, `shutil`, `tempfile`, `pathlib`). No aplica el Package Legitimacy Gate; no hay `T-02-SC`.
</threat_model>

<verification>
1. `cd apps/backend && python -m pytest -q` — suite completa en verde.
2. `python scripts/verify_real_db_migration.py` — código 0, salida pegada en el SUMMARY.
3. `docs/specs/progress-model.md` existe y nombra el rebuild de tabla como alternativa rechazada.
4. `ruff check nyanko_api/ tests/ scripts/` limpio.
</verification>

<success_criteria>
- FND-04: 10.5 se guarda REAL en local, llega como 10 al proveedor, la guarda compara contra el tracker, y `progress_before` se graba en cada sync.
- FND-06: la migración v8 se ejercitó contra una copia de la BD real (no un fixture), con `integrity_check: ok`, recuentos idénticos y el backup disparado.
- El modelo está **escrito** antes que el código que lo implementa.
</success_criteria>

<output>
Create `.planning/phases/01-fundaciones-limitador-esquema-y-modelo-de-progreso/01-02-SUMMARY.md` when done
</output>
