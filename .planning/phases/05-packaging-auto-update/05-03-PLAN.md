---
phase: 05-packaging-auto-update
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/desktop/build/installer.nsh
  - apps/desktop/electron-builder.yml
autonomous: false
requirements: [PKG-01]
user_setup: []

must_haves:
  truths:
    - "Se ha PROBADO empíricamente, contra una instalación Tauri 0.1.15 real, si `uninstall.exe /S` borra o no `%APPDATA%\\app.nyanko.desktop` (D-02)."
    - "Tras instalar 0.2.0 sobre una 0.1.15 existente, la biblioteca del usuario sigue intacta (DATA-01) y Agregar/quitar programas muestra UNA sola entrada «Nyanko»."
    - "La rama de desinstalación solo se escribe si el experimento demostró que es segura."
  artifacts:
    - apps/desktop/build/installer.nsh
  key_links:
    - "Resultado del gate D-02 → qué fichero se toca: rama A escribe `installer.nsh`; rama B escribe `electron-builder.yml` (`nsis.guid`) y deja `installer.nsh` como lo dejó el Plan 01."
    - "GUID/UninstallString de Tauri (los reporta el experimento) → el valor literal que se cablea. No se adivina."
---

<objective>
Resolver el gate empírico D-02 y cablear ÚNICAMENTE la rama que el experimento demuestre segura,
para que un usuario de 0.1.15 (Tauri) acabe con una sola app instalada, una sola entrada en
Agregar/quitar programas, y su biblioteca intacta.

Purpose: PKG-01 (la mitad de migración del instalador) + DATA-01. La pérdida de la biblioteca es
el peor resultado disponible de toda la fase, y `%APPDATA%\app.nyanko.desktop` es exactamente lo
que el desinstalador NSIS de Tauri ofrece borrar.
Output: la rama de migración cableada, y el resultado del experimento documentado en el SUMMARY
(es un hecho sobre el mundo real que ningún plan futuro debería tener que re-descubrir).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-auto-update/05-CONTEXT.md
@.planning/phases/05-packaging-auto-update/05-PATTERNS.md
@.planning/phases/05-packaging-auto-update/05-01-PLAN.md
</context>

<por_que_esto_es_un_gate_y_no_un_supuesto>
El desinstalador NSIS de Tauri incluye una casilla «borrar los datos de la aplicación». Nadie
sabe, sin probarlo, qué hace esa casilla bajo `/S` (silencioso): el modo silencioso de NSIS toma
los valores por defecto de las páginas, y ese defecto no está documentado en ningún sitio que
valga como evidencia. Los datos en juego son `%APPDATA%\app.nyanko.desktop`: la biblioteca
entera del usuario (nyanko.db, historial, tokens de proveedor).

Razonar sobre el código del desinstalador NO cuenta como verificación (CONTEXT.md §specifics).
Se prueba en una instalación real, se anota el resultado, y ese resultado ELIGE la rama.
</por_que_esto_es_un_gate_y_no_un_supuesto>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: GATE EMPÍRICO D-02 — ¿el uninstall silencioso de Tauri borra %APPDATA%?</name>
  <what-built>
    Nada todavía, y a propósito: este checkpoint BLOQUEA la escritura de la rama de
    desinstalación. La Tarea 2 no puede empezar sin las tres respuestas de aquí abajo.
    Requiere una instalación REAL de Nyanko 0.1.15 (Tauri) — idealmente en una VM o en una
    máquina donde perder la biblioteca no importe. Si solo hay una máquina, **copiar
    `%APPDATA%\app.nyanko.desktop` entera a un backup ANTES de empezar**.
  </what-built>
  <how-to-verify>
    Instalar Nyanko 0.1.15 desde el release publicado
    (https://github.com/kfernandoy/Nyanko/releases) si no está ya instalado, abrirlo una vez
    para que cree su data dir, y entonces:

    1. **Localizar la entrada de desinstalación de Tauri** (PowerShell):

       Recorrer `HKCU:\Software\Microsoft\Windows\CurrentVersion\Uninstall\*` y
       `HKLM:\...\Uninstall\*`, buscando la entrada cuyo `DisplayName` sea `Nyanko`.
       Anotar **literalmente**:
       - el NOMBRE DE LA CLAVE (es el GUID del producto, con llaves — p.ej. `{XXXXXXXX-...}`),
       - la colmena en la que vive (HKCU o HKLM),
       - el valor `UninstallString` (y `QuietUninstallString` si existe),
       - el valor `InstallLocation`.

    2. **Fotografiar el data dir**: confirmar que existe
       `%APPDATA%\app.nyanko.desktop\nyanko.db` y anotar su tamaño y el número de ficheros del
       directorio.

    3. **Ejecutar el desinstalador en SILENCIO**: lanzar el `UninstallString` anotado con el
       flag `/S` y esperar a que termine (el proceso del uninstaller se relanza a sí mismo desde
       %TEMP%, así que hay que esperar unos segundos aunque el comando vuelva antes).

    4. **La pregunta que decide todo**: ¿sigue existiendo
       `%APPDATA%\app.nyanko.desktop\nyanko.db`, con el mismo tamaño?

    Reportar exactamente estas cuatro cosas:
    - **GUID / nombre de clave** de la entrada de desinstalación de Tauri, y su colmena.
    - **UninstallString** literal.
    - **InstallLocation** literal.
    - **¿SOBREVIVIÓ `%APPDATA%\app.nyanko.desktop`? SÍ / NO.**

    (Si el desinstalador silencioso además dejó basura en el directorio de instalación, decirlo
    también.)
  </how-to-verify>
  <resume-signal>Pega las cuatro respuestas. Con "SOBREVIVIÓ: SÍ" se cablea la rama A; con "NO", la rama B. Sin respuesta no se escribe ninguna de las dos.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: cablear la rama que el gate haya seleccionado (A: uninstall silencioso · B: instalar encima)</name>
  <read_first>
    - Las cuatro respuestas del gate (Tarea 1) — son los VALORES LITERALES que se cablean; no se adivina ninguno
    - apps/desktop/build/installer.nsh (tal como lo dejó el Plan 01: solo la macro `customInit` con el `taskkill` del sidecar, y su comentario de rationale)
    - apps/desktop/electron-builder.yml (Plan 01: `appId`, bloque `nsis` con `deleteAppDataOnUninstall: false`)
    - .planning/phases/05-packaging-auto-update/05-CONTEXT.md §D-02 (las dos ramas y por qué existen)
    - .planning/phases/05-packaging-auto-update/05-PATTERNS.md §`apps/desktop/build/installer.nsh` (la rama de uninstall NO tiene analog: es código nuevo)
  </read_first>
  <action>
    Escribir **UNA SOLA** de las dos ramas — la que el gate seleccionó. La otra NO se escribe
    (ni comentada, ni "por si acaso": una rama de borrado de datos latente en un `.nsh` es
    exactamente el tipo de cosa que alguien descomenta sin contexto dentro de seis meses).

    ---
    **RAMA A — el gate respondió SOBREVIVIÓ: SÍ.**

    Ampliar la macro `customInit` de `apps/desktop/build/installer.nsh` (la que ya tiene el
    `taskkill`) para que, ANTES de copiar ficheros, desinstale la app Tauri en silencio:
    - `ReadRegStr` del valor `UninstallString` en la clave EXACTA que reportó el gate (colmena y
      GUID literales del gate — no un GUID inventado).
    - Si el valor está vacío (no hay Tauri instalado: usuario nuevo), no hacer nada. Este
      `StrCmp` contra vacío es obligatorio: sin él, un usuario limpio se come un `ExecWait` de
      una ruta vacía.
    - Si no está vacío, `ExecWait` de esa ruta con `/S`, y continuar pase lo que pase (un
      uninstaller que falle no puede abortar la instalación de la versión nueva).
    - Comentario en español citando **D-02**, el resultado del experimento (fecha + «el uninstall
      silencioso NO borra %APPDATA%, verificado en instalación real»), y **DATA-01**. Ese
      comentario es la única defensa contra que un lector futuro «limpie» esto sin saber lo que
      protege.

    `electron-builder.yml` NO se toca en esta rama.

    ---
    **RAMA B — el gate respondió SOBREVIVIÓ: NO.**

    NO se ejecuta ningún desinstalador de Tauri: instalar encima. En `electron-builder.yml`:
    - `nsis.guid`: el GUID literal que reportó el gate. Esto hace que el instalador de Electron
      escriba su entrada de desinstalación en la MISMA clave de registro que usaba Tauri → una
      sola entrada en Agregar/quitar programas, y la entrada vieja (que apuntaba al uninstaller
      peligroso) queda SOBRESCRITA. Ese es el punto: no es cosmética, es lo que impide que el
      usuario dispare el uninstaller de Tauri por error más adelante.
    - `nsis.deleteAppDataOnUninstall: false` ya está (Plan 01, DATA-01) — verificar que sigue.
    - Comentario citando **D-02** con el resultado del experimento («el uninstall silencioso de
      Tauri SÍ borra %APPDATA% → no se ejecuta jamás; se instala encima») y **DATA-01**.

    `installer.nsh` se queda EXACTAMENTE como lo dejó el Plan 01 (solo el `taskkill`).

    Los ficheros del directorio de instalación de Tauri (`InstallLocation` del gate) quedan
    huérfanos en disco (~decenas de MB). Es basura inerte y aceptada: borrarla requeriría un
    `RMDir /r` sobre una ruta leída del registro, que es una primitiva de borrado recursivo
    mucho más peligrosa que el problema que resuelve. Anotarlo con un comentario `ponytail:`.

    ---
    En AMBAS ramas, registrar en el SUMMARY el resultado del gate y los valores literales
    (GUID, UninstallString, InstallLocation). Es un hecho empírico sobre el parque instalado:
    si se pierde, hay que re-hacer el experimento.
  </action>
  <verify>
    <automated>cd apps/desktop && node -e "const fs=require('fs'); const nsh=fs.readFileSync('build/installer.nsh','utf8'); const yml=fs.readFileSync('electron-builder.yml','utf8'); const a=[]; if(!/taskkill \/F \/IM nyanko-api\.exe/.test(nsh))a.push('el taskkill del Plan 01 desaparecio'); if(!/deleteAppDataOnUninstall:\s*false/.test(yml))a.push('DATA-01: deleteAppDataOnUninstall dejo de ser false'); const ramaA=/ReadRegStr/.test(nsh); const ramaB=/guid:/.test(yml); if(ramaA===ramaB)a.push('debe estar cableada EXACTAMENTE una rama: A (ReadRegStr en installer.nsh) o B (nsis.guid en electron-builder.yml), no las dos ni ninguna'); if(ramaA && !/StrCmp/.test(nsh))a.push('rama A sin la guarda de valor vacio (usuario sin Tauri instalado)'); if(ramaA && !/D-02/.test(nsh))a.push('rama A sin el comentario que cita D-02 y el resultado del experimento'); if(a.length){console.error(a.join('\n'));process.exit(1)} console.log('GATE D-02 CABLEADO:', ramaA?'rama A (uninstall silencioso)':'rama B (instalar encima)')"</automated>
  </verify>
  <acceptance_criteria>
    - Está cableada **exactamente una** rama: o `installer.nsh` lee el registro (A), o
      `electron-builder.yml` define `nsis.guid` (B). Nunca las dos, nunca ninguna.
    - El `taskkill` del sidecar que escribió el Plan 01 sigue en `customInit` (D-05 intacto).
    - `nsis.deleteAppDataOnUninstall` sigue siendo `false` (DATA-01).
    - Rama A: el GUID y la colmena en `ReadRegStr` coinciden LITERALMENTE con los reportados por
      el gate, y hay una guarda `StrCmp` para el caso «no hay Tauri instalado».
    - Rama B: el valor de `nsis.guid` coincide LITERALMENTE con el GUID reportado por el gate.
    - El fichero cableado cita `D-02` y el resultado del experimento en un comentario en español.
    - **Prueba end-to-end sobre instalación real** (la que da fe): con Nyanko 0.1.15 instalado y
      su biblioteca poblada, ejecutar `Nyanko-Setup-0.2.0.exe` y comprobar, después:
      1. `%APPDATA%\app.nyanko.desktop\nyanko.db` existe y conserva su tamaño;
      2. Nyanko 0.2.0 abre y la biblioteca carga con el mismo contenido;
      3. Agregar/quitar programas muestra **una** entrada «Nyanko» (no dos).
  </acceptance_criteria>
  <done>Un usuario de 0.1.15 puede instalar 0.2.0 sin perder la biblioteca y sin quedarse con dos apps instaladas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Instalador NSIS → `%APPDATA%\app.nyanko.desktop` | La biblioteca entera del usuario. Ningún camino del instalador puede escribir ahí, y menos borrar. |
| Registro de Windows (HKCU) → hook NSIS | En la rama A el instalador LEE una ruta del registro y la EJECUTA. HKCU es escribible por el propio usuario. |
| Desinstalador de Tauri → sistema | Código de terceros (el NSIS que generó Tauri 2) ejecutándose con la identidad del usuario durante nuestra instalación. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Denial of Service / data loss | `uninstall.exe /S` de Tauri sobre `%APPDATA%` | critical | mitigate | Es EL riesgo de la fase. Mitigación = el gate empírico de la Tarea 1: la rama que ejecuta el desinstalador solo se escribe si una prueba en instalación real demuestra que el modo silencioso preserva el data dir. Si no lo demuestra, esa rama no existe en el árbol (rama B: instalar encima, cero ejecuciones del uninstaller). Refuerzo: `deleteAppDataOnUninstall: false` (Plan 01) + criterio de aceptación end-to-end sobre una biblioteca real. |
| T-05-09 | Elevation of Privilege / Tampering | rama A: `ReadRegStr` de `UninstallString` + `ExecWait` | low | accept | El instalador ejecuta una ruta leída de HKCU. Un atacante que ya pueda escribir en HKCU del usuario tiene ejecución como ese usuario por vías mucho más directas (Run key, carpeta de Inicio): la instalación es per-user (`perMachine: false`), así que no hay cruce de frontera de privilegio. No se lee de HKLM sin necesidad. Aceptado. |
| T-05-10 | Tampering | rama A sin guarda de valor vacío | medium | mitigate | Un `ExecWait` con `UninstallString` vacío en una máquina sin Tauri instalado es un comportamiento indefinido dentro del instalador. La guarda `StrCmp` contra vacío es criterio de aceptación, no una sugerencia. |
</threat_model>

<artifacts_this_phase_produces>
| Artefacto | Tipo | Detalle |
|---|---|---|
| Rama de migración en `apps/desktop/build/installer.nsh` | hook NSIS (rama A, condicional) | `ReadRegStr` + `StrCmp` + `ExecWait` del uninstaller de Tauri, dentro de la macro `customInit` ya existente |
| `nsis.guid` en `apps/desktop/electron-builder.yml` | clave de config (rama B, condicional) | GUID heredado de Tauri → una sola entrada en Agregar/quitar programas |
| Resultado del gate D-02 | hecho empírico (en el SUMMARY) | GUID, colmena, `UninstallString`, `InstallLocation`, y si `%APPDATA%` sobrevive a `/S` |
</artifacts_this_phase_produces>

<verification>
1. El gate de la Tarea 1 se ejecutó contra una instalación Tauri 0.1.15 REAL y sus cuatro
   respuestas están en el SUMMARY.
2. Exactamente una rama cableada (check automático de la Tarea 2).
3. `Nyanko-Setup-0.2.0.exe` sobre una 0.1.15 con biblioteca poblada: la biblioteca sobrevive,
   la app abre, y Agregar/quitar programas muestra una sola entrada.
</verification>

<success_criteria>
- PKG-01 (migración): el usuario de 0.1.15 aterriza en 0.2.0 con su biblioteca intacta.
- DATA-01: ningún camino del instalador borra `%APPDATA%\app.nyanko.desktop`.
- D-02 resuelto con evidencia, no con un supuesto.
</success_criteria>

<output>
Create `.planning/phases/05-packaging-auto-update/05-03-SUMMARY.md` when done
</output>
