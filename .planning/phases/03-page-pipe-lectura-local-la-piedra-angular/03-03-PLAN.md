---
phase: 03-page-pipe-lectura-local-la-piedra-angular
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/backend/nyanko_api/main.py
  - apps/backend/tests/test_manga_pages.py
autonomous: true
requirements: [RD-01]

must_haves:
  truths:
    - "D-01: las paginas se sirven por una RUTA DINAMICA bajo el prefijo `/assets`, sin copiar nada a disco. `FileResponse` cuando la pagina es un fichero suelto (`SourcePageContent.path`), `StreamingResponse` cuando vive dentro de un CBZ/ZIP (`SourcePageContent.chunks`). Un solo camino para las dos formas."
    - "D-04 (LA trampa de la fase): el handler `@app.get(\"/assets/pages/{page_id:path}\")` esta declarado ANTES del `app.mount(\"/assets\", StaticFiles(...))` de `main.py:1442`. Starlette casa rutas EN ORDEN DE LA LISTA y un `Mount` devuelve `Match.FULL` para cualquier path bajo su prefijo: declarado despues, el mount se lo traga y la ruta no existe. Hay un test que FALLA si alguien reordena las declaraciones."
    - "H-1: el handler es una ruta PLANA sobre `app`, como las otras 58 de `main.py`. El modulo `manga.py` con su propio enrutador que preveia ARCHITECTURE.md NO EXISTE COMO OPCION: un test verde de la Fase 2 (`test_source_api.py:155-162`) lo prohibe. Ese test sigue verde despues de este plan."
    - "D-05 (seguridad, no negociable): el mount `/assets` es NO AUTENTICADO. El endpoint NUNCA acepta una ruta del sistema de ficheros: acepta un `page_id` OPACO que se resuelve A TRAVES DE LA FUENTE, que ya valida contencion (`local_archive.py::_resolve_id` -> `candidate.relative_to(root)`). Una ruta bajo ese prefijo que aceptara paths seria lectura arbitraria de disco desde cualquier pagina del renderer."
    - "D-03 / FND-05: la URL de pagina que se emite es RELATIVA (`/assets/pages/{page_id}`), nunca con host ni puerto. `normalizeAssetUrls` (`api.ts:202-215`) reescribe CUALQUIER string que empiece por `/assets/` con el `apiUrl` vivo — venga del mount o no. Este proyecto ya perdio TODAS las portadas de la biblioteca por persistir el puerto efimero dentro de una URL."
    - "H-4: el handler llama a la fuente A TRAVES DE `SourceEngine`, no de `registry.get(...)` a pelo. `SourceEngine._call_source` (`engine.py:103-116`) ya garantiza que NADA escapa sin tipar — ni un `httpx` exotico ni un `IndexError` parseando algo ajeno. Sin eso, el caller se come un 500 y el renderer no sabe si reintentar, esperar o actualizar la fuente."
    - "El registry se lee de `request.app.state.source_registry` (lo siembra `lifespan`, `main.py:1408`). NUNCA se llama a `build_source_registry()` dentro de un handler: hay un test de la Fase 2 que lo prohibe (`test_source_api.py:105-120`)."
  prohibitions:
    - "D-18: el handler NO comprueba el tipo ni el nombre de la fuente para decidir como servir la pagina. La eleccion la hace la FORMA del `SourcePageContent` (`path` o `chunks`). Una rama por fuente funcionaria hoy con la unica fuente que existe y seria una rama por fuente en la Fase 7 — que es lo que SRC-05 prohibe y lo que un test verde de la Fase 2 verifica."
    - "El handler NO acepta, NO construye y NO devuelve NUNCA una ruta del sistema de ficheros. El `page_id` es opaco; el `path` del `SourcePageContent` se pasa a `FileResponse` y jamas se serializa a una respuesta."
    - "D-02: no hay `_stream/`, no hay cache de lectura, no hay limpieza al arrancar. Sin cache no existe el bug «la cache de paginas se come el SSD», y no hay estado en disco que reconciliar tras un cierre brusco."
    - "El fichero `main.py` no gana un enrutador propio ni un `include_...` de nada (prohibido por `test_source_api.py:155-162`)."
    - "El handler no exige el header de instancia: un `<img src>` no lo envia. Es no autenticado A PROPOSITO, y por eso la contencion de rutas es lo unico que lo separa de una lectura arbitraria de disco."
---

<objective>
La ruta por la que pasan los bytes. Un `page_id` opaco entra, una imagen sale — y el mount
`StaticFiles` de `/assets`, que casa TODO su prefijo, no se la traga.

Purpose: aqui la arquitectura de entrega de paginas se vuelve verdadera o falsa. Es la razon del
nombre de la fase. Dos cosas pueden hundirla y las dos tienen test propio en este plan: el ORDEN de
declaracion (D-04) y la CONTENCION de rutas en un endpoint no autenticado (D-05).
Output: `GET /assets/pages/{page_id:path}` sirviendo carpetas y CBZ por el mismo camino, con su test
de orden de rutas y su test de traversal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.planning/CODEX-RULES.md
</execution_context>

<context>
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-CONTEXT.md
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-PATTERNS.md
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces (cuota de este plan)

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `GET /assets/pages/{page_id:path}` | `main.py` (entre la 1441 y la 1442) | La ruta dinamica de paginas. Declarada ANTES del mount o no existe (D-04) |
| `read_page` | `main.py` | El handler. Resuelve por `SourceEngine.page_bytes` y devuelve `FileResponse` o `StreamingResponse` segun la FORMA del contenido |
| `_page_url` | `main.py` | Construye la URL RELATIVA de una pagina: `/assets/pages/{quote(page_id)}`. Gemelo de `_asset_url` (`main.py:316-326`), con su comentario |
| `raise_source_http_error` | `main.py` | Mapeo tipo de `SourceError` -> HTTP. Gemelo de `raise_provider_http_error` (`main.py:1001-1038`) |
| `_source_engine` | `main.py` | Helper que construye `SourceEngine` sobre `request.app.state.source_registry`. NUNCA reconstruye el registry |
| `apps/backend/tests/test_manga_pages.py` | nuevo | Orden de rutas (D-04), traversal (D-05), streaming de CBZ, fichero suelto, URL relativa |

Consume del plan 01: `SourcePageContent`, `Source.page_bytes`, `SourceEngine.page_bytes`,
`ARCHIVE_MEMBER_SEPARATOR`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: La ruta de paginas, declarada antes del mount</name>

  <read_first>
    - `apps/backend/nyanko_api/main.py:1440-1450` — EL SITIO. `app` nace en la 1440, `settings` en la 1441, `app.mount("/assets", StaticFiles(...))` en la 1442. El handler va ENTRE la 1441 y la 1442. Leer tambien `main.py:1-50` (imports: `Response`, `JSONResponse`, `HTMLResponse` y `StaticFiles` ya estan; `FileResponse`/`StreamingResponse` NO — hay que importarlos de `fastapi.responses`. `mimetypes` ya esta importado).
    - `apps/backend/nyanko_api/main.py:316-326` — `_asset_url`: la URL RELATIVA a proposito, con el comentario que explica el bug real (D-I-02). `_page_url` es su gemelo y copia ese comentario adaptado.
    - `apps/backend/nyanko_api/main.py:1001-1038` — `raise_provider_http_error`: la forma exacta del mapeo error -> `HTTPException`. El gemelo para fuentes se escribe igual.
    - `apps/backend/nyanko_api/main.py:1497-1515` — `list_sources` y `_source_info`: como se lee el registry vivo de `request.app.state.source_registry`. NUNCA se reconstruye dentro de un handler.
    - `apps/backend/nyanko_api/sources/engine.py:96-116` — `SourceEngine.pages`, `page_bytes` (plan 01) y `_call_source`, que es lo que garantiza que ningun fallo de fuente escapa sin tipar.
    - `apps/backend/nyanko_api/sources/errors.py` — la taxonomia completa a mapear.
    - `.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-PATTERNS.md` — H-1 (por que el handler tiene que ir literalmente ahi) y la seccion «`main.py` — la ruta de paginas».
  </read_first>

  <action>
    Anadir a `main.py`, EN ESTE ORDEN Y EN ESTE SITIO:

    1. `_page_url(page_id: str) -> str` — devuelve `"/assets/pages/" + quote(page_id, safe="")`
    (`urllib.parse.quote`; `urlsplit` ya se importa de ese modulo). La URL es RELATIVA A PROPOSITO:
    copiar y adaptar el comentario de `_asset_url` (`main.py:316-326`), que explica el bug real —
    una URL con `host:puerto` dentro caduca el dia que el sidecar arranca en otro puerto, y la
    biblioteca se queda sin una sola imagen, de forma permanente y silenciosa. El renderer la compone
    contra la base de API que resuelve en vivo (`normalizeAssetUrls`, `api.ts:202`).

    2. `raise_source_http_error(error: Exception, source_name: str) -> NoReturn` — gemelo de
    `raise_provider_http_error`. Mapeo por TIPO (nunca por `str(exc)`, D-12 de la Fase 2):
    `SourceNotFoundError` -> 404; `SourceUnsupportedError` -> 415; `SourceRateLimitError` -> 429
    (propagando `Retry-After` si el error lo trae); `SourceNetworkError` -> 503;
    `SourceParseError` -> 502; cualquier otro -> `logger.exception(...)` + 502. El `detail` NO debe
    filtrar rutas del disco: usa el mensaje tipado de la fuente, que ya esta en espanol y es de
    dominio.

    3. `_source_engine(request: Request) -> SourceEngine` — construye `SourceEngine` sobre
    `request.app.state.source_registry` (el registry vivo que siembra `lifespan`). NO llamar a
    `build_source_registry()` aqui: hay un test verde de la Fase 2 que lo prohibe.

    4. EL HANDLER, declarado entre `settings = get_settings()` (linea 1441) y la llamada a
    `app.mount("/assets", ...)` (linea 1442), con un comentario en espanol encima explicando la
    trampa: Starlette casa rutas en orden de la lista y el mount de `/assets` devuelve coincidencia
    COMPLETA para todo su prefijo; declarado despues del mount, este handler no se ejecutaria nunca.
    Este es el sitio, y hay un test que falla si alguien lo mueve.

    Firma: `@app.get("/assets/pages/{page_id:path}")` +
    `async def read_page(page_id: str, request: Request, source: str = "local_archive") -> Response`.
    (El `page_id` viene con `:path` porque el id opaco lleva `/` y `:` dentro. El nombre de la fuente
    va como query param con defecto, para que el dia que exista una fuente online la URL de sus
    paginas no cambie de forma.)

    Cuerpo: `content = await _source_engine(request).page_bytes(source, page_id)` dentro de un
    `try` que atrapa `SourceError` y delega en `raise_source_http_error`. Con el `SourcePageContent`
    devuelto:
      - si `content.path` no es `None` -> `FileResponse(content.path, media_type=content.media_type)`
      - si no -> `StreamingResponse(content.chunks, media_type=content.media_type)`
    La eleccion la hace la FORMA DEL CONTENIDO. NO comprobar el tipo ni el nombre de la fuente para
    decidirlo: eso funcionaria hoy con la unica fuente que existe y seria una rama por fuente el dia
    que haya diez (D-18/SRC-05).
    Anadir `Cache-Control: private, max-age=3600` al response (la pagina de un capitulo no cambia; el
    id es unico) — es gratis y evita que el renderer vuelva a pedir la misma pagina al volver atras.
  </action>

  <acceptance_criteria>
    - Asercion de fuente (D-04): en `apps/backend/nyanko_api/main.py`, la linea que declara `@app.get("/assets/pages/` aparece ANTES (numero de linea menor) que la linea que declara `app.mount("/assets"`.
    - Asercion de comportamiento (D-04, la que tiene dientes): un test lee `app.router.routes` y afirma que el indice de la ruta cuyo `path` es `/assets/pages/{page_id:path}` es MENOR que el indice del `Mount` cuyo `path` es `/assets`. Si alguien reordena las declaraciones, ESTE TEST FALLA.
    - Asercion de comportamiento: `GET /assets/pages/<id de una pagina suelta>` devuelve 200, `content-type: image/jpeg` y EXACTAMENTE los bytes del fichero.
    - Asercion de comportamiento: `GET /assets/pages/<id de un miembro de CBZ>` devuelve 200 y EXACTAMENTE los bytes que se escribieron dentro del ZIP. El ZIP no queda abierto tras agotarse el response.
    - Asercion de comportamiento: `GET /assets/otra-cosa.png` (un fichero que SI existe en `assets_dir`) sigue sirviendose por el mount `StaticFiles` — la ruta nueva no rompe lo que ya funcionaba (portadas de `media_details_cache`).
    - Asercion de comportamiento: un capitulo `.cbr` devuelve 415 y el `detail` nombra CBZ. Una pagina inexistente devuelve 404. Ninguno devuelve 500.
    - Asercion negativa: `rg "isinstance\(.*LocalArchiveSource|if source.name ==" apps/backend/nyanko_api/main.py` no encuentra nada (D-18).
    - Asercion negativa: el test verde de la Fase 2 `test_sources_endpoint_is_flat_and_handler_does_not_build_registry` (`test_source_api.py:155-162`) SIGUE PASANDO — `main.py` no gana ningun enrutador (H-1).
    - Asercion negativa: ninguna respuesta JSON ni ningun `detail` de error contiene la ruta absoluta del fichero servido.
    - Comando: `cd apps/backend; python -m pytest tests/test_manga_pages.py tests/test_source_api.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: El test de traversal — el endpoint no autenticado no lee disco arbitrario</name>

  <read_first>
    - `apps/backend/tests/test_source_api.py:61-75` — `_installed_registry`: el context manager que sustituye `app.state.source_registry` sin tocar `lifespan`. El page pipe lo copia LITERAL.
    - `apps/backend/tests/test_source_api.py:78-102` — como se monta un `TestClient(app)` con un `SourceRegistry([LocalArchiveSource(_Fetcher(), [{"id": "0", "path": str(tmp_path)}])])`.
    - `apps/backend/tests/test_sources.py:259-271` — `test_local_archive_rejects_traversal_ids`: la version a nivel de FUENTE. Este plan escribe la version a nivel HTTP, que es la que importa: el mount `/assets` es no autenticado.
    - `apps/backend/nyanko_api/sources/local_archive.py:123-135` — `_resolve_id` y su `candidate.relative_to(root)`. La contencion vive AHI y en ningun otro sitio; el test comprueba que la ruta HTTP se apoya en ella y no la esquiva.
  </read_first>

  <action>
    Crear `apps/backend/tests/test_manga_pages.py`. Usa `tmp_path` de pytest (CODEX-RULES regla 3: no
    inventes directorios de trabajo) y `TestClient(app)` + `_installed_registry(...)` copiado de
    `test_source_api.py:61-75`.

    Fixture: un root de biblioteca en `tmp_path` con (a) una carpeta de capitulo con `1.jpg`, `2.jpg`,
    `10.jpg` de bytes conocidos y distintos entre si, (b) un `Cap 2.cbz` real escrito con
    `zipfile.ZipFile` con miembros de bytes conocidos, y (c) un fichero fuera del root
    (`tmp_path.parent / "secreto.txt"`) que NO debe poder leerse jamas.

    Tests obligatorios:
      - Orden de rutas (D-04): sobre `app.router.routes`, indice de `/assets/pages/{page_id:path}` <
        indice del `Mount` de `/assets`.
      - Traversal (D-05), todos con `assert response.status_code == 404` y `assert response.content`
        no contiene los bytes del fichero secreto — NUNCA 200:
          `GET /assets/pages/0:../secreto.txt`
          `GET /assets/pages/0:../../etc/passwd`
          `GET /assets/pages/0:%2E%2E%2F%2E%2E%2Fetc%2Fpasswd` (el mismo, percent-encoded)
          `GET /assets/pages/C:/Windows/win.ini` (ruta absoluta de Windows como id)
          `GET /assets/pages//etc/passwd` (ruta absoluta POSIX como id)
          `GET /assets/pages/raiz-no-registrada:x.jpg`
          `GET /assets/pages/0:Cap 2.cbz!../../../secreto.txt` (traversal por el MIEMBRO del ZIP)
          `GET /assets/pages/0:Cap 2.cbz!miembro-que-no-existe.jpg`
      - Contenido: la pagina suelta y el miembro del CBZ devuelven los bytes EXACTOS que se
        escribieron. Un `1.jpg` de la carpeta y el `1.jpg` del CBZ tienen bytes distintos y cada uno
        devuelve el suyo (o sea: el id realmente identifica, no se cruzan).
      - URL relativa (FND-05): `_page_url(id)` empieza por `/assets/pages/` y NO contiene `http`,
        `127.0.0.1` ni ningun numero de puerto.
      - El mount sigue vivo: un fichero escrito en `settings.assets_dir` se sirve por
        `GET /assets/<nombre>` con 200.

    NO ejecutar la suite (CODEX-RULES regla 2): el orquestador la corre fuera del sandbox.
  </action>

  <acceptance_criteria>
    - Asercion de seguridad: los OCHO casos de traversal listados devuelven 404 (o 4xx tipado), NUNCA 200, y el cuerpo de la respuesta no contiene ni un byte del fichero de fuera del root.
    - Asercion de seguridad: el traversal POR EL MIEMBRO del ZIP (`...cbz!../../../secreto.txt`) tambien es 404 — el miembro se valida por pertenencia exacta a `namelist()`, no se resuelve como ruta.
    - Asercion de comportamiento (D-04): el test de orden de rutas FALLA si se mueve la declaracion del handler despues del mount. Verificable: mover la declaracion a mano y comprobar que el test se pone rojo (el orquestador puede hacerlo; es la unica prueba de que el test tiene dientes).
    - Asercion de comportamiento: la pagina de la carpeta y la del CBZ devuelven bytes distintos y correctos — el `page_id` identifica de verdad.
    - Asercion de fuente: `rg "http://|127.0.0.1" apps/backend/tests/test_manga_pages.py` solo aparece dentro de aserciones NEGATIVAS (comprobando que la URL emitida NO los lleva), nunca construyendo una URL esperada.
    - Comando: `cd apps/backend; python -m pytest tests/test_manga_pages.py -q -x`.
    - Comando (ORQUESTADOR): `cd apps/backend; python -m pytest tests/ -q --ignore=tests/test_packaged_sources.py` — suite entera verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS nivel 1. Bloquear cualquier amenaza `high`. Este plan concentra la superficie de seguridad de la fase.

## Trust Boundaries

| Boundary | Description |
|---|---|
| renderer (o CUALQUIER pagina cargada en el) -> `GET /assets/pages/{page_id}` | **Frontera critica.** El prefijo `/assets` NO esta autenticado (un `<img src>` no envia el header de instancia). Todo lo que entre por aqui es no confiable. |
| `page_id` -> `LocalArchiveSource._resolve_id` | El unico sitio donde se decide si una ruta esta contenida en la biblioteca. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-10 | Information Disclosure | `GET /assets/pages/{page_id:path}` | critical | mitigate | **Lectura arbitraria de disco.** Un endpoint no autenticado bajo `/assets` que aceptara una ruta seria exactamente eso. Mitigacion: el `page_id` es OPACO y se resuelve a traves de la fuente; la contencion la impone `candidate.relative_to(root)` (`local_archive.py:132`) y el miembro del ZIP por pertenencia exacta a `namelist()`. Ocho casos de traversal en `test_manga_pages.py`, incluyendo el traversal por el miembro del archivo. |
| T-03-11 | Spoofing | Orden de rutas (D-04) | high | mitigate | Si el handler se declara DESPUES del mount, el mount lo traga y la ruta desaparece en silencio — la app parece funcionar hasta que ninguna pagina carga. Mitigacion: test sobre `app.router.routes` que compara indices y falla al reordenar. |
| T-03-12 | Information Disclosure | `detail` de las `HTTPException` | medium | mitigate | Un mensaje de error que filtre la ruta absoluta del fichero revela la estructura del disco del usuario. Mitigacion: `raise_source_http_error` usa el mensaje TIPADO de dominio de la fuente, nunca `str(path)`. Asercion negativa en el test. |
| T-03-13 | Denial of Service | `StreamingResponse` sobre ZIP | medium | mitigate | Un ZIP con una entrada gigante (zip bomb) descomprimida entera en memoria. Mitigacion: el generador de `chunks` (plan 01) cede bloques de 64 KB; nunca hay un capitulo entero en RAM. Techo aceptado: una sola pagina descomprimida sin limite duro — un CBZ con una «pagina» de 4 GB agotaria memoria. Se registra; el limite duro se anade si aparece un caso real. |
| T-03-14 | Elevation of Privilege | `source` query param | medium | mitigate | El cliente elige la fuente por nombre. Mitigacion: `SourceEngine` -> `registry.get(name)` lanza `KeyError` para una fuente desconocida (-> 404), y una fuente rechazada por version NO esta en el registry: no se la puede invocar. |
</threat_model>

<verification>
1. `cd apps/backend; python -m pytest tests/test_manga_pages.py -q -x`
2. `cd apps/backend; python -m pytest tests/ -q --ignore=tests/test_packaged_sources.py` — suite entera.
3. Prueba de dientes del test de D-04 (la hace el ORQUESTADOR): mover a mano la declaracion del handler DESPUES del `app.mount("/assets", ...)`, comprobar que `test_manga_pages.py` se pone ROJO, y revertir. Un test de orden que no falla al reordenar no vale nada.
4. `rg -n "@app.get\(\"/assets/pages|app.mount\(\"/assets\"" apps/backend/nyanko_api/main.py` — el primero tiene que salir con numero de linea MENOR que el segundo.
</verification>

<success_criteria>
- RD-01 (mitad HTTP): un CBZ, un ZIP y una carpeta del disco se sirven como paginas por el mismo endpoint.
- D-01/D-04/D-05 cubiertos, cada uno con un test que falla si se rompen.
- El mount `StaticFiles` de `/assets` sigue sirviendo lo que ya servia (portadas): la ruta nueva no lo desplaza.
- Ninguna URL de pagina lleva host ni puerto dentro (FND-05).
</success_criteria>

<output>
Create `.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-03-SUMMARY.md` when done.
Los resultados de tests van como `unknown` / «pendiente de verificacion por el orquestador» (CODEX-RULES regla 5).
</output>
