---
phase: 03-page-pipe-lectura-local-la-piedra-angular
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/nyanko_api/sources/contract.py
  - apps/backend/nyanko_api/sources/local_archive.py
  - apps/backend/nyanko_api/sources/engine.py
  - apps/backend/nyanko_api/sources/__init__.py
  - apps/backend/tests/test_sources.py
  - apps/backend/tests/test_source_api.py
  - apps/backend/tests/test_source_budget.py
  - apps/backend/tests/test_source_engine.py
autonomous: true
requirements: [RD-01, RD-08]

must_haves:
  truths:
    - "D-16: el `Source` Protocol gana un CUARTO metodo `page_bytes(page)` y `SOURCE_API_VERSION` pasa de 1 a 2. Sin esto la ruta de D-01 no puede servir un solo byte: hoy `SourcePage` no lleva ni URL ni contenido, y nadie sabe convertir un `page_id` en bytes."
    - "D-17: el bump de version se paga HOY (un `int`) y no en la Fase 7 (un cambio de contrato con adapters escritos encima). `registry.py:36-42` sigue rechazando por version exacta: con el 2 tambien."
    - "ROADMAP criterio 1 / RD-01: un CBZ, un ZIP y una carpeta de imagenes se leen los tres por el MISMO camino (`chapters()` + `pages()` + `page_bytes()`), en orden natural (`2.jpg` antes que `10.jpg`), reutilizando `_natural_key` (`local_archive.py:24-28`) — no se escribe una segunda funcion de orden."
    - "ROADMAP criterio 1 / RD-08: si el capitulo trae `ComicInfo.xml`, sus metadatos MANDAN sobre el nombre del fichero. `SourceChapter.number` es `float | None` (el 12.5 CABE — FND-04); el numero adivinado del nombre solo se usa cuando no hay `ComicInfo.xml`."
    - "D-01: `page_bytes()` devuelve UNA sola forma de dato (`SourcePageContent`) que expresa las dos formas fisicas: `path` (fichero suelto en disco -> el llamador puede usar `FileResponse`) o `chunks` (iterador de bytes -> `StreamingResponse`). La eleccion la hace la FORMA DEL CONTENIDO, nunca el nombre ni el tipo de la fuente."
    - "El gotcha del ZIP queda desactivado por construccion: `chunks` es un generador que abre `zipfile.ZipFile` y el miembro DENTRO de un `with` y los cierra al agotarse. No existe ningun `ZipFile` abierto cuya vida dependa de que alguien se acuerde de cerrarlo despues de que el response se agote."
    - "Un `.cbr`/`.rar` se DETECTA y se rechaza con `SourceUnsupportedError` («conviertelo a CBZ»). No se intenta leer (decision de licencia, no tecnica)."
    - "WR-01 (deuda Fase 2, 2 lineas): `SourceEngine` se exporta en `sources/__init__.py`. La ruta de paginas del plan 03 lo consume por `_call_source` — que es lo que garantiza que ningun fallo de fuente escapa sin tipar (`engine.py:103-116`)."
    - "H-5: el bump de contrato pone rojos TRES tests verdes de la Fase 2 POR CONSTRUCCION. Este plan los reescribe deliberadamente: (a) `test_source_api_version_is_exact_integer` afirma `== 1` y el literal `SOURCE_API_VERSION = 1`; (b) `test_source_protocol_is_runtime_checkable_and_minimal` afirma `methods == {\"search\", \"chapters\", \"pages\"}`; (c) los CUATRO dobles de test pasan por `registry.register()` -> `isinstance(source, Source)`, y un Protocol runtime-checkable comprueba EXISTENCIA de atributos: sin `page_bytes` los cuatro se registran como `rejected` y sus tests mueren con `KeyError`."
  prohibitions:
    - "D-18: la eleccion de como servir una pagina NO se hace con una comprobacion de tipo ni de nombre de fuente. Funcionaria hoy con la unica fuente que existe y seria una rama por fuente en la Fase 7 — que es exactamente lo que SRC-05 prohibe y lo que el test verde de la Fase 2 verifica."
    - "D-15 (Fase 2): nada de `pkgutil`, `importlib` ni `__subclasses__()`. `SOURCES` se sigue escribiendo a mano."
    - "D-06 (Fase 2): un modulo de fuente no importa `httpx`, `requests`, `urllib` ni `aiohttp`. El import-guard AST sigue en verde; `zipfile`, `xml.etree.ElementTree` y `mimetypes` son stdlib y no son red."
    - "El id de una pagina NUNCA es una ruta del sistema de ficheros: sigue siendo `{root_key}:{ruta relativa}` (`_make_id`), y para un miembro de archivo `{root_key}:{ruta relativa}!{miembro}`. Ni absoluta, ni con el root dentro."
    - "No se escribe una segunda funcion de orden natural. `_natural_key` ya existe y ordena TAMBIEN los miembros del ZIP."
    - "Deferred: no se construye caché de lectura, ni LRU, ni extraccion a disco. Servir desde el ZIP en cada pagina es lo barato hasta que una MEDICION diga lo contrario (D-02)."
---

<objective>
Dar al contrato de fuente la unica pieza que le falta para que exista un page pipe: la capacidad de
convertir un `page_id` en BYTES. Y ensenar a `LocalArchiveSource` — el primer adapter del contrato de
la Fase 2 — a leer CBZ/ZIP y `ComicInfo.xml`, no solo carpetas.

Purpose: sin `page_bytes()` la ruta del plan 03 no puede servir nada, y sin el bump de version el
mismo agujero se hereda a los adapters online de la Fase 7 — donde arreglarlo ya no cuesta un `int`.
Output: contrato v2, fuente local que lee las tres formas (carpeta / ZIP / CBZ) por un solo camino, y
la suite de la Fase 2 verde con sus tres tests reescritos a proposito.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.planning/CODEX-RULES.md
</execution_context>

<context>
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-CONTEXT.md
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces (cuota de este plan)

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `SOURCE_API_VERSION = 2` | `sources/contract.py` | Version de API del contrato. Sube de 1 a 2 (D-16/D-17) |
| `Source.page_bytes` | `sources/contract.py` | Cuarto metodo del Protocol: `async def page_bytes(self, page: SourcePage \| str) -> SourcePageContent` |
| `SourcePageContent` | `sources/contract.py` | `@dataclass(frozen=True, slots=True)`: `media_type: str`, `path: Path \| None`, `chunks: Iterator[bytes] \| None`. Una sola forma de dato para las dos formas fisicas |
| `SourceChapter.number` | `sources/contract.py` | `float \| None`. El 12.5 CABE (FND-04). `None` si no se puede determinar |
| `SourceChapter.is_chapter` | `sources/contract.py` | `bool = True`. `False` para un nodo que solo contiene subcarpetas (una serie), `True` para una hoja legible. Es DATO, no una rama por fuente |
| `ARCHIVE_EXTENSIONS` | `sources/local_archive.py` | `frozenset` de `.cbz`, `.zip` |
| `UNSUPPORTED_ARCHIVE_EXTENSIONS` | `sources/local_archive.py` | `frozenset` de `.cbr`, `.rar`. Se detectan para decir «conviertelo a CBZ»; no se leen |
| `ARCHIVE_MEMBER_SEPARATOR` | `sources/local_archive.py` | `"!"`. Separa la ruta del archivo del miembro dentro de el en un `source_id` |
| `LocalArchiveSource.page_bytes` | `sources/local_archive.py` | Implementacion: `path` para fichero suelto, generador `chunks` para miembro de ZIP |
| `LocalArchiveSource._comic_info` | `sources/local_archive.py` | Lee `ComicInfo.xml` (carpeta o ZIP) y devuelve sus metadatos; RD-08 |
| `LocalArchiveSource._chapter_number` | `sources/local_archive.py` | Numero adivinado del nombre de fichero. Solo se usa si `ComicInfo.xml` no manda |
| `SourceEngine.page_bytes` | `sources/engine.py` | `async def page_bytes(self, source_name: str, page: SourcePage \| str) -> SourcePageContent`, por `_call_source` |
| `SourceEngine` (exportado) | `sources/__init__.py` | WR-01: deja de mentir la superficie publica del paquete |

Simbolos que este plan NO crea y que otros planes consumiran: la ruta `/assets/pages/{page_id:path}`
(plan 03), los endpoints `/api/manga/*` (plan 04), el esquema v9 (plan 02).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contrato v2 — el cuarto metodo y el tipo de contenido de pagina</name>

  <read_first>
    - `apps/backend/nyanko_api/sources/contract.py` — fichero modificado. Hoy: `SOURCE_API_VERSION = 1`, 3 metodos, `SourcePage` sin URL ni bytes (lineas 7, 32-38, 45-56).
    - `apps/backend/nyanko_api/sources/registry.py:30-58` — `register()` rechaza por version exacta y hace `isinstance(source, Source)`. Un Protocol `@runtime_checkable` solo comprueba que el ATRIBUTO EXISTA: anadir un metodo al Protocol rechaza a toda fuente que no lo tenga.
    - `apps/backend/nyanko_api/sources/engine.py:96-116` — `SourceEngine.pages` y `_call_source`. El gemelo `page_bytes` copia esta forma exacta.
    - `apps/backend/nyanko_api/sources/__init__.py` — imports explicitos y `__all__`. `SourceEngine` NO esta (WR-01).
    - `.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-CONTEXT.md` — D-16, D-17, D-18.
  </read_first>

  <action>
    En `contract.py`: subir `SOURCE_API_VERSION` a `2`.

    Anadir `SourcePageContent` como `@dataclass(frozen=True, slots=True)` con tres campos:
    `media_type: str`, `path: Path | None = None`, `chunks: Iterator[bytes] | None = None`.
    Invariante documentado en el docstring: exactamente uno de `path`/`chunks` es no-`None`. `path`
    existe para que el llamador pueda servir un fichero de disco sin copiarlo a memoria; `chunks`
    existe para lo que no es un fichero de disco (un miembro de un ZIP hoy, la respuesta de un CDN en
    la Fase 7). Importar `Path` de `pathlib` e `Iterator` de `collections.abc`. NO importar nada de
    `nyanko_api.models` (hay un test que lo prohibe).

    Anadir al `Source` Protocol el cuarto metodo:
    `async def page_bytes(self, page: SourcePage | str) -> SourcePageContent: ...`

    Anadir a `SourceChapter` dos campos con valor por defecto (van DESPUES de los existentes o el
    dataclass rompe): `number: float | None = None` y `is_chapter: bool = True`. `number` es `float`
    y no `int` por FND-04: el capitulo 12.5 existe y `playback_events.episode` (INTEGER) es
    exactamente donde se pierde. `is_chapter` es dato: `False` para un nodo que solo contiene
    subcarpetas (o sea, una serie), `True` para una hoja legible — asi el cliente puede navegar el
    arbol con UN endpoint y sin preguntarle a la fuente de que tipo es.

    En `engine.py`: anadir `SourceEngine.page_bytes(source_name, page)`, gemelo de `SourceEngine.pages`
    — resuelve la fuente por el registry y llama a `self._call_source(lambda: source.page_bytes(page))`.
    Nada de cache (D-02: no hay caché de lectura).

    En `sources/__init__.py`: importar `SourceEngine` (y `DefaultSourceFetcher`, `build_source_fetcher`
    si estan a mano) desde `.engine`, y anadirlos a `__all__`. Eso es WR-01.
  </action>

  <acceptance_criteria>
    - Asercion de fuente: `apps/backend/nyanko_api/sources/contract.py` contiene `SOURCE_API_VERSION = 2` y no contiene `SOURCE_API_VERSION = 1`.
    - Asercion de fuente: el `Source` Protocol declara exactamente los metodos `search`, `chapters`, `pages` y `page_bytes` — ni uno mas. Sigue marcado `@runtime_checkable`.
    - Asercion de fuente: `SourcePageContent` es `@dataclass(frozen=True, slots=True)` con los campos `media_type`, `path` y `chunks`.
    - Asercion de fuente: `rg "nyanko_api.models" apps/backend/nyanko_api/sources/contract.py` no encuentra nada (los tipos de fuente siguen separados de los del tracker).
    - Asercion de comportamiento: `from nyanko_api.sources import SourceEngine` importa sin error (hoy lanza `ImportError` — WR-01).
    - Asercion de comportamiento: una fuente fake con `api_version = 1` queda `rejected` en el registry con motivo que nombra la version; con `api_version = 2` queda `ok`.
    - Comando: `cd apps/backend; python -m pytest tests/test_sources.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LocalArchiveSource lee CBZ/ZIP, ComicInfo.xml y sirve bytes</name>

  <read_first>
    - `apps/backend/nyanko_api/sources/local_archive.py` — fichero modificado. LEER ENTERO (148 lineas). `_natural_key` (24-28), `chapters()` (54-76), `pages()` (78-105), `_resolve_id` (123-135) con `candidate.relative_to(root)` — la contencion de rutas vive ahi y en ningun otro sitio —, `_split_id` (137-144), `_make_id` (146-147).
    - `apps/backend/nyanko_api/sources/errors.py` — la taxonomia ya existe: `SourceNotFoundError` (no existe), `SourceParseError` (existe pero no se puede interpretar / 0 resultados), `SourceUnsupportedError` (el CBR/RAR).
    - `apps/backend/nyanko_api/sources/contract.py` — tras el Task 1: `SourcePageContent`, `SourceChapter.number`, `SourceChapter.is_chapter`.
    - `apps/backend/tests/test_sources.py:232-271` — `test_local_archive_lists_only_images_in_natural_order` (el CBZ copia esta forma con un ZIP escrito en `tmp_path`) y `test_local_archive_rejects_traversal_ids`.
    - `.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-PATTERNS.md` — seccion «`sources/local_archive.py`».
  </read_first>

  <action>
    Extender `LocalArchiveSource` SIN reescribirla: se anaden ramas por FORMA DEL FICHERO (carpeta vs
    archivo comprimido), nunca por fuente.

    Constantes nuevas: `ARCHIVE_EXTENSIONS = frozenset((".cbz", ".zip"))`,
    `UNSUPPORTED_ARCHIVE_EXTENSIONS = frozenset((".cbr", ".rar"))`, `ARCHIVE_MEMBER_SEPARATOR = "!"`.

    `chapters(series)`: hoy lista solo subdirectorios. Pasa a listar subdirectorios Y ficheros cuyo
    sufijo este en `ARCHIVE_EXTENSIONS` o en `UNSUPPORTED_ARCHIVE_EXTENSIONS`, todo junto y ordenado
    por `_natural_key(path.name)` (la misma funcion; no se escribe otra). Para cada entrada:
      - `is_chapter`: `False` si es un directorio que NO contiene ninguna imagen de `IMAGE_EXTENSIONS`
        (es decir, un nodo intermedio: una serie). `True` en el resto (directorio con imagenes, o
        archivo comprimido).
      - `number`: el de `ComicInfo.xml` si existe y trae `Number` (RD-08 — MANDA sobre el nombre);
        si no, `_chapter_number(nombre)`.
      - `title`: el `Title` de `ComicInfo.xml` si existe; si no, `path.name` (como hoy).
    Se mantiene `SourceParseError` si no hay ninguna entrada (SRC-07: 0 resultados LANZA).

    `_chapter_number(name: str) -> float | None`: primer numero (con decimal opcional) del nombre,
    como `float`. `None` si no hay ninguno. Usa una `re` propia con decimal; `_natural_key` no sirve
    aqui porque parte por enteros.

    `_comic_info(chapter_path: Path) -> dict[str, str]`: si el capitulo es una carpeta, busca
    `ComicInfo.xml` dentro; si es un ZIP, busca el miembro `ComicInfo.xml` (comparacion insensible a
    mayusculas sobre `namelist()`). Devuelve `{}` si no hay. Parsea con `xml.etree.ElementTree.fromstring`
    y devuelve el texto de los elementos `Number`, `Title`, `Series` y `PageCount`.
    GUARDIA OBLIGATORIA antes de parsear: si los bytes crudos contienen una declaracion de entidad o
    de DTD, se descarta el fichero y se devuelve `{}` — un CBZ puede venir de un torrent, y el parser
    de la stdlib expande entidades internas (billion laughs). Y descarta tambien si el miembro declara
    un `file_size` mayor de 1 MB: un `ComicInfo.xml` legitimo son unos pocos KB.
    Cualquier XML mal formado se traga y devuelve `{}` (un `ComicInfo.xml` roto degrada al nombre del
    fichero; NO tumba el listado del capitulo).

    `pages(chapter)`: mantiene el camino de carpeta tal cual. Anade el camino de archivo: si
    `chapter_path` es fichero con sufijo en `UNSUPPORTED_ARCHIVE_EXTENSIONS`, lanza
    `SourceUnsupportedError("Formato no soportado: conviertelo a CBZ")`. Si el sufijo esta en
    `ARCHIVE_EXTENSIONS`, abre el ZIP, filtra `namelist()` por `IMAGE_EXTENSIONS` (descartando
    entradas de directorio), ordena por `_natural_key(miembro)` y construye cada `SourcePage` con
    `source_id = f"{self._make_id(root_key, root, chapter_path)}{ARCHIVE_MEMBER_SEPARATOR}{miembro}"`
    y `filename = ` el nombre base del miembro. Un ZIP corrupto (`zipfile.BadZipFile`) es
    `SourceParseError`. Sin imagenes dentro: `SourceParseError` (SRC-07).

    `page_bytes(page)`: acepta `SourcePage` o `str`. Parte el id por `ARCHIVE_MEMBER_SEPARATOR`
    (`split(sep, 1)`) ANTES de resolver: la parte izquierda va a `_resolve_id` (que ya valida
    contencion), la derecha es el miembro.
      - Sin miembro: el `candidate` tiene que ser un fichero existente con sufijo en
        `IMAGE_EXTENSIONS`, o `SourceNotFoundError`. Devuelve
        `SourcePageContent(media_type=..., path=candidate)`.
      - Con miembro: el `candidate` tiene que ser un ZIP legible, y el miembro tiene que estar en su
        `namelist()` — comparacion EXACTA contra la lista, no `startswith` ni normalizacion: un
        miembro que no esta en el archivo es `SourceNotFoundError` y no llega nunca a `open()`.
        Devuelve `SourcePageContent(media_type=..., chunks=<generador>)`.
    El generador de `chunks` abre `zipfile.ZipFile(candidate)` y el miembro DENTRO de un `with`
    anidado y cede bloques de 64 KB hasta agotarlo. Al agotarse (o al cerrarse el generador) el `with`
    cierra los dos. Comentario en espanol explicando por que: un `ZipFile` que se cierra antes de que
    el response se agote emite bytes de un fichero cerrado, y uno que no se cierra nunca filtra el
    descriptor. El generador resuelve las dos cosas y no deja a nadie que acordarse de nada.
    `media_type` sale de `mimetypes.guess_type(nombre)` con `"application/octet-stream"` de reserva.
  </action>

  <acceptance_criteria>
    - Asercion de comportamiento: un CBZ con miembros `2.jpg`, `10.jpg`, `1.jpg` devuelve `pages()` con `filename` en orden `1.jpg`, `2.jpg`, `10.jpg` e `index` `[1, 2, 3]` — el mismo assert que ya existe para carpetas.
    - Asercion de comportamiento: un ZIP y una carpeta con las mismas imagenes producen la MISMA lista de `filename`/`index`; el `source_id` del miembro de ZIP lleva el separador `!` y el de la carpeta no.
    - Asercion de comportamiento (RD-08): un CBZ con `ComicInfo.xml` que declara `<Number>12.5</Number>` y `<Title>El eclipse</Title>` produce un `SourceChapter` con `number == 12.5` (float) y `title == "El eclipse"`, AUNQUE el fichero se llame `Cap 003.cbz`. Sin `ComicInfo.xml`, el mismo fichero da `number == 3.0`.
    - Asercion de comportamiento: un `ComicInfo.xml` mal formado NO tumba `chapters()`; el capitulo aparece con el numero adivinado del nombre.
    - Asercion de seguridad: un `ComicInfo.xml` que declara entidades (`<!DOCTYPE ... <!ENTITY ...>`) se descarta sin parsear y `chapters()` devuelve el capitulo con el numero del nombre. El test escribe una bomba de entidades pequena y comprueba que la llamada TERMINA (no se cuelga ni agota memoria).
    - Asercion de comportamiento: `pages()` sobre un `.cbr` lanza `SourceUnsupportedError` y su mensaje nombra CBZ. El fichero NO se abre.
    - Asercion de comportamiento: `page_bytes()` sobre una pagina suelta devuelve `SourcePageContent` con `path` no-`None`, `chunks is None` y `media_type == "image/jpeg"`.
    - Asercion de comportamiento: `page_bytes()` sobre un miembro de CBZ devuelve `chunks` no-`None` y `path is None`; concatenar el generador reproduce EXACTAMENTE los bytes que se escribieron en el ZIP.
    - Asercion de seguridad: `page_bytes("0:../../etc/passwd")`, `page_bytes("0:Cap1.cbz!../../../etc/passwd")` y `page_bytes("raiz-no-registrada:x.jpg")` lanzan `SourceError` tipado. Ningun byte de fuera del root sale.
    - Asercion de seguridad: ningun `source_id` devuelto por `chapters()`/`pages()` contiene la ruta absoluta del root ni es una ruta absoluta (`Path(id).is_absolute()` es `False`).
    - Asercion negativa: `rg "httpx|requests|urllib|aiohttp" apps/backend/nyanko_api/sources/local_archive.py` no encuentra imports de red (el import-guard AST de la Fase 2 sigue en verde).
    - Comando: `cd apps/backend; python -m pytest tests/test_sources.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reescribir los tres tests que el contrato v2 pone rojos por construccion (H-5)</name>

  <read_first>
    - `apps/backend/tests/test_sources.py:42-58` (`_FuenteOk`), `:60-64` (`test_source_api_version_is_exact_integer`), `:67-80` (`test_source_protocol_is_runtime_checkable_and_minimal`), `:119-127` (`test_real_sources_match_protocol` + `_assert_source_signature` en `:303-305`).
    - `apps/backend/tests/test_source_api.py:36-58` — `_AlwaysFailSource` y `_WrongVersionSource`.
    - `apps/backend/tests/test_source_budget.py:33-55` — `_FuenteConFetch`.
    - `apps/backend/tests/test_source_engine.py:26-60` — `_FuenteCache` y sus subclases.
    - `apps/backend/nyanko_api/sources/registry.py:43-51` — la razon: `isinstance(source, Source)` sobre un Protocol runtime-checkable comprueba EXISTENCIA de atributos. Sin `page_bytes`, cada doble se registra como `rejected`, `registry.get(name)` lanza `KeyError` y sus tests mueren.
  </read_first>

  <action>
    Este es el trabajo que NO se le puede dejar al azar: el bump de contrato rompe tres tests verdes de
    la Fase 2 a proposito, y esta tarea es su duena.

    (a) `test_source_api_version_is_exact_integer` (`test_sources.py:60-64`): pasa a afirmar
    `SOURCE_API_VERSION == 2` y el literal correspondiente en el fichero de contrato. Anadir un
    comentario en espanol que diga POR QUE subio: el contrato gano `page_bytes` (D-16) y `registry.py`
    exige coincidencia exacta, asi que la version es lo unico que separa a una extension vieja de un
    fallo silencioso. La forma del test (entero exacto + literal en el fichero) NO cambia: sigue
    teniendo dientes.

    (b) `test_source_protocol_is_runtime_checkable_and_minimal` (`test_sources.py:67-80`): el conjunto
    esperado de metodos pasa a ser `{"search", "chapters", "pages", "page_bytes"}`. Se mantienen las
    aserciones negativas (`popular`, `latest`, `series_detail` siguen sin existir): el contrato gana UN
    metodo porque sin el no hay page pipe, no porque el contrato sea negociable.

    (c) `test_real_sources_match_protocol` (`test_sources.py:119-127`): anadir
    `_assert_source_signature(source, "page_bytes", ("page",))`.

    (d) Los CUATRO dobles de test — `_FuenteOk` (`test_sources.py`), `_AlwaysFailSource`
    (`test_source_api.py`), `_FuenteConFetch` (`test_source_budget.py`), `_FuenteCache`
    (`test_source_engine.py`) — ganan un `page_bytes` async minimo que devuelve un `SourcePageContent`
    con `chunks` (un generador de un solo bloque de bytes) y un `media_type`. Sin esto los cuatro se
    registran como `rejected` y ~20 tests mueren con `KeyError`. Importar `SourcePageContent` donde haga
    falta.

    NO tocar `conftest.py`, `pyproject.toml` ni `pytest.ini`. NO ejecutar la suite (el orquestador la
    corre fuera del sandbox: regla 2 de CODEX-RULES).
  </action>

  <acceptance_criteria>
    - Asercion de fuente: `rg "SOURCE_API_VERSION == 2" apps/backend/tests/test_sources.py` encuentra la asercion; `rg "SOURCE_API_VERSION == 1" apps/backend/tests/` no encuentra nada.
    - Asercion de fuente: el conjunto esperado en `test_source_protocol_is_runtime_checkable_and_minimal` es exactamente `{"search", "chapters", "pages", "page_bytes"}`.
    - Asercion de fuente: los cuatro ficheros `tests/test_sources.py`, `tests/test_source_api.py`, `tests/test_source_budget.py` y `tests/test_source_engine.py` definen `async def page_bytes` en su doble de fuente. `rg -c "async def page_bytes" apps/backend/tests/` devuelve >= 4 ficheros.
    - Asercion de comportamiento: `test_sources_endpoint_reports_ok_and_rejected_sources` sigue viendo `always_fail` con `status == "ok"` (o sea: el doble YA cumple el contrato v2 y no lo rechaza el registry).
    - Asercion negativa: `rg "conftest|pyproject|pytest.ini|.test-work" ` sobre el diff de este plan no encuentra nada — la infraestructura de tests no se toca (CODEX-RULES regla 3).
    - Comando (lo ejecuta el ORQUESTADOR, no el ejecutor): `cd apps/backend; python -m pytest tests/ -q --ignore=tests/test_packaged_sources.py` — la suite entera queda verde. Este es el gate del plan: el contrato v2 no puede dejar ni un test rojo detras.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS nivel 1. Bloquear cualquier amenaza `high`; no aceptar mitigaciones pendientes dentro de esta fase.

## Trust Boundaries

| Boundary | Description |
|---|---|
| CBZ/ZIP del disco -> parser del sidecar | Un CBZ puede venir de un torrent. Sus nombres de miembro y su `ComicInfo.xml` son entrada NO confiable aunque el fichero este en el disco del usuario. |
| `page_id` (cliente) -> `LocalArchiveSource` | El id lo compone el cliente en la URL. Es la unica entrada del page pipe y NO es una ruta: la contencion la impone `_resolve_id`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Information Disclosure | `LocalArchiveSource.page_bytes` | critical | mitigate | Path traversal por el `page_id`. Mitigacion: el id se resuelve por `_resolve_id` (`candidate.relative_to(root)`), y el miembro del ZIP se valida por pertenencia EXACTA a `namelist()`. Tests de traversal con `../`, ruta absoluta, root no registrado y miembro inexistente. |
| T-03-02 | Denial of Service | `_comic_info` (`xml.etree`) | high | mitigate | Billion laughs / quadratic blowup en un `ComicInfo.xml` de un CBZ descargado. Mitigacion: descartar sin parsear todo XML con declaracion de DTD/entidad y todo miembro `ComicInfo.xml` mayor de 1 MB. Sin dependencia nueva (`defusedxml` no esta en el arbol). |
| T-03-03 | Denial of Service | `pages()` sobre ZIP | medium | mitigate | Zip bomb al listar. Mitigacion parcial: listar `namelist()` NO descomprime; la descompresion ocurre pagina a pagina en bloques de 64 KB por el generador de `chunks`, nunca el capitulo entero en memoria. |
| T-03-04 | Tampering | `SOURCE_API_VERSION` | high | mitigate | Una extension escrita contra el contrato v1 (sin `page_bytes`) se registraria como `ok` y reventaria al servir una pagina. Mitigacion: `registry.py:36-42` exige coincidencia EXACTA de version; el bump a 2 la rechaza en el registro, visible y sin tumbar el sidecar (SRC-04). |
| T-03-05 | Information Disclosure | `SourcePageContent.path` | medium | accept | El campo lleva una ruta absoluta del disco. Vive SOLO en el proceso del sidecar y nunca se serializa a JSON ni se persiste (lo consume el plan 03 para construir un `FileResponse`). Aceptado: sin `path` no hay forma de servir un fichero suelto sin copiarlo a memoria. El plan 03 verifica que ninguna respuesta lo expone. |
</threat_model>

<verification>
1. `cd apps/backend; python -m pytest tests/test_sources.py -q -x`
2. `cd apps/backend; python -m pytest tests/ -q --ignore=tests/test_packaged_sources.py` — la suite ENTERA. Este plan cambia el contrato: si deja un solo test rojo, no esta hecho.
3. `cd apps/backend; python -c "from nyanko_api.sources import SourceEngine; print('WR-01 ok')"`
4. `rg "isinstance\(source, Local|if source.name ==" apps/backend/nyanko_api/` no encuentra ramas por fuente (D-18 / SRC-05).
</verification>

<success_criteria>
- RD-01 (mitad backend): CBZ, ZIP y carpeta se leen por el mismo camino y en orden natural.
- RD-08: `ComicInfo.xml` manda sobre el nombre del fichero; `number` es `float` y el 12.5 cabe.
- D-16/D-17: contrato v2 con `page_bytes`, y el registry rechazando por version exacta con el 2.
- WR-01 saldado.
- La suite de la Fase 2 queda verde con sus tres tests reescritos a proposito, no parcheados.
</success_criteria>

<output>
Create `.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-01-SUMMARY.md` when done.
Los resultados de tests van como `unknown` / «pendiente de verificacion por el orquestador» (CODEX-RULES regla 5).
</output>
