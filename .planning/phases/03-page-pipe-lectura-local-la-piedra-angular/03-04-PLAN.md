---
phase: 03-page-pipe-lectura-local-la-piedra-angular
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - apps/backend/nyanko_api/main.py
  - apps/backend/nyanko_api/models.py
  - apps/backend/tests/test_manga_api.py
autonomous: true
requirements: [RD-03, RD-05, RD-06, RD-07]

must_haves:
  truths:
    - "El renderer navega la biblioteca de manga con UN endpoint (`GET /api/manga/chapters`): `is_chapter` es DATO en la respuesta (plan 01), asi que el cliente sabe si un nodo es una serie (hay que seguir bajando) o un capitulo (se abre el reader) sin preguntarle a la fuente de que tipo es."
    - "RD-03: `GET/PUT /api/manga/prefs` lee y escribe `reader_prefs` por `(source, series_id)`. El modo se recuerda POR SERIE y sobrevive a reiniciar la app porque vive en SQLite, no en `localStorage`."
    - "RD-05: `GET/PUT /api/manga/progress` lee y escribe `reader_progress` por `(source, chapter_id)`. El usuario reanuda UN capitulo por la pagina donde lo dejo."
    - "RD-06: `POST /api/manga/reading-events` escribe la fila de «capitulo terminado» con `chapter` REAL. En esta fase NO LA LEE NADIE — eso es el diseno (D-15): el ROADMAP pone esta fase antes del sync para que el evento exista ANTES que su consumidor."
    - "RD-07: `double_page` y `double_page_offset` viajan en el mismo payload de prefs y son independientes del modo."
    - "Todo endpoint pasa por `SourceEngine` y mapea errores con `raise_source_http_error` (plan 03): el renderer recibe 404/415/429/502/503 tipados, nunca un 500 opaco."
    - "Toda URL de pagina que sale por JSON es RELATIVA (`_page_url`, plan 03). `normalizeAssetUrls` (`api.ts:202`) la resuelve en vivo contra el puerto actual. FND-05 sigue en verde POR CONSTRUCCION, y este plan lo demuestra llamando a `assert_no_persisted_urls` despues de escribir por la API."
    - "WR-06 (deuda de la Fase 2 que ESTA fase hace real): el registry se construye una sola vez en `lifespan` (`main.py:1408`), asi que una carpeta de biblioteca anadida en caliente es INVISIBLE para `LocalArchiveSource` hasta reiniciar el sidecar. Latente mientras nadie consumia el engine; bug de usuario en cuanto el reader lo consume. Se paga aqui: los handlers que anaden y borran carpetas reconstruyen `app.state.source_registry`."
  prohibitions:
    - "Ningun handler llama a `build_source_registry()` para SERVIR una peticion de lectura — hay un test verde de la Fase 2 que lo prohibe (`test_source_api.py:105-120`). La reconstruccion de WR-06 ocurre SOLO en los handlers que MUTAN las carpetas de biblioteca (alta/baja), que es donde el registry se queda obsoleto."
    - "El progreso NO se keyea por `media_id` (D-09): el vinculo con el tracker no existe hasta la Fase 4."
    - "El zoom y el paneo NO tienen endpoint: no se persisten (D-11)."
    - "Ninguna respuesta contiene una ruta del sistema de ficheros ni una URL absoluta."
    - "No se anade un enrutador a `main.py` (H-1): rutas planas sobre `app`, como las otras 58."
---

<objective>
Los cinco datos que el reader necesita del sidecar: que capitulos hay, que paginas tiene un capitulo,
que modo usa esta serie, por que pagina iba este capitulo, y «este capitulo se ha terminado».

Purpose: sin esto el ReaderView no tiene con quien hablar. Y de paso se paga WR-06, que la Fase 2
dejo escrito como latente y que esta fase convierte en un bug real de usuario: la carpeta que anades
hoy no aparece hasta que reinicias.
Output: `/api/manga/*` completo, con sus modelos Pydantic y su test de round-trip + guardia FND-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.planning/CODEX-RULES.md
</execution_context>

<context>
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-CONTEXT.md
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-PATTERNS.md
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-02-SUMMARY.md
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-03-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces (cuota de este plan)

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `GET /api/manga/chapters?source=&series_id=` | `main.py` | Hijos de un nodo. Devuelve `MangaChapter[]` con `is_chapter`, `number`, `title` |
| `GET /api/manga/pages?source=&chapter_id=` | `main.py` | Paginas de un capitulo. Devuelve `MangaPage[]` con la URL RELATIVA de cada una |
| `GET /api/manga/prefs?source=&series_id=` | `main.py` | Prefs de lectura de la serie. `null` si nunca se leyo |
| `PUT /api/manga/prefs` | `main.py` | Upsert de prefs (RD-03, RD-07) |
| `GET /api/manga/progress?source=&chapter_id=` | `main.py` | Pagina por la que iba el capitulo. `null` si nunca se abrio (RD-05) |
| `PUT /api/manga/progress` | `main.py` | Upsert de progreso (RD-05) |
| `POST /api/manga/reading-events` | `main.py` | Escribe la fila de «capitulo terminado» (RD-06) |
| `MangaChapter` | `models.py` | `source_id`, `title`, `series_id`, `number: float \| None`, `is_chapter: bool` |
| `MangaPage` | `models.py` | `index: int`, `filename: str`, `url: str` (RELATIVA: `/assets/pages/...`) |
| `ReaderPrefs` | `models.py` | `mode`, `fit`, `double_page`, `double_page_offset` |
| `ReaderPrefsUpdate` | `models.py` | Payload del `PUT` (todo opcional salvo la identidad) |
| `ReaderProgress` | `models.py` | `page: int`, `updated_at: str \| None` |
| `ReaderProgressUpdate` | `models.py` | Payload del `PUT` |
| `ReadingEventCreate` | `models.py` | Payload del `POST` de evento. `chapter: float \| None` |
| `apps/backend/tests/test_manga_api.py` | nuevo | Round-trip de los 7 endpoints + `assert_no_persisted_urls` + WR-06 |

Consume del plan 03: `_page_url`, `raise_source_http_error`, `_source_engine`.
Consume del plan 02: `get/set_reader_prefs`, `get/set_reader_progress`, `insert_reading_event`.
Consume del plan 01: `SourceChapter.number`, `SourceChapter.is_chapter`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modelos Pydantic y los siete endpoints planos</name>

  <read_first>
    - `apps/backend/nyanko_api/models.py:33-77` — `SourceCapabilitiesResponse`, `SourceInfo`, `LibraryFolder`, `LibraryFolderCreate`: la forma exacta de un modelo de este arbol (BaseModel plano, tipos explicitos, `| None = None` para lo opcional).
    - `apps/backend/nyanko_api/main.py:1497-1515` — `list_sources`: el analog EXACTO de un endpoint plano que lee del registry vivo.
    - `apps/backend/nyanko_api/main.py:2095-2112` — `GET`/`PUT /api/library/scan-settings`: el analog exacto de un par get/put con modelo Pydantic y `Depends(get_database)`.
    - `apps/backend/nyanko_api/main.py:1890-1935` — los handlers de `/api/library/folders` (alta y baja). Aqui es donde entra el fix de WR-06 (Task 2).
    - `apps/backend/nyanko_api/sources/engine.py:79-101` — `SourceEngine.chapters` (con su cache en memoria) y `.pages`.
    - `apps/backend/nyanko_api/database.py` — los cinco metodos del plan 02 (`get/set_reader_prefs`, `get/set_reader_progress`, `insert_reading_event`).
  </read_first>

  <action>
    En `models.py`, anadir los modelos de la tabla de artefactos de arriba. `MangaPage.url` es una
    cadena RELATIVA (`/assets/pages/...`): comentario en espanol recordando que el host y el puerto
    los pone el renderer (`normalizeAssetUrls`), y que meterlos aqui es exactamente como este proyecto
    perdio todas las portadas de la biblioteca.

    En `main.py`, siete rutas PLANAS sobre `app` (el patron de las otras 58; nada de enrutadores —
    `test_source_api.py:155-162` lo prohibe). Todas con `source: str = "local_archive"` como query
    param con defecto, y `series_id`/`chapter_id` TAMBIEN como query params (no como segmento de
    ruta): los ids opacos llevan `:` y `/` dentro y como query param viajan sin pelearse con el
    enrutador.

    `GET /api/manga/chapters` -> `list[MangaChapter]`. Llama a `_source_engine(request).chapters(source,
    series_id)`, mapea `SourceChapter` -> `MangaChapter` (incluyendo `number` e `is_chapter`).
    `GET /api/manga/pages` -> `list[MangaPage]`. Llama a `.pages(source, chapter_id)` y construye cada
    `url` con `_page_url(page.source_id)` mas el query param de fuente si no es la de por defecto.
    `GET /api/manga/prefs` -> `ReaderPrefs | None`. `PUT /api/manga/prefs` (body `ReaderPrefsUpdate`)
    -> `ReaderPrefs` ya persistido.
    `GET /api/manga/progress` -> `ReaderProgress | None`. `PUT /api/manga/progress` (body
    `ReaderProgressUpdate`) -> 204.
    `POST /api/manga/reading-events` (body `ReadingEventCreate`) -> `{"id": <int>}`. Escribe con
    `insert_reading_event`. NADIE lo lee en esta fase: es el diseno (D-15).

    Los cuatro endpoints que hablan con una fuente (`chapters`, `pages`) envuelven la llamada en
    `try/except SourceError` -> `raise_source_http_error(error, source)` (plan 03). Los que solo tocan
    la BD no necesitan fuente ni engine: van por `Depends(get_database)`.
  </action>

  <acceptance_criteria>
    - Asercion de comportamiento: `GET /api/manga/chapters?series_id=0:.` sobre una biblioteca con `Serie/Cap 1/*.jpg` devuelve un nodo con `is_chapter == false` (es una serie); `?series_id=0:Serie` devuelve `Cap 1` con `is_chapter == true`. El cliente navega el arbol con un solo endpoint.
    - Asercion de comportamiento (RD-08 de punta a punta): un `Cap 003.cbz` con `ComicInfo.xml` que declara `<Number>12.5</Number>` sale por la API con `number == 12.5`.
    - Asercion de comportamiento: `GET /api/manga/pages?chapter_id=...` devuelve las paginas en orden natural y CADA `url` empieza por `/assets/pages/`.
    - Asercion de seguridad (FND-05): ninguna `url` de la respuesta contiene `http`, `127.0.0.1` ni un numero de puerto. Asercion literal en el test.
    - Asercion de comportamiento (RD-03): `PUT /api/manga/prefs` con `mode: "vertical"` para `0:SoloLeveling` y luego `GET` de `0:Berserk` devuelve `null` o `rtl` — no se contaminan entre series.
    - Asercion de comportamiento (RD-07): `PUT` de `double_page_offset: 1` y luego `PUT` de `mode: "ltr"` deja `double_page_offset == 1`.
    - Asercion de comportamiento (RD-05): `PUT /api/manga/progress` con `page: 47` y `GET` devuelve `47`. Un capitulo nunca abierto devuelve `null`.
    - Asercion de comportamiento (RD-06): `POST /api/manga/reading-events` con `chapter: 12.5` devuelve un `id`, y leer la fila de la BD da `chapter == 12.5` (el decimal sobrevive).
    - Asercion de comportamiento: pedir paginas de un `.cbr` devuelve 415; de un capitulo inexistente, 404; de una fuente desconocida, 404. Ninguno 500.
    - Asercion de seguridad: tras ejercitar los `PUT`/`POST` contra la API, `assert_no_persisted_urls(connection)` sobre la BD pasa — y el test lo llama DESPUES de las escrituras, no antes.
    - Comando: `cd apps/backend; python -m pytest tests/test_manga_api.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-06 — la carpeta anadida en caliente deja de ser invisible</name>

  <read_first>
    - `apps/backend/nyanko_api/main.py:1918-1935` — `POST /api/library/folders` y `DELETE /api/library/folders/{folder_id}`. Hoy escriben en la BD y NO tocan el registry.
    - `apps/backend/nyanko_api/main.py:1408-1410` — `lifespan`: `app.state.source_registry = build_source_registry(library_folders=database.get_library_folders())`. Se construye UNA VEZ, al arrancar. `LocalArchiveSource._load_roots` congela las raices en el `__init__`.
    - `apps/backend/tests/test_source_api.py:105-152` — los dos tests que acotan esto: `GET /api/sources` NO puede reconstruir el registry, y `lifespan` lo construye exactamente una vez. El fix no puede romper ninguno de los dos.
    - `.planning/phases/02-.../02-VERIFICATION.md` §Warnings — WR-06 literal: «Latente hoy (nada consume el engine), bug de usuario en cuanto se cablee en Fase 3».
  </read_first>

  <action>
    En los handlers que MUTAN las carpetas de biblioteca (`POST /api/library/folders` y
    `DELETE /api/library/folders/{folder_id}`), tras escribir en la BD, reconstruir el registry:
    `request.app.state.source_registry = build_source_registry(library_folders=database.get_library_folders())`.
    Anadir `request: Request` a la firma de esos handlers si no lo tienen.

    Comentario en espanol encima explicando el porque (es WR-06 de la Fase 2): el registry se
    construye una sola vez en `lifespan` y `LocalArchiveSource` congela sus raices al instanciarse; sin
    esto, la carpeta que el usuario acaba de anadir no existe para el reader hasta que reinicia el
    sidecar — y el usuario no tiene forma de saber que reiniciar es lo que hace falta.

    Ojo con el limite: se reconstruye SOLO en la mutacion (alta/baja de carpeta), NUNCA al servir una
    peticion de lectura. Reconstruir por peticion es lo que el test verde de la Fase 2 prohibe, y con
    razon: crea un `RateLimitedClient` nuevo por fuente cada vez, o sea, un presupuesto de peticiones
    nuevo cada vez — que es exactamente el bug que la Fase 2 existia para prevenir.
  </action>

  <acceptance_criteria>
    - Asercion de comportamiento (WR-06, el test que importa): con el sidecar arrancado y CERO carpetas, `POST /api/library/folders` con una carpeta que contiene un capitulo, e inmediatamente despues `GET /api/manga/chapters?series_id=0:.` devuelve el capitulo. SIN reiniciar. Contra el codigo actual este test FALLA (la fuente no ve la carpeta).
    - Asercion de comportamiento: `DELETE /api/library/folders/{id}` y despues `GET /api/manga/chapters` sobre esa raiz devuelve 404 — la raiz deja de estar registrada.
    - Asercion negativa: `test_sources_endpoint_uses_live_registry_without_rebuilding` (`test_source_api.py:105-120`) SIGUE VERDE — `GET /api/sources` no reconstruye nada.
    - Asercion negativa: `test_lifespan_builds_source_registry_once` (`test_source_api.py:123-152`) SIGUE VERDE — el arranque sigue construyendo el registry exactamente una vez.
    - Asercion de fuente: `rg -n "build_source_registry" apps/backend/nyanko_api/main.py` aparece SOLO en `lifespan` y en los dos handlers de mutacion de carpetas. En ningun handler de lectura.
    - Comando: `cd apps/backend; python -m pytest tests/test_manga_api.py tests/test_source_api.py -q -x`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS nivel 1. Bloquear cualquier amenaza `high`.

## Trust Boundaries

| Boundary | Description |
|---|---|
| renderer -> `/api/manga/*` | `source`, `series_id`, `chapter_id`, `page` y el payload del evento llegan del renderer. Los ids son opacos y se resuelven a traves de la fuente; el resto se persiste. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-15 | Information Disclosure | `GET /api/manga/chapters` / `pages` | high | mitigate | Los mismos ids opacos que la ruta de paginas, con la misma superficie de traversal. Mitigacion: TODO pasa por `SourceEngine` -> `_resolve_id` -> `candidate.relative_to(root)`. Ningun endpoint acepta ni devuelve una ruta del sistema de ficheros. |
| T-03-16 | Tampering | `PUT /api/manga/progress` / `prefs` | high | mitigate | Persistir una URL absoluta con el puerto efimero (el bug que ya costo todas las portadas). Mitigacion: las columnas no admiten URL y el test llama `assert_no_persisted_urls` DESPUES de escribir por la API — no sobre tablas vacias. |
| T-03-17 | Denial of Service | Reconstruccion del registry (WR-06) | medium | mitigate | Reconstruir el registry crea un `RateLimitedClient` nuevo por fuente: hacerlo por peticion regalaria un presupuesto de peticiones nuevo en cada request (el bug que la Fase 2 existe para prevenir). Mitigacion: se reconstruye SOLO en alta/baja de carpeta, y los dos tests verdes de la Fase 2 que acotan esto siguen pasando. Techo aceptado: los `RateLimitedClient` viejos no se cierran (WR-08, deuda de Fase 2) — con una fuente local de 1 req/min es irrelevante; se paga en Fase 9 o cuando haya fuentes online. |
| T-03-18 | Spoofing | `POST /api/manga/reading-events` | low | accept | Un evento falsificado no puede hacer nada: NADIE lo lee en esta fase (D-15). La Fase 5 (proponer/confirmar/deshacer) es quien debe validar el vinculo antes de escribir al proveedor — y LNK-04 ya dice que el sync falla cerrado sin vinculo confirmado. |
</threat_model>

<verification>
1. `cd apps/backend; python -m pytest tests/test_manga_api.py -q -x`
2. `cd apps/backend; python -m pytest tests/ -q --ignore=tests/test_packaged_sources.py` — suite entera.
3. `rg -n "build_source_registry" apps/backend/nyanko_api/main.py` — solo en `lifespan` y en alta/baja de carpeta.
4. Round-trip manual (ORQUESTADOR, opcional): arrancar el sidecar, `POST /api/library/folders`, `GET /api/manga/chapters?series_id=0:.` sin reiniciar -> el capitulo aparece (WR-06 pagado).
</verification>

<success_criteria>
- RD-03, RD-05, RD-06 y RD-07 tienen endpoint, persistencia y round-trip probado.
- Ninguna URL emitida lleva host ni puerto; la guardia FND-05 se llama despues de escrituras reales por la API.
- WR-06 saldado: la carpeta anadida en caliente es visible sin reiniciar, y los dos tests de la Fase 2 que acotan el registry siguen verdes.
</success_criteria>

<output>
Create `.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-04-SUMMARY.md` when done.
Los resultados de tests van como `unknown` / «pendiente de verificacion por el orquestador» (CODEX-RULES regla 5).
</output>
