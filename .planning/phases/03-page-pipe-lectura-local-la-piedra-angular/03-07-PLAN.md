---
phase: 03-page-pipe-lectura-local-la-piedra-angular
plan: 07
type: execute
wave: 6
depends_on: [03-06]
files_modified:
  - apps/desktop/src/readerWindow.test.ts
  - apps/desktop/scripts/reader-rss.mjs
  - apps/desktop/electron/main/csp.test.ts
  - apps/desktop/electron/main/splash.ts
  - apps/desktop/index.html
  - apps/desktop/electron.vite.config.ts
  - apps/desktop/package.json
autonomous: true
requirements: [RD-09]

must_haves:
  truths:
    - "RD-09 / D-08: el techo de memoria es un NUMERO MEDIDO, no un claim en un SUMMARY. `npm run test:reader-rss` recorre un capitulo REAL de 200 paginas de punta a punta Y VUELVE, mide el RSS del proceso RENDERER (`app.getAppMetrics()` filtrado por `win.webContents.getOSProcessId()`, campo `memory.workingSetSize`), imprime el numero y SALE CON CODIGO DISTINTO DE CERO si supera 500 MB."
    - "La API de medicion es la CORRECTA y esta verificada contra `node_modules/electron/electron.d.ts` (Electron 43.1.0): `WebContents.getOSProcessId()` (l.18006) y `App.getAppMetrics(): ProcessMetric[]` (l.1153); `ProcessMetric.memory.workingSetSize` (l.9446) es el RSS, en KB, y es multiplataforma. **`webContents.getProcessMemoryInfo()` NO EXISTE**: `getProcessMemoryInfo()` vive en `interface Process` (l.26789) y mide EL PROCESO ACTUAL — usarlo desde el main mediria el MAIN, no el renderer, e imprimiria un numero plausible y sin sentido. Y `privateBytes` (l.9442) es SOLO win32: no se usa."
    - "La eleccion del 500: ~5 bitmaps x 24 MB = 120 MB sobre la linea base de Electron (~250 MB). 500 MB deja margen real y sigue apretando lo suficiente para que un leak de bitmaps FALLE el test. «Se siente bien» no es un criterio — es lo que permite que un leak llegue a produccion."
    - "El segundo gate, independiente y barato: `decodeWindow` es una funcion pura y su test afirma que NUNCA devuelve mas de `MAX_LIVE_PAGES` (5) indices, para ningun `current` y ningun `total`. Si alguien sube la ventana, este test falla en milisegundos sin arrancar Electron."
    - "ROADMAP criterio 7 / Seam G: existe una CSP. Hoy NO HAY NI UNA en toda la app. Si no aterriza aqui, no aterriza — la CSP no tiene otro dueno en el ROADMAP."
    - "H-3 (la CSP se escribe CORREGIDA, no literal): la CSP literal del ROADMAP (`img-src 'self' http://127.0.0.1:*`) BORRA todas las portadas de Descubrir, Temporadas, Busqueda y el avatar — son URLs del CDN del proveedor que llegan crudas al renderer (`anilist.py:613` -> `DiscoveryView.tsx:281`). Falta `https:`. Y sin `ws://127.0.0.1:*` en `connect-src` muere `playbackSocket()` (`api.ts:489`). El criterio del ROADMAP se cumple en su INTENCION (existe una CSP, `webSecurity` sigue en `true`); su texto tenia un bug."
    - "H-3 (el vehiculo): la CSP tiene que ser un `<meta http-equiv=\"Content-Security-Policy\">`. En produccion la ventana carga por `file://` (`index.ts:83`, `win.loadFile`) y el splash por `data:text/html` (`splash.ts:71`): ninguno de los dos es una respuesta HTTP, asi que no hay cabeceras que interceptar."
    - "El origen de desarrollo es `http://localhost:1420` (`electron.vite.config.ts`: «Port 1420 es load-bearing: el CORS del backend lo tiene fijado»), NO 5173. Y en dev Vite inyecta un preambulo INLINE de React Refresh: una CSP con `script-src 'self'` a secas rompe `npm run dev`. Por eso la CSP se emite segun el modo, desde el `transformIndexHtml` del config — estricta en produccion, con los origenes de dev anadidos en desarrollo."
    - "La CSP del splash puede ser mucho mas dura que la de la app: el splash no carga NADA externo (ni imagenes, ni red). `default-src 'none'` + lo justo para sus propios estilos y sus manejadores inline (`splash.ts:47-49`, los botones Reintentar / Abrir logs / Salir — y el splash es justo la pantalla que aparece cuando algo ya ha fallado: dejar esos botones muertos seria el peor sitio posible para una CSP mal puesta)."
  prohibitions:
    - "PROHIBIDO que el harness imprima un numero que no ha medido. Si no puede arrancar el sidecar, o no puede cargar el renderer, o no puede leer el RSS: SALE CON ERROR y lo dice. Un harness que falla en silencio y devuelve verde es peor que no tener harness (CODEX-RULES regla 7)."
    - "PROHIBIDO medir con `process.getProcessMemoryInfo()` (mide el proceso MAIN) ni con cualquier cosa que no este filtrada por el PID del renderer. Es el modo de fallo mas peligroso de la fase: da un numero plausible, siempre por debajo de 500 MB, y deja RD-09 dado por bueno sin haber medido nada. Si el RSS del renderer no se puede obtener, el harness FALLA — no cae a medir otra cosa."
    - "PROHIBIDO relajar la ventana de decodificacion para que el numero salga. Si el RSS se pasa de 500 MB, el bug esta en el reader, no en el criterio."
    - "PROHIBIDO usar el hook de cabeceras de respuesta de la sesion de Electron para inyectar la CSP: en produccion no hay respuesta HTTP que interceptar (`file://` y `data:`). Asercion negativa en los criterios."
    - "`webSecurity` sigue en `true`, y `contextIsolation: true` / `nodeIntegration: false` / `sandbox: true` no se tocan — ni en la ventana principal ni en el splash."
    - "No se anade ninguna dependencia npm. `electron`, `sharp` y `tsx` ya son devDependencies; `node:test`, `node:sqlite` y `fetch` son de la plataforma."
    - "No se toca `conftest.py`, `pyproject.toml` ni `pytest.ini` (CODEX-RULES regla 3)."
---

<objective>
Convertir RD-09 de intencion en NUMERO, y poner la unica CSP que va a existir en toda la app.

Purpose: el techo de memoria del reader es el criterio de aceptacion mas caro de la fase y no tiene
precedente en el arbol — y un claim en un SUMMARY no es evidencia. La CSP es Seam G: no tiene otro
dueno en el ROADMAP, asi que si no aterriza aqui no aterriza nunca.
Output: dos gates independientes para RD-09 (uno barato y puro, uno caro y real) y una CSP que
protege sin borrar las portadas de media app.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.planning/CODEX-RULES.md
</execution_context>

<context>
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-CONTEXT.md
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-PATTERNS.md
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-06-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces (cuota de este plan)

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `apps/desktop/src/readerWindow.test.ts` | nuevo | Test unitario (`node:test`) de la ventana pura. Gate barato de RD-09 |
| `apps/desktop/scripts/reader-rss.mjs` | nuevo | Harness de Electron: 200 paginas reales, ida y vuelta, RSS medido. Gate real de RD-09 |
| `npm run test:reader` | `package.json` | `node --import tsx --test src/readerWindow.test.ts` |
| `npm run test:reader-rss` | `package.json` | `electron scripts/reader-rss.mjs` |
| `npm run test:csp` | `package.json` | `node --import tsx --test electron/main/csp.test.ts` |
| `<meta http-equiv="Content-Security-Policy">` | `index.html` | La CSP de la ventana principal (Seam G) |
| CSP del splash | `electron/main/splash.ts` | `default-src 'none'` + lo justo. El splash no carga nada externo |
| `cspPlugin` (transformIndexHtml) | `electron.vite.config.ts` | Emite la CSP segun el modo: estricta en prod, con los origenes de dev en desarrollo |
| `apps/desktop/electron/main/csp.test.ts` | nuevo | Afirma las directivas que NO pueden faltar y las que NO pueden estar |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: El gate barato — la ventana de decodificacion, testeada como funcion pura</name>

  <read_first>
    - `apps/desktop/src/readerWindow.ts` — `decodeWindow`, `pagePairs`, `MAX_LIVE_PAGES` (plan 06).
    - `apps/desktop/src/native.test.ts` — el analog EXACTO de un test de este arbol: `node:test` + `node:assert`, sin framework, sin fixtures, ejecutado por `node --import tsx --test`.
    - `apps/desktop/package.json` — los scripts `test:datadir`, `test:sidecar`, `test:native`, `test:prefs`. El script nuevo copia esa forma.
  </read_first>

  <action>
    Crear `apps/desktop/src/readerWindow.test.ts` con `node:test` + `node:assert/strict` (el patron de
    `native.test.ts`; NO se anade vitest ni jest).

    Tests, y el primero es EL que importa:
      - PROPIEDAD: para `total = 200` y TODOS los `current` de 1 a 200, `decodeWindow(current, total)`
        devuelve como mucho `MAX_LIVE_PAGES` indices, todos dentro de `[1, total]`, sin duplicados y
        en orden. Un bucle de 200 iteraciones con un assert dentro. Si alguien sube la ventana «solo un
        poquito», este test falla en milisegundos y sin arrancar Electron.
      - `MAX_LIVE_PAGES === 5` y `decodeWindow(50, 200)` devuelve exactamente `[48, 49, 50, 51, 52]` —
        el **+-2 literal de D-07** (`DECODE_BEHIND = 2`, `DECODE_AHEAD = 2`, 2+1+2 = 5). Comentario en
        espanol: el 5 es lo que hace alcanzable el techo de 500 MB de RSS (~5 x 24 MB = 120 MB sobre la
        linea base de Electron); subirlo a 10 son 240 MB de bitmaps y el otro gate se pone rojo.
      - Bordes: `decodeWindow(1, 200)` no incluye 0 ni negativos; `decodeWindow(200, 200)` no incluye
        201; `decodeWindow(1, 2)` devuelve solo `[1, 2]`.
      - `pagePairs` (RD-07): con offset 0 y con offset 1, y con un total impar. Que la portada quede
        sola con offset 1.

    En `package.json`, anadir `"test:reader": "node --import tsx --test src/readerWindow.test.ts"`.
  </action>

  <acceptance_criteria>
    - Asercion de comportamiento: `npm run test:reader --workspace @nyanko/desktop` pasa.
    - Asercion de comportamiento (dientes): subir `DECODE_AHEAD` a 5 en `readerWindow.ts` pone ROJO el test de la propiedad. El orquestador puede comprobarlo cambiandolo a mano y revirtiendo. Un test de techo que no falla al subir el techo no vale nada.
    - Asercion de fuente: el test importa de `./readerWindow` y NO importa React, ni el DOM, ni `api`.
    - Comando: `npm run test:reader --workspace @nyanko/desktop`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: El gate real — medir el RSS del renderer con un capitulo de 200 paginas</name>

  <read_first>
    - `apps/backend/tests/test_packaged_sources.py` — EL PRECEDENTE del arbol para «arranca el sidecar de verdad en un data dir temporal y hablale por HTTP»: lanza el proceso con `NYANKO_DATA_DIR` fresco y LEE EL PUERTO de `<data_dir>/port`. El harness hace exactamente eso.
    - `apps/desktop/src/api.ts:67-90` — `readAppDataFile` / `resolveApiUrl`: el renderer descubre el puerto del sidecar llamando a `native.readAppDataFile("port")`, que va por el bridge `window.nyanko` -> `ipcMain.handle("readAppDataFile", ...)`. **El harness tiene que registrar ESE handler** apuntando a su data dir temporal, o el renderer caera al 8765 hardcodeado y no encontrara nada.
    - `apps/desktop/electron/main/ipc.ts:30-40` — el handler `readAppDataFile` real. Copiar su forma.
    - `apps/desktop/electron/main/index.ts:40-50, 78-90` — las `webPreferences` (`contextIsolation`, `nodeIntegration: false`, `sandbox: true`, `webSecurity: true`) y el `win.loadFile(join(__dirname, "../renderer/index.html"))`. El harness usa LAS MISMAS `webPreferences` y el MISMO preload construido (`out/preload/index.cjs`): si no, no esta midiendo la app.
    - `apps/desktop/scripts/publish-bridge.mjs` — la forma de un script `.mjs` de este arbol.
    - `apps/desktop/package.json` — `sharp` y `electron` ya son devDependencies. No se anade ninguna.
  </read_first>

  <action>
    Crear `apps/desktop/scripts/reader-rss.mjs`, un main de Electron que se ejecuta con
    `electron scripts/reader-rss.mjs` y mide de verdad.

    Requisito previo: `out/` construido (`npm run build`). Si no existe `out/renderer/index.html`, el
    script FALLA con un mensaje que diga que hay que construir antes. No lo construye el solo (eso es
    del orquestador).

    Pasos:
    1. `app.commandLine.appendSwitch("js-flags", "--expose-gc")` — para poder forzar el GC antes de
       medir. Sin esto se mide basura sin recoger y el numero no significa nada.
    2. Data dir temporal (`fs.mkdtemp` en el tmp del SO). Dentro, una biblioteca sintetica:
       `<tmp>/biblioteca/Serie/Cap 1/` con **200 JPEG de 2000x3000** generados con `sharp` (ruido o un
       degradado; lo que importa es el TAMANO DEL BITMAP: 2000x3000x4 = 24 MB en RAM, que es el numero
       real del requisito). Generarlos una vez y cachearlos entre ejecuciones si ya existen.
    3. Arrancar el sidecar como proceso hijo con `NYANKO_DATA_DIR=<tmp>` y puerto dinamico, igual que
       `test_packaged_sources.py`: usar el python del venv (`apps/backend/.venv/Scripts/python.exe` en
       Windows) y ESPERAR a que aparezca `<tmp>/port`, con timeout. Si no arranca: fallar y decirlo.
    4. `POST /api/library/folders` (con `fetch`, nativo en Node 22) apuntando a `<tmp>/biblioteca`.
       Gracias a WR-06 (plan 04) el registry se reconstruye y la fuente ve la carpeta sin reiniciar —
       si esto falla, el fallo es real y hay que reportarlo, no rodearlo.
    5. `ipcMain.handle("readAppDataFile", ...)` leyendo de `<tmp>` (es como el renderer descubre el
       puerto: `api.ts:81`). Registrar tambien los handlers minimos que el preload exija para no
       reventar al arrancar.
    6. `BrowserWindow` con las MISMAS `webPreferences` que `index.ts:42-48` y el preload de
       `out/preload/index.cjs`. `loadFile("out/renderer/index.html")`.
    7. Conducir el reader por `webContents.executeJavaScript` y/o `webContents.sendInputEvent` (teclas
       reales: es la misma ruta que usa el usuario, y de paso ejercita RD-04): navegar hasta el
       capitulo, y recorrer las 200 paginas HACIA ADELANTE y luego las 200 HACIA ATRAS, esperando a que
       cada pagina cargue.
    8. Forzar GC (`executeJavaScript("window.gc && window.gc()")`), esperar ~2 s, y medir EL PROCESO
       RENDERER — no el main. La ruta es esta, y esta verificada contra `electron.d.ts` de Electron
       43.1.0 (no la cambies por otra que «parezca» equivalente):
         - `const pid = win.webContents.getOSProcessId();`  (l.18006)
         - `const metric = app.getAppMetrics().find((p) => p.pid === pid);`  (l.1153)
         - `const rssMB = metric.memory.workingSetSize / 1024;`  (`workingSetSize` es KB, l.9446)
       `workingSetSize` es el RSS y es multiplataforma. NO uses `privateBytes` (l.9442: solo win32).
       Y OJO — la trampa: **`webContents.getProcessMemoryInfo()` NO EXISTE**. El metodo con ese nombre
       vive en `interface Process` (l.26789) y mide EL PROCESO ACTUAL, o sea el MAIN. Llamarlo desde
       aqui compila, corre, e imprime un numero plausible por debajo de 500 MB que NO ES el renderer:
       daria RD-09 por bueno sin haber medido nada. Si `metric` sale `undefined`, el harness FALLA;
       no cae a medir otro proceso.
       Imprimir el numero en MB con dos decimales, junto con el pico observado durante el recorrido
       (muestrear `getAppMetrics()` cada N paginas: el pico importa tanto como el valor final).
    9. `app.exit(rss > 500 ? 1 : 0)` y una linea final legible:
       `RSS renderer: 312.44 MB (techo 500 MB) -> OK` / `-> FALLO`.
    10. Limpiar: matar el sidecar, borrar el data dir temporal.

    HONESTIDAD (CODEX-RULES regla 7): si CUALQUIER paso falla (el sidecar no arranca, el renderer no
    carga, `getAppMetrics()` no trae el `metric` del PID del renderer), el script sale con codigo distinto de cero y dice
    QUE paso fallo. NO imprime un numero que no ha medido. Un harness que devuelve verde cuando no ha
    medido nada es peor que no tenerlo.

    En `package.json`: `"test:reader-rss": "electron scripts/reader-rss.mjs"`.

    NOTA PARA EL EJECUTOR: no puedes ejecutar este harness (tu sandbox no arranca Electron ni el
    sidecar). Escribelo con cuidado, deja claro en el SUMMARY que NO lo has ejecutado, y el
    orquestador lo corre. Si algo no te cuadra del mecanismo de descubrimiento de puerto o del
    preload, PARA Y DILO (regla 6) en vez de inventar un camino alternativo.
  </action>

  <acceptance_criteria>
    - Asercion de comportamiento (EL GATE de RD-09, lo ejecuta el ORQUESTADOR): `npm run build --workspace @nyanko/desktop && npm run test:reader-rss --workspace @nyanko/desktop` imprime un RSS medido y sale con 0. El numero impreso es < 500 MB.
    - Asercion de comportamiento (dientes): con la ventana de decodificacion desactivada a mano (montar `pages` entero en `ReaderView.tsx`), el harness sale con codigo != 0. Si no falla, el harness no esta midiendo nada y RD-09 no esta verificado. El orquestador debe comprobar esto ANTES de dar la fase por buena.
    - Asercion de comportamiento: el harness recorre las 200 paginas de ida Y de vuelta (el requisito dice «de punta a punta y volver»: un leak que solo se manifiesta al retroceder es un leak igual).
    - Asercion de fuente: las paginas sinteticas son de 2000x3000 (24 MB de bitmap). Con imagenes pequenas el test seria verde con o sin ventana, y no valdria nada.
    - Asercion de fuente: `rg "getOSProcessId|getAppMetrics|workingSetSize" apps/desktop/scripts/reader-rss.mjs` aparece — el RSS se mide del proceso RENDERER (filtrado por su PID), no del main.
    - Asercion negativa (LA que evita el falso verde): `rg "getProcessMemoryInfo|privateBytes" apps/desktop/scripts/reader-rss.mjs` no encuentra NADA. El primero no existe en `WebContents` y desde el main mide el proceso equivocado; el segundo es solo win32.
    - Asercion negativa: `rg "500|OK" ` en el harness no aparece en ningun `console.log` que se ejecute sin haber medido. Todo camino de fallo sale con codigo != 0.
    - Comando (ORQUESTADOR): `npm run test:reader-rss --workspace @nyanko/desktop`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: La CSP (Seam G) — corregida, no literal, y en las dos ventanas</name>

  <read_first>
    - `apps/desktop/index.html` — 14 lineas. El `<head>` donde va el `<meta>`. Fijate en el `<link rel="icon" href="data:image/svg+xml,...">`: por eso `img-src` necesita `data:`.
    - `apps/desktop/electron/main/splash.ts:14-55` — `SPLASH_HTML` entero, y en particular los `onclick=` INLINE de los botones Reintentar / Abrir logs / Salir (lineas 47-49). Una `script-src` sin `'unsafe-inline'` los deja muertos — y el splash es justo la pantalla que aparece cuando algo YA ha fallado.
    - `apps/desktop/electron/main/splash.ts:71` — `win.loadURL("data:text/html;...")`. Y `electron/main/index.ts:83` — `win.loadFile(...)`. NINGUNO de los dos es una respuesta HTTP: por eso el vehiculo tiene que ser un `<meta>` y no una cabecera.
    - `apps/desktop/electron.vite.config.ts` — el config. El puerto de dev es **1420** («load-bearing: el CORS del backend lo tiene fijado»), NO 5173.
    - `apps/desktop/src/api.ts:489-492` — `playbackSocket()`: un `WebSocket` a `ws://127.0.0.1:<puerto>`. Sin `ws://127.0.0.1:*` en `connect-src`, MUERE.
    - `apps/desktop/src/DiscoveryView.tsx:281` y `apps/desktop/src/App.tsx:1185` — las portadas del CDN del proveedor (`https://s4.anilist.co/...`) que llegan CRUDAS al renderer. Sin `https:` en `img-src`, la app se queda sin una sola portada en Descubrir, Temporadas, Busqueda y el avatar. Este es el bug de la CSP literal del ROADMAP (H-3).
  </read_first>

  <action>
    (a) `apps/desktop/index.html`: anadir en el `<head>` un
    `<meta http-equiv="Content-Security-Policy" content="%CSP%" />` — un marcador que el config
    sustituye segun el modo.

    (b) `apps/desktop/electron.vite.config.ts`: anadir al `renderer` un plugin minimo con
    `transformIndexHtml` que sustituya `%CSP%`. Dos valores:

    PRODUCCION (el estricto):
      `default-src 'self'; img-src 'self' http://127.0.0.1:* https: blob: data:;
       connect-src 'self' http://127.0.0.1:* ws://127.0.0.1:*; style-src 'self' 'unsafe-inline';
       script-src 'self'; object-src 'none'; frame-src 'none'; base-uri 'none'; form-action 'none'`

    DESARROLLO: lo mismo, mas el origen de dev en `connect-src` (`http://localhost:1420
    ws://localhost:1420`) y `'unsafe-inline'` en `script-src`. Comentario en espanol obligatorio
    explicando por que dev es mas laxo: Vite inyecta un preambulo INLINE de React Refresh y sin
    `'unsafe-inline'` el `npm run dev` no arranca — es una concesion SOLO de desarrollo, y por eso se
    emite desde el config y no se escribe a mano en el HTML (donde un dia acabaria en produccion).

    Cada directiva lleva su comentario en espanol diciendo QUE se rompe sin ella:
      - `https:` en `img-src` -> sin esto desaparecen TODAS las portadas del CDN del proveedor. Es el
        bug de la CSP literal del ROADMAP.
      - `data:` en `img-src` -> el favicon de `index.html:6`.
      - `blob:` en `img-src` -> imagenes generadas en el renderer.
      - `http://127.0.0.1:*` en `img-src` -> las paginas del reader y los assets locales del sidecar.
      - `ws://127.0.0.1:*` en `connect-src` -> `playbackSocket()` (`api.ts:489`). Sin esto, la
        deteccion de reproduccion se queda muda.
      - `'unsafe-inline'` en `style-src` -> todo el arbol usa `style={{...}}`.

    (c) `apps/desktop/electron/main/splash.ts`: anadir en el `<head>` de `SPLASH_HTML` su propia CSP,
    que puede ser MUCHO mas dura porque el splash no carga NADA externo — ni imagenes ni red:
      `default-src 'none'; style-src 'unsafe-inline'; script-src 'unsafe-inline'`
    Comentario en espanol: el `'unsafe-inline'` de `script-src` es para los tres manejadores `onclick`
    de los botones (Reintentar / Abrir logs / Salir); sin el, el splash aparece justo cuando algo ha
    fallado y sus botones no responden. Como `default-src` es `'none'`, no hay nada que un script
    inyectado pudiera cargar ni a donde exfiltrar.

    (d) `apps/desktop/electron/main/csp.test.ts` (`node:test`, patron de `compat-paths.test.ts`): lee
    `index.html`, `electron.vite.config.ts` y `splash.ts` como TEXTO y afirma:
      - `index.html` contiene un `<meta http-equiv="Content-Security-Policy"`.
      - La CSP de produccion contiene `https:` en `img-src` (el bug del ROADMAP — si alguien la
        «arregla» quitandolo, este test lo caza y las portadas se salvan).
      - La CSP de produccion contiene `ws://127.0.0.1:*` en `connect-src` (o `playbackSocket()` muere).
      - La CSP de produccion contiene `data:` y `blob:` en `img-src`, y `http://127.0.0.1:*` en
        `img-src` y `connect-src`.
      - La CSP de PRODUCCION **no** contiene `'unsafe-inline'` en `script-src` (la concesion es solo de
        dev) ni `'unsafe-eval'` en ninguna directiva.
      - `SPLASH_HTML` contiene su `<meta>` con `default-src 'none'`.
      - `webSecurity` sigue `true` y `nodeIntegration` sigue `false` en las dos ventanas (`index.ts` y
        `splash.ts`).
      - Asercion NEGATIVA: no se usa el hook de cabeceras de respuesta de la sesion de Electron para
        inyectar la CSP (en produccion no hay respuesta HTTP que interceptar: `file://` y `data:`).

    En `package.json`: `"test:csp": "node --import tsx --test electron/main/csp.test.ts"`.
  </action>

  <acceptance_criteria>
    - Asercion de comportamiento: `npm run test:csp --workspace @nyanko/desktop` pasa.
    - Asercion de comportamiento (ROADMAP criterio 7): la app construida (`npm run build`) sirve el renderer con la CSP aplicada. Verificable: en la consola del renderer, `document.querySelector('meta[http-equiv="Content-Security-Policy"]').content` devuelve la CSP de produccion.
    - Asercion de comportamiento (H-3, LA que hay que comprobar a mano): con la CSP puesta, **las portadas de Descubrir, Temporadas, Busqueda y el avatar de la cuenta SIGUEN VIENDOSE**. Si desaparecen, falta `https:` en `img-src` y la CSP ha roto media app. El orquestador abre la app y lo mira.
    - Asercion de comportamiento: con la CSP puesta, `playbackSocket()` sigue conectando (la deteccion de reproduccion sigue viva). Sin `ws://127.0.0.1:*` no lo hace.
    - Asercion de comportamiento: con la CSP puesta, las paginas del reader (`http://127.0.0.1:<puerto>/assets/pages/...`) siguen cargando, y la consola del renderer no muestra NI UN error de CSP al recorrer un capitulo.
    - Asercion de comportamiento: los tres botones del splash (Reintentar / Abrir logs / Salir) siguen respondiendo. Verificable forzando el estado de error del splash.
    - Asercion de comportamiento: `npm run dev` sigue arrancando con HMR (la CSP de dev incluye `http://localhost:1420` y `ws://localhost:1420`).
    - Asercion de fuente: `rg "webSecurity: *false|nodeIntegration: *true|'unsafe-eval'" apps/desktop/` no encuentra NADA.
    - Comando: `npm run test:csp --workspace @nyanko/desktop`; `npm run check --workspace @nyanko/desktop`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS nivel 1. Este plan es la mitigacion, no la superficie.

## Trust Boundaries

| Boundary | Description |
|---|---|
| contenido del renderer -> red | Sin CSP, cualquier cosa que llegue a ejecutarse en el renderer puede cargar un script remoto y exfiltrar por `fetch`. Hoy NO HAY NINGUNA CSP en toda la app. |
| `ComicInfo.xml` / nombres de fichero de un CBZ -> DOM | Contenido de un torrent que acaba pintado en la interfaz. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-25 | Elevation of Privilege | Renderer sin CSP | high | mitigate | Un XSS en el renderer (via un titulo de `ComicInfo.xml`, por ejemplo) podria cargar script remoto y exfiltrar la biblioteca o el token de instancia. Mitigacion: `script-src 'self'` + `object-src 'none'` + `default-src 'self'` en produccion, y `connect-src` acotado a `'self'` + el sidecar local. Sin `connect-src` abierto no hay a donde exfiltrar. |
| T-03-26 | Denial of Service | Memoria del renderer | high | mitigate | RD-09. Mitigado por el mecanismo (plan 06) y VERIFICADO aqui con una medicion real que falla si el reader filtra bitmaps. |
| T-03-27 | Information Disclosure | `connect-src` | high | mitigate | Exfiltracion silenciosa por `fetch`/WebSocket. Mitigacion: `connect-src 'self' http://127.0.0.1:* ws://127.0.0.1:*` — solo el propio origen y el sidecar local. |
| T-03-28 | Tampering | CSP de dev con `'unsafe-inline'` | medium | mitigate | La concesion de dev podria filtrarse a produccion. Mitigacion: la CSP se emite desde `transformIndexHtml` segun el modo (nunca escrita a mano en el HTML), y `csp.test.ts` afirma que la variante de PRODUCCION no lleva `'unsafe-inline'` en `script-src`. |
| T-03-29 | Elevation of Privilege | `img-src https:` | low | accept | `https:` permite cargar imagenes de CUALQUIER origen HTTPS. Es la unica forma de que las portadas del CDN del proveedor (que llegan crudas al renderer) sigan viendose. Aceptado: una imagen no ejecuta codigo, y la alternativa (localizar TODAS las portadas por el sidecar) es un cambio de arquitectura fuera del alcance de esta fase. Registrado para la Fase 9 (auditoria de costuras). |
</threat_model>

<verification>

**Nota para el ORQUESTADOR (W3):** `package.json` NO tiene un script `test` agregado — los tests del
desktop se invocan UNO A UNO (`test:datadir`, `test:sidecar`, `test:native`, `test:prefs`,
`test:publish`). Los tres scripts nuevos de este plan (`test:reader`, `test:reader-rss`, `test:csp`)
siguen esa convencion y hay que **invocarlos explicitamente**: no los recoge ningun agregador. Este
plan NO crea un script `test` agregado — eso cambiaria como se corre el resto de la suite del desktop
y no es su trabajo.

1. `npm run test:reader --workspace @nyanko/desktop` — la ventana pura.
2. `npm run test:csp --workspace @nyanko/desktop` — las directivas.
3. `npm run check --workspace @nyanko/desktop` — tsc.
4. **El gate de RD-09** (ORQUESTADOR): `npm run build --workspace @nyanko/desktop && npm run test:reader-rss --workspace @nyanko/desktop` -> imprime el RSS medido y sale con 0.
5. **Prueba de dientes** (ORQUESTADOR, obligatoria antes de dar la fase por buena): desactivar la ventana de decodificacion a mano en `ReaderView.tsx` (montar todas las paginas), reconstruir, y comprobar que el harness sale con codigo != 0. Y subir `DECODE_AHEAD` y comprobar que `test:reader` se pone rojo. Si ninguno de los dos falla, RD-09 NO esta verificado y la fase no esta hecha.
6. **Manual** (ORQUESTADOR): abrir la app construida y comprobar que Descubrir/Temporadas/Busqueda siguen mostrando portadas (H-3) y que la consola no escupe errores de CSP.
</verification>

<success_criteria>
- RD-09: hay un NUMERO medido, no un claim. Y hay dos gates independientes que fallan si el techo se rompe.
- ROADMAP criterio 7 / Seam G: existe una CSP en la app (la primera), `webSecurity` sigue en `true`, y no ha borrado ni una portada.
</success_criteria>

<output>
Create `.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-07-SUMMARY.md` when done.
Los resultados de tests van como `unknown` / «pendiente de verificacion por el orquestador» (CODEX-RULES regla 5). El harness de RSS NO lo puedes ejecutar: dilo explicitamente.
</output>
