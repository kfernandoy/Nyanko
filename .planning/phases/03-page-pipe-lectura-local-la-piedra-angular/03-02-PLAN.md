---
phase: 03-page-pipe-lectura-local-la-piedra-angular
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/backend/nyanko_api/database.py
  - apps/backend/tests/test_source_api.py
  - apps/backend/tests/test_reader_persistence.py
autonomous: true
requirements: [RD-03, RD-05, RD-06, RD-07]

must_haves:
  truths:
    - "D-10 / RD-03: el modo de lectura se recuerda POR SERIE (`reader_prefs`, PK `(source_name, series_id)`), no por capitulo ni global. Una biblioteca tiene manga RTL y webtoon vertical a la vez."
    - "D-10 / RD-05: el progreso se recuerda POR CAPITULO (`reader_progress`, PK `(source_name, chapter_id)`). RD-05 dice «reanuda UN capitulo», no «el ultimo capitulo»: guardar una sola pagina global seria otro requisito."
    - "D-09: la clave es la identidad de la FUENTE (`source_name` + id opaco), NUNCA `media_id`. El vinculo con el tracker no existe hasta la Fase 4; keyear por `media_id` aqui obligaria a adivinar el defecto de los usuarios existentes cuando llegue."
    - "D-13 / RD-06: `reading_events` es tabla propia, calcada de `playback_events` — el patron que ya hace que proponer/confirmar/deshacer (SYN-02) salga gratis en la Fase 5. Se escribe la fila y en esta fase NO LA LEE NADIE: eso es el diseno (D-15), no un cabo suelto."
    - "D-14: `reading_events.chapter` es REAL. `playback_events.episode` es INTEGER y el 12.5 se perderia ahi — que es exactamente donde se corrompe el tracker (FND-04). Por eso NO se reutiliza `playback_events`."
    - "D-07/RD-07: `reader_prefs` guarda `double_page` y `double_page_offset` (el offset es MANUAL y ajustable, no adivinado)."
    - "D-11: el zoom y el paneo NO se persisten. No hay columna para ellos y no debe haberla."
    - "Migracion aditiva v8 -> v9: tres `CREATE TABLE IF NOT EXISTS` dentro de la constante `SCHEMA` + `CANONICAL_SCHEMA_VERSION = 9`. CERO codigo de migracion nuevo — la escalera existente (`database.py:298-355`) ya dispara `_backup_before_migration()` al detectar la version nueva, que es el unico rollback que existe."
    - "H-2: el esquema v9 pone rojo `test_phase_2_does_not_add_source_persistence_columns` (`test_source_api.py:165-184`) POR CONSTRUCCION — afirma `\"source_name\" not in database_module.SCHEMA` y las tres tablas nuevas lo llevan. Este plan es el dueno de reescribirlo: la mitad que sigue teniendo sentido (las columnas de identidad de fuente son `source_name`/`source_id`, y NINGUNA lleva `url`, `path` ni `_local`) se conserva; la mitad que afirmaba que la Fase 2 no persistia nada se retira, porque la Fase 3 SI persiste."
    - "FND-05 (decision de la Fase 1, no opcional): `assert_no_persisted_urls(connection)` se llama DESPUES de escrituras REALES en `reader_prefs`, `reader_progress` y `reading_events`. Sobre una tabla vacia la guardia es verde SIN HABER MIRADO NADA — este proyecto ya perdio TODAS las portadas de la biblioteca por persistir un puerto efimero dentro de una URL, y el reader es una superficie diez veces mayor."
  prohibitions:
    - "Ninguna columna nueva guarda una URL, un host, un puerto ni una ruta del sistema de ficheros. Los ids son opacos (`{root_key}:{ruta relativa}`), que es lo que hace legal a D-03."
    - "No se reutiliza `playback_events` para lectura (D-14). No se anade una columna `chapter REAL` a `playback_events`: SQLite no tiene `ALTER COLUMN TYPE` y mezclar los dos flujos es como se corrompe el progreso del tracker."
    - "No se usa la tabla `settings` (key/value) para datos de dominio: existe, pero no es para esto (D-10)."
    - "No se escriben `update_reading_event` ni `undo`: no tienen consumidor hasta la Fase 5 y escribirlos aqui es codigo sin test de comportamiento posible (D-15)."
    - "No se toca `conftest.py`, `pyproject.toml` ni `pytest.ini` (CODEX-RULES regla 3)."
---

<objective>
Dar al reader su memoria: que modo usa cada serie, por que pagina iba cada capitulo, y la fila que
dice «este capitulo se termino» — la que la Fase 5 va a encontrar ya escrita cuando llegue a
construir el sync.

Purpose: RD-03 y RD-05 son «se recuerda», y sin esquema no hay nada que recordar. RD-06 nace aqui
porque el ROADMAP pone esta fase ANTES que el sync a proposito: el evento existe antes que su
consumidor, no despues.
Output: schema v9 (tres tablas aditivas), su CRUD minimo, el test de la Fase 2 reescrito a proposito
(H-2), y la guardia FND-05 llamada contra escrituras REALES.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.planning/CODEX-RULES.md
</execution_context>

<context>
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-CONTEXT.md
@.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces (cuota de este plan)

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `reader_prefs` | `database.py` (`SCHEMA`) | Tabla. PK `(source_name, series_id)`. Columnas: `mode TEXT NOT NULL DEFAULT 'rtl'`, `fit TEXT`, `double_page INTEGER NOT NULL DEFAULT 0`, `double_page_offset INTEGER NOT NULL DEFAULT 0`, `updated_at TEXT` |
| `reader_progress` | `database.py` (`SCHEMA`) | Tabla. PK `(source_name, chapter_id)`. Columnas: `page INTEGER NOT NULL DEFAULT 1`, `updated_at TEXT` |
| `reading_events` | `database.py` (`SCHEMA`) | Tabla. `id`, `detected_at`, `source_name`, `series_id`, `chapter_id`, `chapter REAL`, `status TEXT DEFAULT 'pending'`, `media_id INTEGER` (NULL hasta Fase 4), `progress_before REAL`, `progress_after REAL` |
| `CANONICAL_SCHEMA_VERSION = 9` | `database.py:275` | Bump de 8 a 9. Dispara `_backup_before_migration()` en BDs existentes |
| `Database.get_reader_prefs` | `database.py` | `(source_name, series_id) -> dict \| None` |
| `Database.set_reader_prefs` | `database.py` | Upsert `ON CONFLICT(source_name, series_id) DO UPDATE` |
| `Database.get_reader_progress` | `database.py` | `(source_name, chapter_id) -> dict \| None` |
| `Database.set_reader_progress` | `database.py` | Upsert `ON CONFLICT(source_name, chapter_id) DO UPDATE` |
| `Database.insert_reading_event` | `database.py` | Gemelo de `insert_playback_event`, con `chapter: float \| None` en vez de `episode: int \| None` |
| `Database.get_recent_reading_events` | `database.py` | Lectura minima para poder testear la escritura. Nadie mas la usa en esta fase (D-15) |
| `apps/backend/tests/test_reader_persistence.py` | nuevo | Round-trip de las tres tablas + `assert_no_persisted_urls` DESPUES de escrituras reales |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Esquema v9 — tres tablas aditivas y el bump que dispara el backup</name>

  <read_first>
    - `apps/backend/nyanko_api/database.py:15-35` — la constante `SCHEMA` y `playback_events`, la tabla que `reading_events` calca. Fijate en `episode INTEGER`: es la razon EXACTA por la que no se reutiliza (D-14).
    - `apps/backend/nyanko_api/database.py:258-275` — el final de `SCHEMA` y `CANONICAL_SCHEMA_VERSION = 8`.
    - `apps/backend/nyanko_api/database.py:298-355` — `initialize()`: `_requires_canonical_migration()` -> `_backup_before_migration()` -> `executescript(SCHEMA)` -> `_add_column(...)` -> `INSERT OR IGNORE INTO schema_migrations`. NO es una escalera numerada: es `CREATE TABLE IF NOT EXISTS` idempotente. Tres tablas nuevas = tres `CREATE TABLE IF NOT EXISTS` y nada mas.
    - `apps/backend/nyanko_api/database.py:344-346` — el comentario de la v8 (`chapter_progress REAL`). Es el MODELO de comentario para la v9: explica la trampa y el bug real, en espanol.
    - `.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-CONTEXT.md` — D-09, D-10, D-11, D-13, D-14.
  </read_first>

  <action>
    En la constante `SCHEMA` de `database.py`, anadir tres `CREATE TABLE IF NOT EXISTS` (y NADA de
    codigo de migracion nuevo: la escalera existente ya las crea de forma idempotente):

    `reader_prefs`: PK compuesta `(source_name, series_id)`, ambas `TEXT NOT NULL`. Columnas
    `mode TEXT NOT NULL DEFAULT 'rtl'` (D-12: RTL es el defecto — abrir un manga L->R esta roto de
    nacimiento), `fit TEXT`, `double_page INTEGER NOT NULL DEFAULT 0`,
    `double_page_offset INTEGER NOT NULL DEFAULT 0`, `updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`.

    `reader_progress`: PK compuesta `(source_name, chapter_id)`, ambas `TEXT NOT NULL`. Columnas
    `page INTEGER NOT NULL DEFAULT 1`, `updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`.

    `reading_events`: `id INTEGER PRIMARY KEY AUTOINCREMENT`,
    `detected_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP`, `source_name TEXT NOT NULL`,
    `series_id TEXT`, `chapter_id TEXT`, `chapter REAL`, `status TEXT NOT NULL DEFAULT 'pending'`,
    `media_id INTEGER`, `progress_before REAL`, `progress_after REAL`.

    Comentario obligatorio (en espanol, estilo `database.py:344-346` — explica el POR QUE, no el QUE)
    sobre `reading_events.chapter`: es REAL y no INTEGER porque el capitulo 12.5 existe;
    `playback_events.episode` es INTEGER y por eso esta tabla no se reutiliza. Y sobre `media_id`: es
    NULL hasta la Fase 4, que es donde nace el vinculo; keyear el progreso por `media_id` hoy
    obligaria a adivinar el defecto de los usuarios existentes.

    Subir `CANONICAL_SCHEMA_VERSION` de `8` a `9`. Eso, y solo eso, dispara `_backup_before_migration()`
    contra las BDs existentes.
  </action>

  <acceptance_criteria>
    - Asercion de fuente: `apps/backend/nyanko_api/database.py` contiene `CANONICAL_SCHEMA_VERSION = 9` y no contiene `CANONICAL_SCHEMA_VERSION = 8`.
    - Asercion de fuente: `SCHEMA` contiene `CREATE TABLE IF NOT EXISTS reader_prefs`, `CREATE TABLE IF NOT EXISTS reader_progress` y `CREATE TABLE IF NOT EXISTS reading_events`.
    - Asercion de comportamiento: sobre una BD nueva, `PRAGMA table_info(reading_events)` declara `chapter` con tipo `REAL` (no `INTEGER`), y `progress_before`/`progress_after` tambien `REAL`.
    - Asercion de comportamiento: `PRAGMA table_info(reader_prefs)` NO tiene ninguna columna de zoom ni de paneo (D-11), y `mode` tiene `dflt_value` `'rtl'` (D-12).
    - Asercion de comportamiento: las PK compuestas son las declaradas — `reader_prefs` por `(source_name, series_id)` y `reader_progress` por `(source_name, chapter_id)`. Insertar dos filas con la misma PK actualiza, no duplica.
    - Asercion de comportamiento: `Database.initialize()` sobre una BD v8 preexistente crea las tres tablas, deja `integrity_check` en `ok`, conserva los recuentos de las tablas viejas y ESCRIBE el backup previo (`_backup_before_migration` disparado por el bump).
    - Asercion negativa: `playback_events` NO gana ninguna columna nueva. `git diff` sobre `SCHEMA` solo anade; no modifica ni una linea de las tablas existentes.
    - Comando: `cd apps/backend; python -m pytest tests/test_database.py tests/test_reader_persistence.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CRUD del reader — dos upserts y un insert de evento</name>

  <read_first>
    - `apps/backend/nyanko_api/database.py:2307-2334` — `insert_playback_event`. `insert_reading_event` es ESTA funcion con `chapter: float | None` en vez de `episode: int | None`.
    - `apps/backend/nyanko_api/database.py:612-618` — `set_setting`: el patron de upsert (`ON CONFLICT(key) DO UPDATE SET value = excluded.value`). `set_reader_prefs`/`set_reader_progress` lo copian con PK compuesta.
    - `apps/backend/nyanko_api/database.py:606-611` — `get_setting`: el patron de lectura (`connection.execute(...).fetchone()`, `row["col"] if row else None`).
    - `apps/backend/nyanko_api/progress.py` — `effective_chapter` / `to_provider`: el modelo de progreso de la Fase 1. `floor()` solo al ENVIAR al proveedor; en local el capitulo es REAL.
  </read_first>

  <action>
    Anadir a la clase `Database` cinco metodos, todos con el mismo estilo del fichero
    (`with self.connect() as connection`, SQL literal, sin ORM):

    `get_reader_prefs(source_name: str, series_id: str) -> dict | None` — devuelve `mode`, `fit`,
    `double_page`, `double_page_offset`; `None` si no hay fila. El defecto (`'rtl'`) lo pone el
    esquema, no un `or` en Python: si el defecto vive en dos sitios, un dia discrepan.

    `set_reader_prefs(source_name, series_id, *, mode=None, fit=None, double_page=None,
    double_page_offset=None) -> None` — upsert con `ON CONFLICT(source_name, series_id) DO UPDATE`.
    Solo pisa las columnas que llegan no-`None` (`COALESCE(excluded.col, reader_prefs.col)` o el
    equivalente que salga limpio): cambiar el modo no debe borrar el offset de doble pagina.
    Actualiza `updated_at`.

    `get_reader_progress(source_name: str, chapter_id: str) -> dict | None` — devuelve `page` y
    `updated_at`; `None` si el capitulo nunca se abrio.

    `set_reader_progress(source_name: str, chapter_id: str, page: int) -> None` — upsert con
    `ON CONFLICT(source_name, chapter_id) DO UPDATE SET page = excluded.page, updated_at = ...`.

    `insert_reading_event(source_name: str, series_id: str | None, chapter_id: str | None,
    chapter: float | None, status: str = "pending", media_id: int | None = None,
    progress_before: float | None = None, progress_after: float | None = None) -> int` — devuelve
    `int(cursor.lastrowid)`, igual que `insert_playback_event`.

    `get_recent_reading_events(limit: int = 20) -> list[dict]` — `ORDER BY id DESC LIMIT ?`. Existe
    para que la escritura sea testeable; en esta fase no la consume nadie mas (D-15). No escribir
    `update_reading_event` ni `undo`: los necesita la Fase 5 y alli se escriben, con su consumidor
    delante.
  </action>

  <acceptance_criteria>
    - Asercion de comportamiento: `set_reader_prefs("local_archive", "0:Berserk", mode="vertical")` seguido de `get_reader_prefs` devuelve `mode == "vertical"`; una serie NUNCA escrita devuelve `None` (y el defecto `'rtl'` lo aplica el esquema al primer INSERT).
    - Asercion de comportamiento (RD-03): dos series distintas guardan modos distintos a la vez (`"0:Berserk"` -> `rtl`, `"0:SoloLeveling"` -> `vertical`) y ninguna pisa a la otra. Este es el requisito literal: una biblioteca tiene manga y webtoon a la vez.
    - Asercion de comportamiento (RD-07): `set_reader_prefs(..., double_page=1, double_page_offset=1)` y luego `set_reader_prefs(..., mode="ltr")` deja `double_page_offset == 1` intacto — cambiar el modo no borra el offset.
    - Asercion de comportamiento (RD-05): `set_reader_progress("local_archive", "0:Berserk/Cap 12", 47)` y `get_reader_progress` devuelve `page == 47`. Un capitulo nunca abierto devuelve `None`. Dos capitulos distintos guardan paginas distintas (el progreso es POR CAPITULO, no global).
    - Asercion de comportamiento (RD-06/D-14): `insert_reading_event(..., chapter=12.5, ...)` y `get_recent_reading_events()` devuelve `chapter == 12.5` — el decimal SOBREVIVE al round-trip. Este es el test que falla si alguien cambia la columna a INTEGER.
    - Asercion de comportamiento: `insert_reading_event` con `media_id=None` funciona (el vinculo no existe hasta la Fase 4).
    - Asercion negativa: `rg "def update_reading_event|def undo_reading" apps/backend/nyanko_api/database.py` no encuentra nada — no se escribe codigo sin consumidor (D-15).
    - Comando: `cd apps/backend; python -m pytest tests/test_reader_persistence.py -q -x`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reescribir el test H-2 y llamar a la guardia FND-05 contra escrituras REALES</name>

  <read_first>
    - `apps/backend/tests/test_source_api.py:165-186` — `test_phase_2_does_not_add_source_persistence_columns` ENTERO, y `_columns()` (`:188+`). Afirma `"source_name" not in database_module.SCHEMA`: el v9 lo pone rojo por construccion. Fijate en las lineas 179-184: esa mitad SIGUE teniendo sentido y se conserva.
    - `apps/backend/tests/test_persisted_urls.py:1-40` — el docstring del modulo. Dice literalmente lo que hay que hacer: *«Llama a `assert_no_persisted_urls(connection)` al final de CUALQUIER test que persista una URL de pagina... sobre una tabla vacia, un `SELECT ... LIKE 'http%'` no encuentra nada y el test es verde SIN HABER MIRADO NADA»*.
    - `apps/backend/tests/test_persisted_urls.py:48-60` — `REMOTE_URL_ALLOWLIST` y por que existe.
    - `apps/backend/tests/test_persisted_urls.py:156` — la firma de `assert_no_persisted_urls(connection)`.
    - `apps/backend/tests/test_source_api.py:25` — precedente de import cruzado entre ficheros de test (`from tests.test_persisted_urls import assert_no_persisted_urls`).
  </read_first>

  <action>
    (a) Reescribir `test_phase_2_does_not_add_source_persistence_columns` en `tests/test_source_api.py`.
    Renombrarlo a `test_source_identity_columns_never_carry_urls_or_paths`. Se RETIRA la mitad que
    afirmaba que el esquema no tiene columnas de identidad de fuente (la Fase 3 SI persiste identidad
    de fuente: es su diseno, D-09/D-10). Se CONSERVA — y se refuerza — la mitad que importa:
      - las columnas de identidad de fuente son exactamente `source_name` y `source_id`, y ninguna de
        las dos contiene `url`, `path` ni `_local` en su nombre;
      - `assert_no_persisted_urls(connection)` sobre la BD inicializada;
      - `database_module.SCHEMA` no declara NINGUNA columna cuyo nombre contenga `url` o `path` dentro
        de las tres tablas del reader (`reader_prefs`, `reader_progress`, `reading_events`).
    Comentario en espanol arriba del test explicando el cambio: el test viejo se llamaba
    `test_phase_2_...` y existia para afirmar que la Fase 2 no persistia nada; la Fase 3 es la que
    persiste, y este test es su version util — lo que se prohibe no es persistir identidad de fuente,
    es persistir una URL o una ruta.

    (b) Crear `apps/backend/tests/test_reader_persistence.py`. Cubre el Task 1 y el Task 2 (round-trip
    de las tres tablas, decimal 12.5, aislamiento por serie y por capitulo, migracion v8 -> v9), y
    cierra CADA test que escribe una fila con `assert_no_persisted_urls(connection)` DESPUES de las
    escrituras. Usa `tmp_path` de pytest para la BD (stdlib, CODEX-RULES regla 3: no inventes
    directorios de trabajo).

    El test que de verdad tiene dientes, y que hay que escribir explicitamente: escribir en
    `reader_progress` / `reading_events` un id de capitulo LEGITIMO (opaco: `0:Berserk/Cap 12`),
    llamar a la guardia, y comprobar que pasa; y ADEMAS un test negativo que escriba a proposito una
    URL absoluta con puerto en una de las columnas nuevas y compruebe que `assert_no_persisted_urls`
    LANZA. Sin ese test negativo, la guardia podria estar mirando las tablas equivocadas y nadie se
    enteraria — que es exactamente como este proyecto perdio todas las portadas de la biblioteca.

    NO tocar `conftest.py`, `pyproject.toml` ni `pytest.ini`. NO ejecutar la suite (CODEX-RULES regla 2).
  </action>

  <acceptance_criteria>
    - Asercion de fuente: `rg "test_phase_2_does_not_add_source_persistence_columns" apps/backend/tests/` no encuentra nada — el test viejo esta reescrito, no comentado ni skippeado.
    - Asercion de fuente: `apps/backend/tests/test_source_api.py` define `test_source_identity_columns_never_carry_urls_or_paths` y sigue llamando a `assert_no_persisted_urls`.
    - Asercion de fuente: `rg "assert_no_persisted_urls" apps/backend/tests/test_reader_persistence.py` aparece en TODOS los tests que insertan filas (>= 3 llamadas).
    - Asercion de comportamiento (la que tiene dientes): un test escribe `'http://127.0.0.1:49876/assets/pages/x.jpg'` en una columna de `reader_progress` o `reading_events` y `assert_no_persisted_urls(connection)` LANZA. Si no lanza, la guardia no esta mirando las tablas nuevas y el plan no esta hecho.
    - Asercion de comportamiento: los tests de round-trip escriben filas reales en las tres tablas y DESPUES llaman a la guardia, que pasa. La guardia no se llama sobre tablas vacias (seria verde sin haber mirado nada).
    - Comando (lo ejecuta el ORQUESTADOR): `cd apps/backend; python -m pytest tests/ -q --ignore=tests/test_packaged_sources.py` — la suite entera verde. El esquema v9 no puede dejar ningun test rojo detras.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS nivel 1. Bloquear cualquier amenaza `high`.

## Trust Boundaries

| Boundary | Description |
|---|---|
| API del reader -> `database.py` | `source_name`, `series_id`, `chapter_id` y `page` llegan del renderer. Son datos, no rutas — pero se PERSISTEN, y lo que se persiste mal no se cura solo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-06 | Tampering | `reader_progress` / `reading_events` | critical | mitigate | Persistir una URL absoluta con el puerto efimero dentro. YA OCURRIO en este proyecto (`cover_image_local`): la biblioteca se quedo sin una sola portada, de forma permanente y silenciosa. Mitigacion: ninguna columna nueva admite URL; `assert_no_persisted_urls` se llama DESPUES de escrituras reales, con un test negativo que demuestra que la guardia tiene dientes sobre las tablas nuevas. |
| T-03-07 | Tampering | Migracion v8 -> v9 | high | mitigate | Una migracion que corrompe la BD real (2.761 `library_entries`, 25.727 `episodes`) no tiene rollback. Mitigacion: la migracion es ADITIVA (`CREATE TABLE IF NOT EXISTS`, cero `ALTER`/rebuild) y el bump de `CANONICAL_SCHEMA_VERSION` dispara `_backup_before_migration()`, que es el unico rollback que existe. |
| T-03-08 | Information Disclosure | `reader_progress.chapter_id` | medium | mitigate | El id opaco es `{root_key}:{ruta relativa}` — lleva la ruta RELATIVA del fichero dentro de la biblioteca del usuario. No lleva la raiz ni una ruta absoluta, y no sale del disco del usuario. Mitigacion: el test afirma que ningun id persistido es una ruta absoluta ni contiene la raiz de la biblioteca. |
| T-03-09 | Repudiation | `reading_events` | low | accept | Sin consumidor en esta fase (D-15): nadie puede actuar sobre un evento falsificado todavia. La Fase 5 (proponer/confirmar/deshacer) es la que debe validar antes de escribir al proveedor. Aceptado y registrado aqui para que la Fase 5 lo encuentre. |
</threat_model>

<verification>
1. `cd apps/backend; python -m pytest tests/test_reader_persistence.py tests/test_source_api.py tests/test_persisted_urls.py -q -x`
2. `cd apps/backend; python -m pytest tests/ -q --ignore=tests/test_packaged_sources.py` — suite entera.
3. Migracion contra copia de BD real: inicializar `Database` sobre una copia de la BD de produccion, comprobar `PRAGMA integrity_check` -> `ok`, mismos recuentos por tabla, y que el fichero de backup previo existe.
4. `rg "url|path" ` sobre las tres tablas nuevas dentro de `SCHEMA` no encuentra ningun nombre de columna.
</verification>

<success_criteria>
- RD-03: el modo se recuerda por serie y sobrevive a reiniciar (esta persistido en SQLite, no en `localStorage`).
- RD-05: el progreso se recuerda por capitulo.
- RD-06: la fila del evento «capitulo terminado» existe, con `chapter` REAL.
- RD-07: `double_page` y `double_page_offset` persisten y son independientes del modo.
- H-2 cerrado: el test de la Fase 2 esta reescrito a proposito, no roto ni skippeado.
- FND-05 sigue en verde y AHORA MIRA de verdad: hay un test negativo que lo demuestra.
</success_criteria>

<output>
Create `.planning/phases/03-page-pipe-lectura-local-la-piedra-angular/03-02-SUMMARY.md` when done.
Los resultados de tests van como `unknown` / «pendiente de verificacion por el orquestador» (CODEX-RULES regla 5).
</output>
</content>
