---
phase: 04-identidad-y-v-nculo-fuente-entrada-del-tracker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/nyanko_api/database.py
  - apps/backend/nyanko_api/linking.py
  - apps/backend/tests/test_database.py
  - apps/backend/tests/test_linking.py
autonomous: true
requirements: [LNK-01, LNK-04]

must_haves:
  truths:
    - "Criterio 2 del ROADMAP: el vinculo se ALMACENA, y se almacena en `media_mappings` — la tabla que este problema ya tiene construida (el ROADMAP la nombra como precedente: «la tabla que este problema necesita, con columna de offset y todo, construida para la extension de navegador»). PK `(provider, site_identifier)`; para manga `provider` = el `source_name` de la fuente (`local_archive`) y `site_identifier` = el `series_id` opaco de la Fase 3."
    - "Criterio 2: `chapter_offset INTEGER NOT NULL DEFAULT 0` es un MIRROR literal de `episode_offset` (`database.py:45`), que ya esta enviado y funcionando desde la extension de navegador. Misma tabla, misma clave, misma semantica: el numero que va de la numeracion de la fuente a la numeracion del tracker."
    - "Criterio 2: «nunca se calcula en el momento del sync». El offset se guarda como DATO en la fila. `SeriesLink.absolute_chapter(chapter)` es aritmetica sobre lo guardado, no una inferencia: capitulo 12 con `chapter_offset` 100 da 112, siempre, sin mirar nada mas."
    - "Criterio 1 / LNK-01: en el namespace de manga, «existe fila en `media_mappings`» ES «el usuario lo confirmo». El invariante se sostiene porque solo hay UN escritor de filas de manga (el endpoint de confirmar, plan 04-03) y las propuestas NO se persisten jamas. Sin ese invariante, `require_link` estaria leyendo basura del matcher difuso y creyendo que es consentimiento."
    - "Criterio 4 / LNK-04: `require_link(database, source_name, series_id)` lanza `UnlinkedSeriesError` cuando no hay fila. Es la UNICA puerta: la Fase 5 la cruza antes de `enqueue_mutation` o construye el bug que esta fase existe para impedir."
    - "Criterio 4: `UnlinkedSeriesError` lleva un mensaje para el usuario («se lo dice al usuario»), no un `assert` mudo. El plan 04-03 lo traduce a HTTP; aqui nace con texto."
    - "El bump `CANONICAL_SCHEMA_VERSION = 10 -> 11` es lo que dispara `_backup_before_migration()` (`database.py:337-338`), que es el unico rollback que existe. La migracion completa es UNA linea de `_add_column`: SQLite rellena las filas vivas con el DEFAULT 0, y 0 es exactamente el offset correcto para todo mapping preexistente."
  artifacts:
    - "apps/backend/nyanko_api/linking.py — `SeriesLink`, `UnlinkedSeriesError`, `resolve_link`, `require_link`"
    - "apps/backend/tests/test_linking.py — el gate de LNK-04, incluida la propuesta fuerte que NO vale como confirmacion"
    - "media_mappings.chapter_offset — columna aditiva, schema v11"
  key_links:
    - "`database.py:2418-2441` — `get_media_mapping`/`set_media_mapping`. Son las funciones que ya existen y que este plan extiende con `chapter_offset`. NO se crea un par gemelo para manga."
    - "`database.py:337-338` -> `_backup_before_migration()`. El bump de version es lo que lo arma; sin bump, una BD viva se queda sin backup y sin columna."
  prohibitions:
    - statement: "PROHIBICION CENTRAL DE LA FASE (Criterio 4). Una propuesta del matcher, por alto que sea su score, NO es un vinculo. `require_link` no consulta a `matcher.py`, no acepta un `score`, y no tiene ningun umbral por encima del cual se de por vinculado. Un intento de sync sobre una serie sin fila confirmada falla cerrado: se lo dice al usuario, NO escribe y NO encola."
      status: pending
      verification: "Asercion de comportamiento (`tests/test_linking.py`): con `media_mappings` vacia para `('local_archive', '0:Berserk')`, `require_link(database, 'local_archive', '0:Berserk')` lanza `UnlinkedSeriesError`, y lo sigue lanzando si en la misma BD existe una fila de `match_corrections` con `raw_pattern='berserk'` y una serie del catalogo que el matcher puntuaria a 0.94. Asercion de fuente: `linking.py` no importa `matcher`, ni `find_best_match`, ni `rank_matches`. Asercion de comportamiento: tras el `UnlinkedSeriesError`, `SELECT COUNT(*) FROM pending_mutations` sigue siendo 0 y `SELECT COUNT(*) FROM media_mappings` no ha cambiado."
    - statement: "`resolve_link` y `require_link` no ESCRIBEN nada. Leer si hay vinculo no puede, jamas, crear el vinculo."
      status: pending
      verification: "Asercion de comportamiento: 100 llamadas a `resolve_link` sobre una serie sin vinculo dejan `SELECT COUNT(*) FROM media_mappings` en 0. Asercion de fuente: `linking.py` no contiene `set_media_mapping`, `INSERT` ni `UPDATE`."
    - statement: "No se anade una tabla nueva de vinculos de manga. `media_mappings` es la tabla; duplicarla es tener dos verdades sobre la misma pregunta."
      status: pending
      verification: "Asercion de fuente: `SCHEMA` en `database.py` no gana ningun `CREATE TABLE`. El diff de `SCHEMA` es cero: `chapter_offset` entra por `_add_column`, como el `kind` de la v10 (`database.py:384-388`)."
    - statement: "No se toca `conftest.py`, `pyproject.toml` ni `pytest.ini` (CODEX-RULES regla 3)."
      status: pending
      verification: "`git status` no lista esos ficheros."
---

<objective>
Darle al vinculo un sitio donde vivir y una puerta por la que se pregunte por el — la puerta que la
Fase 5 tiene que cruzar antes de escribir en el AniList real de nadie.

Purpose: LNK-01 (el vinculo es explicito y ALMACENADO) y LNK-04 (el sync falla cerrado sin el). El
ROADMAP ya nombro la tabla: `media_mappings` existe, tiene `episode_offset` y esta enviada. Este plan
no inventa un modelo de datos: le anade la columna gemela (`chapter_offset`) y escribe las 30 lineas
que convierten «hay fila» en una pregunta que se puede hacer y que sabe decir que no.
Output: schema v11 (una columna aditiva), `linking.py`, y el test que demuestra que una propuesta a
0.94 no vale como consentimiento.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.planning/CODEX-RULES.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces (cuota de este plan)

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `media_mappings.chapter_offset` | `database.py` (`_add_column` en `initialize`) | Columna. `INTEGER NOT NULL DEFAULT 0`. Mirror de `episode_offset` (`database.py:45`) |
| `CANONICAL_SCHEMA_VERSION = 11` | `database.py:312` | Bump de 10 a 11. Arma `_backup_before_migration()` |
| `Database.get_media_mapping` | `database.py:2418-2429` | MODIFICADO: devuelve tambien `chapter_offset` |
| `Database.get_media_mapping_full` | `database.py` | NUEVO. `(provider, site_identifier) -> dict \| None` con las tres columnas. La firma vieja se queda intacta para el anime |
| `Database.set_media_mapping` | `database.py:2431-2441` | MODIFICADO: acepta `chapter_offset: int = 0` |
| `Database.delete_media_mapping` | `database.py` | NUEVO. Gemelo de `delete_match_correction` (`database.py:2457-2459`). Lo consume `DELETE /api/manga/link` (plan 04-03) |
| `apps/backend/nyanko_api/linking.py` | nuevo | El modulo del vinculo |
| `SeriesLink` | `linking.py` | Dataclass frozen: `source_name`, `series_id`, `media_id`, `chapter_offset`. Metodo `absolute_chapter(chapter: float) -> float` |
| `UnlinkedSeriesError` | `linking.py` | Excepcion con mensaje para el usuario |
| `resolve_link` | `linking.py` | `(database, source_name, series_id) -> SeriesLink \| None`. Solo lee |
| `require_link` | `linking.py` | `(database, source_name, series_id) -> SeriesLink`. Lanza `UnlinkedSeriesError`. LA puerta |
| `apps/backend/tests/test_linking.py` | nuevo | El gate de LNK-04 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema v11 — la columna gemela de `episode_offset`</name>
  <files>apps/backend/nyanko_api/database.py, apps/backend/tests/test_database.py</files>

  <read_first>
    - `apps/backend/nyanko_api/database.py:41-48` — `media_mappings` en la constante `SCHEMA`. PK `(provider, site_identifier)`, y `episode_offset INTEGER NOT NULL DEFAULT 0` en la linea 45. Esa linea es el modelo literal de lo que hay que anadir.
    - `apps/backend/nyanko_api/database.py:384-388` — el `kind` de la v10 y su comentario. Es el PRECEDENTE EXACTO de esta migracion: `_add_column` con DEFAULT es la migracion completa, porque SQLite rellena las filas vivas con el default. El comentario dice por que; copia ese estilo.
    - `apps/backend/nyanko_api/database.py:312` y `:335-396` — `CANONICAL_SCHEMA_VERSION = 10` y `initialize()`. Fijate en el orden: `_requires_canonical_migration()` -> `_backup_before_migration()` -> `executescript(SCHEMA)` -> la escalera de `_add_column`.
    - `apps/backend/nyanko_api/database.py:2418-2441` — `get_media_mapping` y `set_media_mapping`. Las dos funciones a extender. Mira el `ON CONFLICT ... DO UPDATE SET` de `set_media_mapping`: es donde esta la trampa de la Task.
    - `apps/backend/nyanko_api/main.py:3689-3690` y `:3947-3950` — los DOS llamadores vivos de `set_media_mapping`, ambos de anime. Ninguno pasa `chapter_offset`. Este cambio no puede romperlos.
    - `apps/backend/tests/test_database.py:81`, `:1039-1042`, `:1089`, `:1095`, `:1167`, `:1173` — las SEIS aserciones que el bump de version pone rojas por construccion. Este plan las posee.
  </read_first>

  <action>
    En `database.py`:

    1. Anadir `self._add_column(connection, "media_mappings", "chapter_offset", "INTEGER NOT NULL DEFAULT 0")`
       en la escalera de `initialize()`, junto al bloque de la v10. La constante `SCHEMA` NO se toca:
       `media_mappings` ya existe ahi y `_add_column` es como esta casa hace las columnas aditivas
       (precedente `database.py:384-388`). Comentario obligatorio en espanol, estilo de esa misma
       linea: la migracion es esta linea y nada mas — DEFAULT 0 es el offset correcto para todo
       mapping preexistente (los de la extension de navegador son de anime y su capitulo no existe),
       asi que no hay backfill que escribir.

    2. Subir `CANONICAL_SCHEMA_VERSION` de `10` a `11`. Eso es lo que arma `_backup_before_migration()`.

    3. `get_media_mapping`: anadir `chapter_offset` al `SELECT` y devolverlo. OJO al tipo de retorno:
       hoy es `tuple[int, int] | None` y los llamadores desempaquetan dos valores
       (`main.py:3553`: `mapped_id, episode_offset = mapping`). Cambiarlo a una tupla de tres ROMPE
       ese desempaquetado en silencio si se olvida un sitio. Devolver un `dict` con claves
       `media_id`, `episode_offset` y `chapter_offset` y actualizar el unico llamador vivo
       (`main.py:3552-3553`) NO es opcion aqui: `main.py` no esta en `files_modified` de este plan
       (CODEX-RULES regla 6). Por tanto: mantener la firma `tuple[int, int] | None` intacta y anadir
       un metodo NUEVO `get_media_mapping_full(provider, site_identifier) -> dict | None` que
       devuelva las tres columnas. `linking.py` usa el nuevo; el anime se queda con el viejo, sin
       tocar ni una linea de su camino. Si crees que hace falta tocar `main.py`, PARA y dilo.

    4. `delete_media_mapping(provider, site_identifier) -> None`: gemelo literal de
       `delete_match_correction` (`database.py:2457-2459`), un `DELETE ... WHERE provider = ? AND
       site_identifier = ?`. Lo consume `DELETE /api/manga/link` (plan 04-03), que sin esto se
       quedaria sin forma de desvincular o — peor — escribiria SQL crudo desde `main.py`. Es la unica
       operacion del CRUD del vinculo que la tabla no tenia ya: `media_mappings` nacio para la
       extension de navegador, que nunca desvincula.

    5. `set_media_mapping`: anadir el parametro `chapter_offset: int = 0` al final (despues de
       `episode_offset`), incluirlo en el `INSERT` y en el `ON CONFLICT ... DO UPDATE SET`.
       TRAMPA MEDIDA, y va con comentario: el `DO UPDATE` pisa la columna con lo que traiga el
       `excluded`, asi que un llamador de anime que no pase `chapter_offset` pondria un 0 encima del
       offset de manga de la MISMA clave `(provider, site_identifier)`. Hoy no colisiona porque el
       namespace de `provider` es disjunto (anime usa el nombre del adapter del sitio; manga usa
       `local_archive`), pero eso es una propiedad de los datos, no del codigo. Escribir el
       comentario ES la mitigacion barata; una tabla nueva no.

    En `tests/test_database.py`: actualizar las seis aserciones que el bump pone rojas
    (`:81`, `:1042` y el nombre `test_canonical_schema_version_is_10`, `:1089`, `:1095`, `:1167`,
    `:1173` — las dos ultimas incluyen el glob `nyanko.backup-v10-*.sqlite3`, que pasa a `v11` porque
    `_backup_before_migration` nombra el fichero CON la version). Anadir ademas un test de la
    migracion real: una BD v10 con una fila viva en `media_mappings` sobrevive a `initialize()` con
    `chapter_offset` = 0, `integrity_check` en `ok` y los recuentos por tabla iguales.
  </action>

  <verify>
    <automated>cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_database.py tests/test_persisted_urls.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Asercion de fuente: `database.py` contiene `CANONICAL_SCHEMA_VERSION = 11` y no contiene `CANONICAL_SCHEMA_VERSION = 10`.
    - Asercion de fuente: la constante `SCHEMA` no gana ningun `CREATE TABLE` ni ninguna columna. `git diff` sobre `SCHEMA` es vacio.
    - Asercion de comportamiento: sobre una BD nueva, `PRAGMA table_info(media_mappings)` declara `chapter_offset` con tipo `INTEGER`, `notnull` = 1 y `dflt_value` = `0`.
    - Asercion de comportamiento: `Database.initialize()` sobre una BD v10 con una fila preexistente en `media_mappings` deja esa fila con `chapter_offset` = 0, `integrity_check` en `ok`, los mismos recuentos por tabla, y ESCRIBE `nyanko.backup-v11-*.sqlite3`.
    - Asercion de comportamiento: `set_media_mapping('local_archive', '0:Berserk', 42, chapter_offset=100)` seguido de `get_media_mapping_full('local_archive', '0:Berserk')` devuelve `media_id` 42 y `chapter_offset` 100.
    - Asercion de comportamiento (no-regresion del anime): `get_media_mapping` sigue devolviendo una tupla de DOS elementos y `mapped_id, episode_offset = mapping` sigue funcionando. `set_media_mapping('crunchyroll', 'abc', 7, 3)` sin `chapter_offset` sigue funcionando.
    - Asercion de comportamiento: `delete_media_mapping('local_archive', '0:Berserk')` deja `get_media_mapping_full('local_archive', '0:Berserk')` en `None`, y llamarlo sobre una clave inexistente no lanza (idempotente).
    - Asercion de fuente: el `ON CONFLICT ... DO UPDATE SET` de `set_media_mapping` lleva comentario en espanol sobre el pisado entre namespaces.
    - Comando: `cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_database.py tests/test_persisted_urls.py -q` sale 0.
  </acceptance_criteria>

  <done>El vinculo tiene columna de offset, la BD viva migra con backup, y el camino del anime no se ha enterado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `linking.py` — la puerta que sabe decir que no</name>
  <files>apps/backend/nyanko_api/linking.py, apps/backend/tests/test_linking.py</files>

  <read_first>
    - `apps/backend/nyanko_api/database.py:2418-2441` — `get_media_mapping_full` (Task 1) y `set_media_mapping`. `linking.py` LEE por el primero y no llama al segundo nunca.
    - `apps/backend/nyanko_api/sources/errors.py` — la taxonomia de errores de la Fase 2 (`SourceError` y familia) y como se declaran. `UnlinkedSeriesError` NO hereda de `SourceError`: no es un fallo de la fuente, es la ausencia de consentimiento del usuario. Miralo para seguir el estilo de declaracion, no para colgarse de esa jerarquia.
    - `apps/backend/nyanko_api/progress.py` — `effective_chapter` / `next_progress`. El precedente de esta casa para «modulo pequeno, puro, con el invariante derivado AL LEER en vez de mantenido en cuatro sitios» (decision de la Fase 1). `linking.py` es su gemelo para el vinculo.
    - `apps/backend/nyanko_api/database.py:262-270` — `pending_mutations`. La cola que la Fase 5 va a usar y que, sin esta puerta, reintentaria de forma duradera una escritura en la serie equivocada. Es el modo de fallo que el ROADMAP describe («no hay deshacer, porque nadie se entero de nada»).
    - `.planning/ROADMAP.md`, seccion «Phase 4 ... Por que es fase propia» — el parrafo entero. Es la especificacion de esta task.
  </read_first>

  <action>
    Crear `apps/backend/nyanko_api/linking.py` con cuatro simbolos y nada mas:

    - `SeriesLink`: dataclass `frozen=True, slots=True` con `source_name: str`, `series_id: str`,
      `media_id: int`, `chapter_offset: int`. Un metodo `absolute_chapter(self, chapter: float) -> float`
      que devuelve `chapter + self.chapter_offset`. Ese metodo es lo que hace verdadero el «nunca se
      calcula en el momento del sync» del criterio 2: el offset es dato guardado y esto es aritmetica
      sobre el dato, no una inferencia.

    - `UnlinkedSeriesError(Exception)`: se construye con `source_name` y `series_id` y expone un
      atributo `message` en espanol y dirigido al usuario, del tipo «Esta serie no esta vinculada a
      ninguna entrada de tu biblioteca. Vinculala para poder sincronizar el progreso.» El criterio 4
      dice «se lo dice al usuario»: un error mudo no lo cumple. El plan 04-03 lo traduce a HTTP.

    - `resolve_link(database, source_name, series_id) -> SeriesLink | None`: llama a
      `database.get_media_mapping_full(source_name, series_id)` y compone el `SeriesLink`, o `None`.
      Solo lee. Comentario obligatorio en espanol: en el namespace de manga, «hay fila» ES «el usuario
      lo confirmo», y ese invariante lo sostiene que el UNICO escritor de filas de manga sea el
      endpoint de confirmar (plan 04-03) — las propuestas del matcher no se persisten jamas. Si
      alguien anade un segundo escritor, este comentario es lo que le dice que esta rompiendo LNK-01.

    - `require_link(database, source_name, series_id) -> SeriesLink`: `resolve_link` o
      `raise UnlinkedSeriesError`. Comentario obligatorio: esta es LA puerta de LNK-04; la Fase 5 la
      cruza ANTES de `enqueue_mutation` o construye el bug que esta fase existe para impedir.

    `linking.py` NO importa `matcher`, ni `find_best_match`, ni `rank_matches`, ni nada que sepa
    puntuar. La ausencia de ese import es el diseno: si `require_link` pudiera ver un score, alguien
    acabaria poniendole un umbral, y un umbral es exactamente el auto-vinculo que LNK-01 prohibe.

    Crear `apps/backend/tests/test_linking.py`:

    1. Round-trip: `set_media_mapping('local_archive', '0:Berserk', 42, chapter_offset=100)` ->
       `require_link` devuelve un `SeriesLink` con `media_id` 42 y `chapter_offset` 100.
    2. `absolute_chapter`: con `chapter_offset` 100, el capitulo 12.5 da 112.5. Con offset 0, 12.5 da
       12.5. Con offset negativo -10, el capitulo 12 da 2. El decimal SOBREVIVE (FND-04: el 10.5 es
       REAL en local; el `floor()` es cosa del envio al proveedor, en la Fase 5).
    3. EL GATE DE LNK-04, y el unico test que de verdad importa de este plan: sobre una BD SIN fila
       para `('local_archive', '0:Berserk')`, `require_link` lanza `UnlinkedSeriesError` — y lo sigue
       lanzando cuando en esa misma BD SI existe (a) una fila de `match_corrections` con
       `raw_pattern='berserk'` apuntando a un `media_id`, y (b) una entrada de biblioteca cuyo titulo
       el matcher puntuaria altisimo. Nombra el test para que se lea el modo de fallo, no la funcion:
       algo como `test_una_propuesta_fuerte_no_es_un_vinculo_confirmado`. Comentario en espanol
       citando el escenario del ROADMAP: leo el capitulo 12 de la serie A, el matcher difuso lo
       vincula a la B, la app escribe en la B de mi AniList real, `pending_mutations` lo reintenta de
       forma duradera, y no hay deshacer.
    4. «No escribe, no encola», medido y no razonado: tras el `UnlinkedSeriesError` de (3),
       `SELECT COUNT(*) FROM pending_mutations` es 0 y `SELECT COUNT(*) FROM media_mappings` es el
       mismo de antes de la llamada. Y `resolve_link` llamado 100 veces sobre una serie sin vinculo
       deja `media_mappings` vacia: preguntar si hay vinculo no puede crearlo.
    5. `UnlinkedSeriesError` lleva `message` no vacio y menciona la serie.

    Todos los tests llaman a `require_link`/`resolve_link` directamente contra una `Database` de
    `tmp_path`. Sin `TestClient`: esto es un modulo puro sobre una BD, y su gate no depende de que
    exista un endpoint.
  </action>

  <verify>
    <automated>cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_linking.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Asercion de comportamiento: `require_link(db, 'local_archive', '0:Berserk')` sobre una BD sin la fila lanza `UnlinkedSeriesError`.
    - Asercion de comportamiento (el gate): lo sigue lanzando con una fila de `match_corrections` para `berserk` y una entrada de biblioteca fuertemente puntuable presentes en la misma BD. Este test es rojo contra cualquier implementacion que consulte al matcher.
    - Asercion de comportamiento: tras ese `UnlinkedSeriesError`, `SELECT COUNT(*) FROM pending_mutations` = 0 y `SELECT COUNT(*) FROM media_mappings` no ha variado.
    - Asercion de comportamiento: `resolve_link` sobre una serie sin vinculo devuelve `None` y, llamado 100 veces, deja `SELECT COUNT(*) FROM media_mappings` en 0.
    - Asercion de comportamiento: `SeriesLink(source_name='local_archive', series_id='0:B', media_id=1, chapter_offset=100).absolute_chapter(12.5)` devuelve `112.5`. Con `chapter_offset=-10`, `absolute_chapter(12)` devuelve `2`.
    - Asercion de comportamiento: `UnlinkedSeriesError` expone `message` no vacio, en espanol, mencionando la serie.
    - Asercion de fuente: `linking.py` no contiene `matcher`, `find_best_match`, `rank_matches`, `score`, `INSERT`, `UPDATE` ni `set_media_mapping`.
    - Comando: `cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_linking.py -q` sale 0.
  </acceptance_criteria>

  <done>Existe una puerta, sabe decir que no, y el test que lo demuestra falla si alguien le ensena el score del matcher.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `media_mappings` (SQLite) -> `resolve_link` -> (Fase 5) escritura en AniList | La frontera que importa. Lo que salga de aqui se convierte, una fase mas adelante, en una escritura IRREVERSIBLE en la cuenta real del usuario, reintentada de forma duradera por `pending_mutations`. No hay deshacer aguas abajo. |
| BD v10 viva (2.761 `library_entries`) -> `initialize()` v11 | Migracion sobre datos reales del usuario. El unico rollback es el backup que arma el bump de version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | Tampering | `require_link` -> (Fase 5) `enqueue_mutation` -> AniList real | critical | mitigate | ESTA ES LA AMENAZA DE LA FASE. Sin vinculo confirmado, `require_link` lanza `UnlinkedSeriesError` y no hay umbral de score que lo evite. Verificado por `test_una_propuesta_fuerte_no_es_un_vinculo_confirmado` + recuento de `pending_mutations` = 0, no por diseno. |
| T-04-02-02 | Tampering | `set_media_mapping` `ON CONFLICT DO UPDATE` | medium | mitigate | Un llamador de anime sin `chapter_offset` pisaria con 0 el offset de manga de la misma clave. Namespace de `provider` disjunto (`local_archive` vs adapters de sitio) + comentario en el `DO UPDATE` + test de no-regresion del camino de anime. Aceptado como propiedad de los datos, documentada en el codigo. |
| T-04-02-03 | Tampering | migracion v10 -> v11 sobre BD real | high | mitigate | `_add_column` aditivo con DEFAULT 0: cero backfill, cero rebuild (precedente medido de la Fase 1: un rebuild sobre 2.774 filas vivas cambiaba valores). `_backup_before_migration()` armado por el bump. Test: BD v10 con fila viva -> `integrity_check` ok + recuentos iguales + backup escrito. |
| T-04-02-04 | Information Disclosure | `media_mappings.chapter_offset` | low | accept | La columna es INTEGER. No guarda host, puerto, ruta ni URL, asi que no abre superficie de FND-05. `media_mappings` ya esta cubierta por `tests/test_persisted_urls.py:255`. |
| T-04-02-05 | Repudiation | `chapter_offset` sin acotar | low | accept | El offset lo escribe el propio usuario desde su UI y solo afecta a su propio numero de capitulo. La validacion de rango vive en el borde HTTP (plan 04-03, T-04-03-03), que es donde esta la frontera de confianza; duplicarla aqui es validar dos veces lo mismo. |
| T-04-02-SC | Tampering | npm/pip/cargo installs | low | accept | Este plan no instala ni un paquete: `sqlite3` y `dataclasses` son stdlib. Sin superficie de cadena de suministro no hay tabla de legitimidad que exigir. |
</threat_model>

<verification>
- `cd apps/backend; .venv/Scripts/python.exe -m pytest -q` — la suite entera sigue verde. Ojo: el bump de version toca seis aserciones de `test_database.py` que este plan posee; si alguna sigue diciendo `10`, la suite lo dira.
- `cd apps/backend; .venv/Scripts/python.exe -m ruff check nyanko_api/linking.py nyanko_api/database.py tests/test_linking.py`
- `cd apps/backend; .venv/Scripts/python.exe scripts/verify_real_db_migration.py` — FND-06: la migracion v11 contra una COPIA de la BD real, no contra un fixture. Si el script pide la copia y no esta, el SUMMARY lo dice como `unknown` y el orquestador lo cierra; no se inventa el resultado (CODEX-RULES regla 7).
</verification>

<success_criteria>
- Criterio 2 del ROADMAP verdadero: el vinculo se almacena en `media_mappings` con su `chapter_offset`, mirror de `episode_offset`; y el offset es dato guardado, no calculo del sync (`SeriesLink.absolute_chapter`).
- Criterio 4 a medio camino y con su mitad dura hecha: existe `require_link`, falla cerrado, y su test es rojo contra una implementacion que confunda propuesta con confirmacion. El plan 04-03 lo cuelga del endpoint y le pone el tripwire de `pending_mutations`.
- La BD real migra con backup y sin cambiar un solo recuento.
</success_criteria>

<output>
Create `.planning/phases/04-identidad-y-v-nculo-fuente-entrada-del-tracker/04-02-SUMMARY.md` when done
</output>
