---
context: phase
phase: 04-identidad-y-v-nculo-fuente-entrada-del-tracker
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-07-17T06:45:15.599Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Esto no son sugerencias. Se descubrió fallando, midiendo, en esta misma fase.

- [ ] CONSTRAINT: **Un gate de grep/git escrito razonando está mal hasta que lo midas** — 4 de 4 en esta fase fallaron al medirse, y 2 habrían reprobado la fase por su propio éxito — mitigación estructural: antes de escribir o aceptar cualquier gate, **ejecutarlo contra un fixture real** (o contra el árbol) y comprobar que da el número esperado Y que se pone rojo cuando debe.

**No sigas hasta tener esa casilla marcada.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Gate escrito razonando | 4/4 en esta fase. (1) El gate del censo afirmaba 4 llamadores *después* de la fase, pero el PUT es el quinto → habría fallado por su propio éxito. (2) El gate del escritor único exigía dos tokens en la misma línea; la llamada real ocupa 121 chars con `line-length=100` y el estilo de la casa la parte en 5 líneas → devolvía **0** sobre código correcto. (3) El remedio que propuso el plan-checker (`grep -A2`) **reproducía el mismo bug** (el arg cae en +5, fuera de la ventana). (4) El gate del criterio 3 decía que `git log --diff-filter=A` «lista el TEST antes que el MÓDULO», pero `git log` ordena del más nuevo al más viejo → sin `--reverse` se lee como fallado sobre un árbol correcto. | blocking | Ejecutar el gate contra un fixture con el estilo real del repo ANTES de escribirlo. Comprobar las dos direcciones: que dé el número correcto, y que se ponga rojo al inyectar la regresión que existe para cazar. Preferir `git merge-base --is-ancestor` a leer orden de `git log`. |
| `rg -c` sin matches no imprime `0` | Imprime **nada** y sale 1. Un orquestador que lea vacío como `0` y `0` como aprobado **des-verifica LNK-01 en silencio**. | advisory | Los gates de 04-03 ya lo documentan. Tratar salida vacía como FALLO, no como cero. |
| Codex muere a los ~9 min si lo lanza el subagente | 3 de 3. El subagente `codex:codex-rescue` dice que lo manda a background y **no lo hace** (tarda 10+ min en «volver»; un background real devuelve en segundos). El límite de foreground del harness se lleva su árbol de procesos, siempre justo tras un comando PowerShell con exit 0. | advisory | Lanzarlo con `node <companion> task --write --fresh --prompt-file <f>` dentro de un **Bash con `run_in_background: true`** (lo que manda `CODEX-RULES.md`). Nota: el `--background` del companion también funcionó (2 de 2, 10m y 7m) pese a que CODEX-RULES dice que muere — conflicto medido, sin resolver. Ante la duda, la regla del proyecto. |
| El status del companion miente | Reporta jobs muertos como `running` durante horas, y su índice llegó a listar como «latest» jobs de hace 4 días ignorando los vivos en disco. | advisory | El **log en disco es la única fuente fiable**: `.claude/plugins/data/codex-openai-codex/state/<repo>/jobs/<job>.log`. Vigilar su mtime. El resultado sale de `storedJob.result.rawOutput`. |
| `/codex:cancel` puede matarte Discord | Comprueba si un job vive mirando **solo si su PID existe**, sin verificar identidad. Un job del 13-jul tiene PID 35216, reciclado por el SO a **Discord**: cancelarlo ejecuta `taskkill /PID 35216 /T /F`. | advisory | No correr `/codex:cancel` a ciegas. Verificar identidad del proceso primero (`Get-Process -Id <pid>`). Hay más zombis listados: `task-mroa9isn-155dow`, `task-mro88rz5-k1eqc5`. |
| Los worktrees nacen de `origin/main`, 117 commits atrás | Un executor de `/gsd-quick` leyó el `PROJECT.md` de la era v0.2 e iba a escribir **lo contrario** de su tarea, reportando éxito. El fichero destino era byte-idéntico en ambos commits: la corrupción estaba entera del lado de la fuente. | advisory | Ya cubierto: el `worktree.base-check` de execute-phase lo detecta (`shouldDegrade: true`) y degrada a secuencial sobre el árbol principal. Si aparece en otro flujo, comprobar `git rev-list --left-right --count origin/main...main`. |

<current_state>
Fase 04 **en ejecución**. Plan 04-01 de 4 **cerrado**; los otros tres sin empezar. Árbol limpio, todo commiteado, sin jobs async pendientes.

La fase está planificada y verificada a fondo: 3 rondas con dos revisores independientes (`gsd-plan-checker` + Codex), 6 issues → 2 → 3 → 0. Ninguno era de diseño; todos de precisión.

Suite actual: **476 passed, 0 failed** (baseline Fase 3: 461, +15 de la tabla nueva de 04-01).
</current_state>

<completed_work>

- **04-01 — DONE.** 4 commits atómicos:
  - `9ada90e` **rojo**: tabla de 14 casos, exit 2, `ModuleNotFoundError: No module named 'nyanko_api.chapter_recognition'`
  - `3096d27` el módulo puro, tabla **verde** (15 passed)
  - `ceb5914` `LocalArchiveSource` delega; la regex vieja **desaparece** (dos implementaciones = criterio 3 falso)
  - `227ca7d` SUMMARY cerrado + el gate del criterio 3 arreglado (`--reverse`)

  Criterio 3 verificado como hecho de git: `git merge-base --is-ancestor 9ada90e 3096d27` → exit 0.

  Hallazgo: la regex vieja **estaba mal** (`Vol.2 Cap 15` → `2.0`, agarraba el volumen; `Cap 12 extra` → `12.0`, el mismo número que el capítulo 12 → dos capítulos subiendo el mismo progreso al AniList real). La delegación lo corrige y **ningún test existente dependía del comportamiento viejo**.
</completed_work>

<remaining_work>

- **04-02** (ola 1) — esquema v11 + `chapter_offset`, `assert_manga_namespace`, `linking.py` con `require_link`. **El más delicado de la fase.**
- **04-03** (ola 2) — proponer/confirmar/desvincular en HTTP, sin las DOS ramas de auto-persistencia al 0.85, tripwire de `pending_mutations`.
- **04-04** (ola 3) — panel de vínculo + aviso «sin vincular» en el lector.
- Después: verifier de fase + UAT.
</remaining_work>

<decisions_made>

- **Planificada sin CONTEXT.md ni RESEARCH.md** — el ROADMAP dice `Research pass: No` y sus 4 Success Criteria hicieron de decisiones fijadas. Confirmado por el usuario.
- **Codex ejecuta; el orquestador corre la suite, commitea y cierra `.planning/`** — práctica de las fases 1-3 (`CODEX-RULES.md`). Elegido por el usuario. Los planes están escritos para ese reparto.
- **Criterio 4 cierra con el test de `require_link` de 04-02 + el tripwire declarado** — Codex pedía un test de integración sobre el enqueue de la Fase 5; el usuario lo rechazó. El tripwire pasa vacuo hoy y el plan **lo dice** en vez de disimularlo.
- **La cuarta costura se documenta, no se guarda** — Codex la puntuó HIGH alegando que llega a escrituras automáticas de progreso. **Medido: no.** `match_playback` solo propone; los dos `enqueue_mutation` viven en `update_progress` (:2776) y `bulk_update_library_entry` (:3276), endpoints aparte. T-04-02-08 low/accept.
- **Olas secuenciales pese a `parallelization: true`** — el companion está en modo *shared session*: un runtime único de Codex. Dos jobs a la vez se pisan.
</decisions_made>

<blockers>
Ninguno técnico. Dos cosas esperan decisión del usuario, ninguna bloquea la Fase 4:
- **117 commits sin pushear** a `origin/main` (tip remoto `c9a7d62`). Todo v0.3. No lo hice: publica el milestone en el GitHub público y es una acción hacia afuera.
- **Dos bugs vivos de anime, al backlog** (los encontró el planner, fuera de alcance): `main.py:3690` y `:4093` no pasan offset, así que su `ON CONFLICT DO UPDATE` pone `episode_offset` a 0 en cada escritura — el mismo bug que `:3617-3618` documenta haber arreglado ya en `:3619`. Y `media_mappings.media_id` guarda **las dos especies** de id en el namespace de anime, que es por lo que `:3552-3561` tiene que adivinar.
</blockers>

## Required Reading (in order)

1. `.planning/CODEX-RULES.md` — el contrato de ejecución. Codex NO corre pytest (su sandbox deniega el TEMP del sistema: 6 failed dentro, 17 passed fuera), NO commitea (deniega escribir en `.git/`), escribe `unknown` en el SUMMARY. **El orquestador mide y firma.** Lee también su sección final «Notas para el ORQUESTADOR».
2. `.planning/phases/04-.../04-02-PLAN.md` — el siguiente plan.
3. `.planning/STATE.md` § Current Position — lleva el contexto de ejecución de la fase.

## Infrastructure State

- **Árbol:** limpio, en `main`, HEAD `227ca7d`.
- **Suite:** 476 passed, 0 failed. `cd apps/backend; .venv/Scripts/python.exe -m pytest -q` (~107s).
- **Worktrees:** `execute-phase` degrada solo a secuencial (`shouldDegrade: true`, HEAD divergido de `origin/HEAD`). Correcto, no lo pelees.
- **Codex:** sin jobs vivos. Varias entradas zombi listadas como `running` — ignóralas, no las canceles.

<context>
El valor de esta fase no está en el código, está en lo que los gates cazaron antes de gastar dinero en ejecución: un invariante central que era **falso** (la fase afirmaba «un solo escritor de manga» cuando había cuatro, todos tomando el provider del cliente), una guarda que cubría **solo la mitad de las puertas** (escribía protegido, leía y borraba abierto — un reading event de manga con `source=crunchyroll` resolvía un mapping de anime como vínculo confirmado, y el DELETE podía borrarlo), y cuatro gates que se ponían verdes o rojos por las razones equivocadas.

Los dos revisores se equivocaron en direcciones **opuestas** y se corrigieron mutuamente: el checker sobredimensionó la seguridad de la guarda, Codex sobredimensionó el peligro de la cuarta costura, y el remedio que propuso el checker para el gate roto reproducía el bug que él mismo había diagnosticado. Ninguno era fiable solo. Lo único que resolvió cada desempate fue **medir en el código**. Mantén esa postura: cuando dos agentes discrepan, no promedies ni le creas al último — mide.

04-02 mete la guarda **dentro** de `set_media_mapping`, que es código de anime ya enviado con cuatro llamadores vivos. Es el único punto de la fase donde un error rompe algo que hoy funciona. La suite completa (476) es la red; míralas con más atención que en 04-01.
</context>

<next_action>
Empezar por: `/gsd-execute-phase 4` — retoma solo desde 04-02 (04-01 ya tiene su SUMMARY, así que lo salta).

Para 04-02: escribir el prompt de Codex a un fichero (recordándole `CODEX-RULES.md` y las reglas 1/2/4/6/7 explícitamente), lanzarlo con `node <companion> task --write --fresh --prompt-file <f>` dentro de un **Bash con `run_in_background: true`**, y al volver: verificar el alcance (solo `files_modified`), correr la **suite completa** fuera del sandbox (baseline 476), commitear **atómico por tarea** con mensajes en inglés y prefijo `(04-02)`, y cerrar los `unknown` del SUMMARY con los resultados medidos.
</next_action>
