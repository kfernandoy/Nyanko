---
phase: 04-identidad-y-v-nculo-fuente-entrada-del-tracker
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - apps/backend/nyanko_api/main.py
  - apps/backend/nyanko_api/models.py
  - apps/backend/tests/test_manga_link.py
autonomous: true
requirements: [LNK-01, LNK-02, LNK-04]

must_haves:
  truths:
    - "Criterio 1 / LNK-02: `matcher.py` PROPONE con un score de confianza. `POST /api/manga/link/match` devuelve `match` + `match_score` + `suggestions`, calcado de `PlaybackMatchResponse` (`models.py:486-494`) — el flujo detectar->proponer->confirmar->deshacer de `playback_events` que el ROADMAP nombra como precedente."
    - "Criterio 1 / LNK-01: «nada queda vinculado sin que el usuario lo confirme». El endpoint de proponer NO ESCRIBE. Cero filas. El unico escritor de vinculos de manga es `PUT /api/manga/link`, que solo corre cuando el usuario le da al boton."
    - "Criterio 1: «la correccion del usuario se guarda y manda sobre la propuesta». Se guarda en `media_mappings` y manda porque `GET`/`match` cortocircuitan por la fila antes de llamar al matcher — el precedente literal de `main.py:3552-3570`, donde un mapping existente devuelve score 1.0 y el fuzzy no llega a correr."
    - "Criterio 4 / LNK-04: la puerta `require_link` (plan 04-02) queda COLGADA de la unica puerta viva: `POST /api/manga/reading-events` (`main.py:1711-1725`), que es donde nace el evento «capitulo terminado» (RD-06) y donde la Fase 5 va a enganchar `enqueue_mutation` (SYN-01/SYN-05). Sin vinculo, la respuesta dice `linked: false` con el mensaje de `UnlinkedSeriesError`."
    - "Criterio 4, «no escribe, no encola», verificado como TEST y no como intencion: el test del camino sin vinculo afirma `SELECT COUNT(*) FROM pending_mutations` = 0. Hoy esa asercion es un TRIPWIRE para la Fase 5, no una tautologia: el dia que SYN-01 anada el `enqueue_mutation` en este endpoint sin cruzar `require_link`, este test se pone rojo. Es la defensa de costura que la leccion B-1 de 0.2 pide (una fase verificada no es una fase segura)."
    - "El vinculo rellena `reading_events.media_id`, la columna que la Fase 3 dejo reservada con el comentario «El vinculo nace en la Fase 4» (`database.py:304-306`). La fila del evento se sigue escribiendo con o sin vinculo: un log local de lectura no es un sync, y romper RD-06 para probar LNK-04 seria cambiar el requisito."
    - "El catalogo contra el que se propone es la biblioteca de MANGA del usuario: `_scan_match_library` (`main.py:2076-2097`) parametrizado por `media_type`, reusando `get_combined_library('MANGA', ...)` que ya existe. No se escribe un segundo constructor de MediaItems."
  artifacts:
    - "POST /api/manga/link/match — propone, no escribe"
    - "PUT /api/manga/link — confirma. El unico escritor"
    - "GET /api/manga/link — el vinculo vigente o null"
    - "DELETE /api/manga/link — desvincular"
    - "apps/backend/tests/test_manga_link.py — incluido el tripwire de `pending_mutations`"
  key_links:
    - "`main.py:3684-3690` — LA TRAMPA. El precedente de anime PERSISTE el mapping cuando `best_score >= 0.85`. Esa rama es un auto-confirm y es EXACTAMENTE lo que LNK-01 prohibe. Se copia el resto del flujo; esa rama NO."
    - "`main.py:1711-1725` -> `require_link` -> (Fase 5) `enqueue_mutation`. La costura que esta fase existe para blindar."
  prohibitions:
    - statement: "PROHIBICION CENTRAL (Criterio 1 / LNK-01). El endpoint de proponer no persiste NADA, con ningun score, ni siquiera con 1.0. No se replica la rama `if request.site_identifier and best_score >= 0.85: database.set_media_mapping(...)` de `main.py:3688-3690`. Un auto-confirm por umbral es el bug de esta fase con otro nombre."
      status: pending
      verification: "Asercion de comportamiento (`tests/test_manga_link.py`): sobre una biblioteca de manga que contiene la entrada exacta `Berserk`, `POST /api/manga/link/match` para la serie `0:Berserk` devuelve 200 con `match_score` >= 0.99 Y `SELECT COUNT(*) FROM media_mappings` sigue siendo 0. El test es rojo contra cualquier umbral de auto-persistencia."
    - statement: "Un intento de sync sobre una serie sin vinculo confirmado falla cerrado: no escribe en el proveedor y NO ENCOLA."
      status: pending
      verification: "Asercion de comportamiento: `POST /api/manga/reading-events` sobre una serie sin vinculo responde con `linked` = false y un `reason` en espanol, deja `reading_events.media_id` en NULL, y `SELECT COUNT(*) FROM pending_mutations` = 0. TRIPWIRE de la Fase 5: cuando SYN-01 anada el encolado aqui sin cruzar `require_link`, esta asercion se pone roja."
    - statement: "`PUT /api/manga/link` no acepta un `media_id` que no este en la biblioteca de manga del usuario."
      status: pending
      verification: "Asercion de comportamiento: `PUT /api/manga/link` con `media_id` 999999 (inexistente) responde 422 y `SELECT COUNT(*) FROM media_mappings` sigue en 0."
    - statement: "No se construye un segundo camino de sync ni se encola nada en esta fase. `enqueue_mutation` no aparece en el codigo nuevo — SYN-01/SYN-05 son de la Fase 5, y adelantarlo aqui es doblar el vinculo como «paso 1 del sync», que es lo que el ROADMAP prohibe explicitamente."
      status: pending
      verification: "Asercion de fuente: el diff de `main.py` de este plan no anade ninguna llamada a `enqueue_mutation`, `edit_entry` ni `update_remote_library_entry`."
    - statement: "No se toca `conftest.py`, `pyproject.toml` ni `pytest.ini` (CODEX-RULES regla 3)."
      status: pending
      verification: "`git status` no lista esos ficheros."
---

<objective>
Poner el flujo entero en HTTP: el matcher propone con su score, el usuario confirma (y solo el
usuario), y la puerta del plan 04-02 queda colgada del sitio exacto por donde la Fase 5 va a intentar
pasar.

Purpose: LNK-02 (proponer con score), LNK-01 (nada se vincula sin confirmacion) y LNK-04 (falla
cerrado). El flujo ya existe entero para anime — `playback/match` propone, `playback/confirm` guarda
el mapping y la correccion. Este plan lo calca para manga con UNA diferencia deliberada y central: se
deja fuera la rama de `main.py:3688-3690`, la que persiste sola cuando el score supera 0.85. Esa rama
es comoda para el anime y es el bug que esta fase existe para impedir en el manga.
Output: cuatro endpoints, el catalogo de manga reusando `_scan_match_library`, y el tripwire que le
espera a la Fase 5 en `reading-events`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.planning/CODEX-RULES.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
@.planning/phases/04-identidad-y-v-nculo-fuente-entrada-del-tracker/04-02-PLAN.md
</context>

## Artifacts this phase produces (cuota de este plan)

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `MangaLinkMatchResponse` | `models.py` | `series_id`, `series_title`, `link` (`MangaLink \| None`), `match` (`MediaItem \| None`), `match_score: float`, `suggestions: list[MediaItem]`. Gemelo de `PlaybackMatchResponse` (`models.py:486-494`) |
| `MangaLink` | `models.py` | `media_id: int`, `chapter_offset: int`, `title: str \| None` |
| `MangaLinkConfirm` | `models.py` | `media_id: int`, `chapter_offset: int = 0` (acotado `ge=-9999, le=9999`) |
| `ReadingEventResponse` | `models.py` | `id: int`, `media_id: int \| None`, `linked: bool`, `reason: str \| None`. Reemplaza el `dict[str, int]` de `main.py:1718` |
| `POST /api/manga/link/match` | `main.py` | Propone. NO escribe |
| `GET /api/manga/link` | `main.py` | El vinculo vigente o `null` |
| `PUT /api/manga/link` | `main.py` | Confirma. EL UNICO escritor de vinculos de manga |
| `DELETE /api/manga/link` | `main.py` | Desvincula |
| `_scan_match_library` | `main.py:2076-2097` | MODIFICADO: gana el parametro `media_type: str = "ANIME"` |
| `_series_title_from_id` | `main.py` | `("0:Berserk") -> "Berserk"`. El titulo sale del id opaco, no del cliente |
| `create_reading_event` | `main.py:1711-1725` | MODIFICADO: resuelve el vinculo y rellena `reading_events.media_id` |
| `apps/backend/tests/test_manga_link.py` | nuevo | El gate de LNK-01/02/04 en HTTP |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Proponer — el matcher con su score, y ni una escritura</name>
  <files>apps/backend/nyanko_api/main.py, apps/backend/nyanko_api/models.py, apps/backend/tests/test_manga_link.py</files>

  <read_first>
    - `apps/backend/nyanko_api/main.py:3520-3700` — `playback_match` ENTERO. Es el precedente que este endpoint calca. Lee con lupa DOS sitios: `:3552-3570` (si hay mapping, cortocircuita y el score es 1.0 — eso SI se copia, es «la correccion manda sobre la propuesta») y `:3684-3690` (si `best_score >= 0.85` PERSISTE el mapping solo — eso NO se copia, es el auto-confirm que LNK-01 prohibe).
    - `apps/backend/nyanko_api/main.py:3821-3851` — como se componen `suggestions` con `rank_matches` y como se devuelve `PlaybackMatchResponse`. La forma de la respuesta.
    - `apps/backend/nyanko_api/main.py:2076-2097` — `_scan_match_library`. Ya construye `MediaItem`s desde `get_combined_library("ANIME", ...)`. El `"ANIME"` esta hardcodeado en `:2080`: ese literal es el parametro que falta.
    - `apps/backend/nyanko_api/matcher.py:133-159` — `rank_matches`: top-N con `floor=0.55`, y el comentario que explica por que ese suelo (por debajo, `SequenceMatcher` puntua ~0.45 entre titulos sin relacion que comparten «no»/«to»).
    - `apps/backend/nyanko_api/matcher.py:216-278` — `find_best_match`: la firma, el desempate por ambiguedad (`:269-277`) y el camino de `corrections` que devuelve 1.0 (`:228-240`).
    - `apps/backend/nyanko_api/main.py:3605-3612` — el `asyncio.to_thread(find_best_match, ...)` y su comentario: el fuzzy sobre ~2300 titulos BLOQUEA el event loop. La biblioteca de manga es mas pequena pero el patron no se rompe.
    - `apps/backend/nyanko_api/models.py:486-494` — `PlaybackMatchResponse`, el modelo a calcar.
    - `apps/backend/nyanko_api/linking.py` — `resolve_link` (plan 04-02).
  </read_first>

  <action>
    En `main.py`:

    1. `_scan_match_library(database, media_type="ANIME")`: anadir el parametro y pasarlo a
       `get_combined_library(media_type, primary, DEFAULT_ACCOUNT_ALIAS)` (`main.py:2080`). El default
       `"ANIME"` conserva a los llamadores vivos sin tocarlos. Ojo al campo `episodes` del `MediaItem`
       (`main.py:2090`): para manga el equivalente es `chapters`/`chapter_count` — mapear lo que
       `get_combined_library("MANGA", ...)` devuelva; si la clave no existe, `None`, sin inventar.

    2. `_series_title_from_id(series_id: str) -> str`: el id de la Fase 3 es
       `{root_key}:{ruta relativa posix}` (`local_archive.py:336-337`), luego el titulo es el ultimo
       segmento: partir por `:` una vez y quedarse con el ultimo tramo tras `/`. El titulo NO se
       acepta del cliente: la fuente de verdad del nombre es el id opaco que el propio backend emitio.
       Un `series_id` cuya ruta relativa sea `.` es la RAIZ de una carpeta de biblioteca, no una
       serie: responder 400.

    3. `POST /api/manga/link/match`, con `series_id: str` y `source: str = "local_archive"` como query
       params (el mismo estilo que `main.py:1618-1623`), devolviendo `MangaLinkMatchResponse`:
       - `resolve_link(database, source, series_id)` primero. Si hay vinculo, devolverlo en `link`,
         con `match_score` 1.0 y sin correr el fuzzy. Es el cortocircuito de `main.py:3552-3570`: la
         correccion guardada del usuario MANDA sobre la propuesta.
       - Si no hay vinculo: `library = _scan_match_library(database, media_type="MANGA")`, y
         `find_best_match` + `rank_matches` sobre `_series_title_from_id(series_id)`, ambos dentro de
         `asyncio.to_thread` por el motivo de `main.py:3605-3607`. Devolver `match`, `match_score` y
         hasta 5 `suggestions` sin duplicar el match.
       - Biblioteca de manga vacia: 200 con `match` null, `match_score` 0.0 y `suggestions` vacia. No
         es un error; es un usuario que aun no tiene manga en su lista.

    LA LINEA QUE NO SE ESCRIBE: este endpoint no llama a `set_media_mapping` ni a
    `set_match_correction`. Ni con score 1.0. La rama de `main.py:3688-3690` se queda donde esta,
    sirviendo al anime, y no viaja a este fichero. Va comentario en espanol en el endpoint diciendo
    exactamente eso y por que: el ROADMAP trata el mal-vinculo como corrupcion de datos, y un umbral
    de auto-persistencia es un 5% de fallo invisible en una demo y devastador en una lista de 2.761
    entradas.
  </action>

  <verify>
    <automated>cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_manga_link.py -q -k propone</automated>
  </verify>

  <acceptance_criteria>
    - Asercion de comportamiento: con una biblioteca de manga que contiene `Berserk`, `POST /api/manga/link/match?series_id=0:Berserk` responde 200 con `match.title` = `Berserk` y `match_score` >= 0.99.
    - Asercion de comportamiento (LA de este task): tras esa llamada, `SELECT COUNT(*) FROM media_mappings` sigue siendo 0. Rojo contra cualquier auto-persistencia por umbral.
    - Asercion de comportamiento: con un vinculo ya confirmado para `0:Berserk`, la respuesta trae `link.media_id` y `match_score` = 1.0. La correccion del usuario manda sobre lo que el fuzzy opinaria.
    - Asercion de comportamiento: con biblioteca de manga vacia, responde 200 con `match` null y `match_score` 0.0 (no 404, no 500).
    - Asercion de comportamiento: `series_id` = `0:.` (raiz) responde 400.
    - Asercion de comportamiento: `suggestions` no contiene el propio `match` y trae como mucho 5 entradas.
    - Asercion de comportamiento: la biblioteca contra la que se propone es de MANGA — una entrada de ANIME con titulo identico NO aparece ni en `match` ni en `suggestions`.
    - Asercion de fuente: el cuerpo del endpoint no contiene `set_media_mapping` ni `set_match_correction`, y lleva el comentario que explica por que no se copia `main.py:3688-3690`.
    - Comando: `cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_manga_link.py -q -k propone` sale 0.
  </acceptance_criteria>

  <done>El matcher propone con su score y no ha tocado la BD ni una vez.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirmar — el unico escritor, y lo que se niega a escribir</name>
  <files>apps/backend/nyanko_api/models.py, apps/backend/nyanko_api/main.py, apps/backend/tests/test_manga_link.py</files>

  <read_first>
    - `apps/backend/nyanko_api/main.py:3854-3960` — `confirm_playback` entero. En `:3935-3952` esta el cierre del flujo: `set_media_mapping(...)` con el offset APRENDIDO del progreso confirmado, y `set_match_correction(...)`. El manga simplifica: el offset lo escribe el usuario, no se aprende.
    - `apps/backend/nyanko_api/database.py:2431-2441` — `set_media_mapping` con el `chapter_offset` del plan 04-02.
    - `apps/backend/nyanko_api/database.py:2418-2429` — `get_media_mapping_full` (plan 04-02).
    - `apps/backend/nyanko_api/main.py:2076-2097` — `_scan_match_library`, ahora con `media_type` (Task 1). Es de donde sale el conjunto de `media_id` validos.
    - `apps/backend/nyanko_api/models.py:497-503` — `PlaybackConfirmRequest`, la forma del cuerpo a calcar.
    - `apps/backend/nyanko_api/main.py:1662-1690` — `reader_prefs`/`set_reader_prefs`: el estilo GET/PUT con query params + body de esta casa para `/api/manga/*`.
  </read_first>

  <action>
    En `models.py`, anadir `MangaLink`, `MangaLinkConfirm` y `MangaLinkMatchResponse` (formas en la
    tabla de artefactos de arriba). `MangaLinkConfirm.chapter_offset` va acotado con
    `Field(default=0, ge=-9999, le=9999)`: el offset lo teclea un humano y es aritmetica que acaba en
    el AniList real. La cota es una linea; razonar sobre desbordamientos no.

    En `main.py`, tres endpoints con `series_id` y `source = "local_archive"` como query params:

    - `GET /api/manga/link` -> `MangaLink | None`: `resolve_link`, y `title` resuelto contra la
      biblioteca de manga (para que la UI pueda decir a QUE esta vinculado sin una segunda llamada).
      Sin vinculo, `null`. NUNCA 404: «no vinculado» es una respuesta legitima y esperada, no un error.

    - `PUT /api/manga/link` con cuerpo `MangaLinkConfirm` -> `MangaLink`. Este es el unico escritor de
      vinculos de manga del arbol, y por tanto el sitio donde vive el invariante «hay fila = el
      usuario confirmo» del que depende `require_link`. Antes de escribir:
      1. Rechazar `series_id` de raiz (`_series_title_from_id` ya lo detecta) con 400.
      2. VALIDAR `media_id` contra los ids de `_scan_match_library(database, media_type="MANGA")`. Un
         `media_id` que no este en la biblioteca de manga del usuario responde 422 y no escribe fila.
         Es la frontera de confianza: el cuerpo lo manda el renderer, y lo que se guarde aqui es lo
         que la Fase 5 usara para escribir en una cuenta real e irreversible. Un `media_id` arbitrario
         aceptado aqui es una escritura en la serie de otro.
      3. `set_media_mapping(source, series_id, body.media_id, chapter_offset=body.chapter_offset)`.
      Devolver el `MangaLink` resultante. Idempotente: dos PUT con el mismo cuerpo dejan una fila (el
      `ON CONFLICT DO UPDATE` de la PK ya lo garantiza), y un PUT con `media_id` distinto REVINCULA —
      corregirse es la operacion normal, no un caso de error.

    - `DELETE /api/manga/link` -> 204: llamar a `database.delete_media_mapping(source, series_id)`,
      que el plan 04-02 entrega (gemelo de `delete_match_correction`, `database.py:2457-2459`). Si no
      habia fila, 204 igual (idempotente). `set_media_mapping` NO sirve para borrar y `main.py` no
      escribe SQL crudo: si `delete_media_mapping` no existe al llegar aqui, PARA y dilo en la
      respuesta final en vez de inventar un rodeo — `database.py` no esta en `files_modified` de este
      plan (CODEX-RULES regla 6).

    NO se escribe `set_match_correction` para manga. El precedente de anime (`main.py:3952`) lo hace
    porque ahi la correccion se keyea por titulo normalizado y la aprovecha un SEGUNDO consumidor: el
    escaneo de ficheros de video (`main.py:2166`, `get_local_match_overrides`). En manga el vinculo se
    keyea por `series_id` y no hay segundo consumidor: escribir en `match_corrections` seria una fila
    que nadie lee y una segunda verdad sobre la misma pregunta. Va comentario en espanol diciendolo,
    para que el que venga no lo lea como un olvido.
  </action>

  <verify>
    <automated>cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_manga_link.py -q -k "confirma or vincul"</automated>
  </verify>

  <acceptance_criteria>
    - Asercion de comportamiento: `PUT /api/manga/link?series_id=0:Berserk` con `{"media_id": 42, "chapter_offset": 100}` (estando 42 en la biblioteca de MANGA) responde 200, y `GET /api/manga/link?series_id=0:Berserk` devuelve `media_id` 42 y `chapter_offset` 100.
    - Asercion de comportamiento: tras ese PUT, `require_link(db, 'local_archive', '0:Berserk')` (plan 04-02) devuelve el `SeriesLink` en vez de lanzar. La puerta y el escritor hablan de la misma fila.
    - Asercion de comportamiento: `PUT` con `media_id` 999999 (ausente de la biblioteca de manga) responde 422 y `SELECT COUNT(*) FROM media_mappings` sigue en 0.
    - Asercion de comportamiento: `PUT` con `media_id` de una entrada de ANIME responde 422. La biblioteca de validacion es la de manga.
    - Asercion de comportamiento: `PUT` con `chapter_offset` 100000 responde 422 (cota `le=9999`).
    - Asercion de comportamiento: `GET /api/manga/link` sobre una serie sin vinculo responde 200 con cuerpo `null`, no 404.
    - Asercion de comportamiento: dos `PUT` identicos dejan UNA fila. Un `PUT` con `media_id` distinto revincula (una fila, el nuevo `media_id`).
    - Asercion de comportamiento: `DELETE /api/manga/link` deja la serie sin vinculo (`require_link` vuelve a lanzar `UnlinkedSeriesError`) y responde 204 tambien la segunda vez.
    - Asercion de fuente: el diff no contiene `set_match_correction`, y el comentario que explica por que no esta.
    - Comando: `cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_manga_link.py -q -k "confirma or vincul"` sale 0.
  </acceptance_criteria>

  <done>Hay exactamente un escritor de vinculos de manga, valida lo que le llega, y se niega a guardar un media_id que el usuario no tiene.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Colgar la puerta — el fail-closed y el tripwire que le espera a la Fase 5</name>
  <files>apps/backend/nyanko_api/models.py, apps/backend/nyanko_api/main.py, apps/backend/tests/test_manga_link.py</files>

  <read_first>
    - `apps/backend/nyanko_api/main.py:1711-1725` — `create_reading_event`. La puerta. Hoy escribe la fila y devuelve `{"id": ...}`.
    - `apps/backend/nyanko_api/database.py:294-309` — `reading_events` en `SCHEMA`, y el comentario de `:304-306`: «El vinculo nace en la Fase 4. Hasta entonces media_id es NULL». Esta task es la Fase 4 llegando a por esa columna.
    - `apps/backend/nyanko_api/database.py:726-754` — `insert_reading_event`. YA acepta `media_id: int | None = None` (linea 734): la Fase 3 dejo el parametro puesto y a NULL a proposito. Esta task solo tiene que pasarselo. No hace falta tocar `database.py` (y no esta en `files_modified`: CODEX-RULES regla 6).
    - `apps/backend/nyanko_api/linking.py` — `require_link`, `resolve_link`, `UnlinkedSeriesError` (plan 04-02).
    - `apps/backend/nyanko_api/database.py:262-270` — `pending_mutations`. La tabla del tripwire.
    - `apps/backend/tests/test_manga_api.py:265-300` — el test vivo de `POST /api/manga/reading-events`. Afirma `{"id": N}` y la fila. El cambio de modelo de respuesta lo toca; este plan lo posee.
    - `.planning/ROADMAP.md`, «Phase 5 ... Success Criteria» punto 1 y 5 — a que se va a enganchar la Fase 5 aqui, y su «Sin vinculo confirmado (Fase 4), no sube nada: falla cerrado».
  </read_first>

  <action>
    En `models.py`: `ReadingEventResponse` con `id: int`, `media_id: int | None`, `linked: bool`,
    `reason: str | None`.

    En `main.py`, `create_reading_event` (`:1711-1725`): antes de responder, resolver el vinculo con
    `resolve_link(database, source, series_id)` y:

    - CON vinculo: pasar `media_id` a `insert_reading_event` — es la columna que la Fase 3 dejo
      reservada — y responder `linked` = true, `media_id`, `reason` = null.
    - SIN vinculo: escribir la fila IGUAL, con `media_id` NULL, y responder `linked` = false,
      `reason` = el `message` de `UnlinkedSeriesError`. HTTP 200, no error.

    Por que 200 y no 409, que es la pregunta que hay que contestar antes de tocar nada: registrar que
    el usuario termino un capitulo es un LOG LOCAL, no un sync. RD-06 pide que el evento se emita y la
    Fase 3 lo entrego; hacer que reviente sin vinculo romperia un requisito ya verificado y le pondria
    un error en la cara al usuario cada vez que termina un capitulo de una serie que no le apetece
    vincular. Lo que el criterio 4 exige que falle cerrado es el SYNC — la escritura al proveedor y el
    encolado — y eso es justo lo que `linked: false` le dice a la Fase 5 y a la UI. Comentario en
    espanol en el endpoint con este razonamiento y con la frase que importa: la Fase 5 engancha su
    `enqueue_mutation` AQUI, y lo hace detras de `require_link`, no detras de este `resolve_link`
    informativo.

    En `tests/test_manga_link.py`, EL TEST DE LA FASE:

    1. Sin vinculo: `POST /api/manga/reading-events?series_id=0:Berserk&chapter_id=0:Berserk/Cap 12`
       con `{"chapter": 12}` responde 200 con `linked` = false y `reason` no vacio; la fila existe con
       `media_id` NULL; y — la asercion que es el punto entero de la fase —
       `SELECT COUNT(*) FROM pending_mutations` es 0.
    2. Con vinculo confirmado (`PUT /api/manga/link` con `media_id` 42): el mismo POST responde
       `linked` = true, `media_id` 42, y la fila de `reading_events` tiene `media_id` = 42.
    3. El caso que da miedo, escrito como test: existe una entrada de manga `Berserk: The Prototype`
       que el matcher puntuaria alto para la serie `0:Berserk`, y NO hay vinculo confirmado. El POST
       responde `linked` = false y `pending_mutations` sigue en 0. Nombre que se lea como el bug:
       algo como `test_una_propuesta_alta_no_encola_nada_sin_confirmacion`.

    El test 1 lleva comentario obligatorio explicando que es un TRIPWIRE y no una tautologia: hoy nada
    encola en este camino, luego la asercion pasa «gratis». Su trabajo es ponerse ROJA el dia que la
    Fase 5 anada `enqueue_mutation` aqui sin cruzar `require_link`. Eso es exactamente el control que
    en 0.2 falto: B-1 combino dos fases individualmente correctas, ambas con su gate en verde, en un
    resultado roto en silencio, y solo el audit cruzado lo vio. Esta asercion es ese audit, escrito
    por adelantado y ejecutandose en cada suite.

    Actualizar el test vivo de `test_manga_api.py:265-300` al modelo de respuesta nuevo... que NO
    esta en `files_modified` de este plan. Si el cambio de `dict[str, int]` a `ReadingEventResponse`
    lo pone rojo, PARA y dilo en la respuesta final con el nombre del test: el orquestador decide si
    amplia el alcance o si la respuesta conserva la clave `id` para no romperlo. (Conservarla es
    gratis: `ReadingEventResponse` tiene `id`, asi que un test que solo lea `["id"]` sigue verde. Ese
    es el diseno; el aviso es por si afirma la forma exacta del dict.)
  </action>

  <verify>
    <automated>cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_manga_link.py tests/test_manga_api.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Asercion de comportamiento: sin vinculo, `POST /api/manga/reading-events` responde 200 con `linked` = false y `reason` no vacio en espanol; la fila de `reading_events` existe con `media_id` NULL.
    - Asercion de comportamiento (EL TRIPWIRE, criterio 4): tras esa llamada, `SELECT COUNT(*) FROM pending_mutations` = 0. Y con una propuesta fuerte presente en la biblioteca pero sin confirmar, sigue siendo 0.
    - Asercion de comportamiento: con vinculo confirmado a `media_id` 42, el mismo POST responde `linked` = true, `media_id` 42, y `SELECT media_id FROM reading_events ORDER BY id DESC LIMIT 1` devuelve 42.
    - Asercion de comportamiento: la respuesta conserva la clave `id` (no-regresion de `test_manga_api.py:265-300`).
    - Asercion de fuente: el diff de `main.py` no anade ninguna llamada a `enqueue_mutation`, `edit_entry` ni `update_remote_library_entry`. La Fase 4 no sincroniza.
    - Asercion de fuente: el endpoint lleva el comentario que explica por que 200 y no 409, y que dice donde engancha la Fase 5.
    - Asercion de fuente: el test del tripwire lleva el comentario que explica que hoy pasa «gratis» y cual es su trabajo.
    - Comando: `cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_manga_link.py tests/test_manga_api.py -q` sale 0.
  </acceptance_criteria>

  <done>La puerta esta colgada donde la Fase 5 va a llamar, y hay un test que se pondra rojo si llama sin llamar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> sidecar (`/api/manga/link*`) | El cuerpo del `PUT` (`media_id`, `chapter_offset`) es entrada NO CONFIABLE que se persiste y que la Fase 5 convertira en una escritura irreversible en la cuenta AniList real del usuario. Es LA frontera de este plan. |
| `matcher.py` (heuristica difusa) -> `media_mappings` (verdad persistida) | La frontera conceptual de la fase: una opinion estadistica no puede convertirse en un hecho almacenado sin que un humano lo diga. Solo el `PUT` la cruza. |
| `series_id` del cliente -> `media_mappings.site_identifier` | Id opaco de la Fase 3. En este plan NO toca el disco: es una clave de tabla. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-01 | Tampering | `PUT /api/manga/link` -> `media_id` -> (Fase 5) `edit_entry` en AniList real | high | mitigate | El `media_id` del cuerpo se valida contra los ids de `_scan_match_library(database, media_type="MANGA")`. Un id ausente responde 422 y no escribe fila. Sin esta validacion, un `media_id` cualquiera queda vinculado y la Fase 5 escribe progreso en la serie de otro, con reintento duradero y sin deshacer. |
| T-04-03-02 | Tampering | `POST /api/manga/link/match` | high | mitigate | El endpoint no persiste con ningun score. La rama `best_score >= 0.85` de `main.py:3688-3690` NO se replica. Verificado como recuento: `media_mappings` sigue en 0 tras una propuesta a 1.0. |
| T-04-03-03 | Tampering | `chapter_offset` del cuerpo | medium | mitigate | `Field(ge=-9999, le=9999)` en `MangaLinkConfirm`. El offset es aritmetica que acaba en el progreso enviado al proveedor; un valor absurdo es progreso absurdo en una cuenta real. |
| T-04-03-04 | Spoofing | `series_id` -> `_series_title_from_id` | medium | mitigate | El titulo contra el que se matchea se DERIVA del id opaco que emitio el propio backend; no se acepta un `title` del cliente. Un titulo suministrado por el cliente permitiria dirigir el matcher hacia una serie elegida y cosechar una propuesta a 1.0. En este plan `series_id` no toca el sistema de ficheros (es clave de tabla), asi que el traversal de la Fase 3 (D-05, `_resolve_id`) no es superficie aqui. |
| T-04-03-05 | Denial of Service | `find_best_match` sobre la biblioteca | low | mitigate | `asyncio.to_thread`, por el precedente medido de `main.py:3605-3607` (el fuzzy sobre ~2300 titulos bloqueaba el event loop y las busquedas manuales expiraban). |
| T-04-03-06 | Information Disclosure | respuestas de `/api/manga/link*` | low | accept | Solo devuelven datos de la biblioteca del propio usuario. La API ya esta detras del token de instancia (`instance.py`) y escucha en `127.0.0.1`. |
| T-04-03-07 | Repudiation | vinculo cambiado sin rastro | low | accept | Revincular pisa la fila sin historial. El vinculo es visible y editable en la UI (plan 04-04) y el usuario es su unico autor: un log de auditoria para el vinculo de tu propia biblioteca local es peso muerto. Se revisa si la Fase 5 necesita explicar un sync raro a posteriori. |
| T-04-03-SC | Tampering | npm/pip/cargo installs | low | accept | Este plan no instala ni un paquete: FastAPI, pydantic y `asyncio` ya estan. Sin superficie de cadena de suministro no hay tabla de legitimidad que exigir. |
</threat_model>

<verification>
- `cd apps/backend; .venv/Scripts/python.exe -m pytest -q` — la suite entera verde, incluida `test_manga_api.py` (no-regresion de RD-06).
- `cd apps/backend; .venv/Scripts/python.exe -m ruff check nyanko_api/main.py nyanko_api/models.py tests/test_manga_link.py`
- Grep de costura (lo corre el orquestador): `enqueue_mutation` sigue apareciendo solo en `main.py:2776` y `main.py:3276`, los dos sitios de anime que ya existian. Si aparece un tercero en el diff de esta fase, la Fase 4 se ha doblado como «paso 1 del sync» y hay que revertirlo.
</verification>

<success_criteria>
- Criterio 1 verdadero: el matcher propone con score, nada se vincula sin confirmacion (recuento de `media_mappings` = 0 tras una propuesta a 1.0), y la correccion guardada manda sobre la propuesta (cortocircuito con score 1.0).
- Criterio 4 verdadero y verificado como test: sin vinculo confirmado no se escribe ni se encola, y la asercion sobre `pending_mutations` queda armada como tripwire para la Fase 5.
- Cero caminos de sync nuevos en el arbol.
</success_criteria>

<output>
Create `.planning/phases/04-identidad-y-v-nculo-fuente-entrada-del-tracker/04-03-SUMMARY.md` when done
</output>
