---
phase: 04-identidad-y-v-nculo-fuente-entrada-del-tracker
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/backend/tests/test_chapter_recognition.py
  - apps/backend/nyanko_api/chapter_recognition.py
  - apps/backend/nyanko_api/sources/local_archive.py
autonomous: true
requirements: [LNK-03]

must_haves:
  truths:
    - "Criterio 3 del ROADMAP: el reconocimiento de numero de capitulo es un componente PROPIO — vive en su modulo `nyanko_api/chapter_recognition.py`, no dentro del motor de fuentes ni del sync. Hoy son 3 lineas escondidas en `local_archive.py:242-244` (`re.search(r\"\\d+(?:\\.\\d+)?\", name)`) sin un solo test: exactamente el «se embadurna entre el motor y el sync y no se testea en ninguno de los dos» que el criterio nombra."
    - "Criterio 3: el componente es PURO. Firma `recognize_chapter(name: str, series_title: str | None = None) -> float | None`. Sin I/O, sin BD, sin Path, sin red. Se testea llamandolo, no montando una app."
    - "Criterio 3: la tabla de casos se escribe ANTES que el codigo y se ve en ROJO. La Task 1 entrega el test contra un modulo que todavia no existe (falla con `ModuleNotFoundError`); la Task 2 lo pone verde. El orden no es ceremonia: es la unica forma de que la tabla describa el problema en vez de describir la implementacion que ya se escribio."
    - "Criterio 3, quien verifica el ROJO: el executor NO puede (CODEX-RULES regla 2 le prohibe correr pytest, y la regla 5 le obliga a escribir `unknown` en el SUMMARY). El rojo lo ejecuta y lo registra el ORQUESTADOR entre la Task 1 y la Task 2. El gate determinista y obtenible del «antes» es el ORDEN DE COMMIT (`git log`), no una linea de texto en el SUMMARY. Precedente vivo: `01-04-SUMMARY` («Commit rojo 7f229e3, verde b456463») — el mecanismo ya existe y ya se uso en esta misma milestone."
    - "Criterio 3, los tres casos NOMBRADOS son obligatorios y literales: `extra` = .99, `omake` = .98, `12a` -> 12.1. No son ejemplos a parafrasear."
    - "`recognize_chapter` falla cerrado: un nombre sin numero devuelve `None`, jamas un 0.0 ni un 1.0 de relleno. Es el mismo criterio que `progress.next_progress` de la Fase 1 (decision: «next_progress falla cerrado ... un 0 de relleno pondria a cero el AniList real»). Un capitulo cuyo numero se invento es un capitulo que sube progreso inventado al AniList real."
    - "`LocalArchiveSource._chapter_number` DELEGA en el modulo nuevo. El motor deja de tener su propia regex de numeros: si quedan dos implementaciones, el criterio 3 es falso aunque el test nuevo este verde."
  artifacts:
    - "apps/backend/nyanko_api/chapter_recognition.py — el componente, puro, sin importar nada de `nyanko_api` salvo stdlib"
    - "apps/backend/tests/test_chapter_recognition.py — la tabla de casos parametrizada"
  key_links:
    - "`local_archive.py:124-128` — la unica llamada viva: `chapters()` decide entre el `Number` de ComicInfo (RD-08 manda) y el nombre del fichero. Ese reparto NO cambia; lo que cambia es a quien llama por debajo."
  prohibitions:
    - statement: "No queda ni una regex de numero de capitulo en `local_archive.py`. `_chapter_number` no vuelve a hacer `re.search` de digitos por su cuenta."
      status: pending
      verification: "Asercion de fuente: en `apps/backend/nyanko_api/sources/local_archive.py`, el cuerpo de `_chapter_number` contiene una llamada a `recognize_chapter` y no contiene `re.search`. `_DIGITS`/`_natural_key` (que son ORDENACION de paginas, no reconocimiento de capitulo) se quedan donde estan."
    - statement: "`recognize_chapter` no toca el disco, la BD ni la red, y no importa `pathlib`, `zipfile`, `httpx` ni `.database`."
      status: pending
      verification: "Asercion de fuente: `chapter_recognition.py` solo importa de stdlib (`re`, y `typing` si hace falta). El test lo ejercita sin `tmp_path`, sin fixtures y sin `TestClient`."
    - statement: "No se toca `conftest.py`, `pyproject.toml` ni `pytest.ini` (CODEX-RULES regla 3)."
      status: pending
      verification: "`git status` no lista esos ficheros."
---

<objective>
Sacar el reconocimiento de numero de capitulo de donde esta escondido y ponerle nombre, modulo y una
tabla de casos escrita antes que el codigo.

Purpose: LNK-03. Hoy el reconocimiento son tres lineas en `local_archive.py:242-244`
(`re.search(r"\d+(?:\.\d+)?", name)`) que nadie testea y que se equivocan en silencio: «Berserk 12»
devuelve 12 por suerte, «Vol.2 Cap 15» devuelve 2, y «Cap 12 extra» devuelve 12 — el mismo numero que
el capitulo 12 de verdad. En la Fase 5 ese numero se convierte en un `floor()` y sube al AniList real
del usuario. El criterio 3 no pide un refactor bonito: pide que el numero deje de ser un efecto
colateral de una regex que nadie ha mirado nunca.
Output: el modulo `chapter_recognition`, su tabla de casos en verde, y `LocalArchiveSource`
delegando en el.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@.planning/CODEX-RULES.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces (cuota de este plan)

| Simbolo/ruta | Fichero | Que es |
|---|---|---|
| `apps/backend/nyanko_api/chapter_recognition.py` | nuevo | El componente. Modulo puro, solo stdlib |
| `recognize_chapter` | `chapter_recognition.py` | `(name: str, series_title: str \| None = None) -> float \| None`. El unico simbolo publico |
| `apps/backend/tests/test_chapter_recognition.py` | nuevo | La tabla de casos, parametrizada. Se escribe ANTES que el modulo |
| `LocalArchiveSource._chapter_number` | `local_archive.py:242-244` | MODIFICADO: pasa de tener su propia regex a delegar en `recognize_chapter` |

> Nota de nombre (discrecion del planificador, declarada): el ROADMAP llama al componente
> `ChapterRecognition`. En el arbol se materializa como el modulo `chapter_recognition` con una
> funcion pura. Una clase sin estado con un solo metodo es una funcion con ceremonia, y este proyecto
> ya tiene escrito que el cargo cult no entra (REQUIREMENTS.md, «Out of Scope»). Lo que el criterio 3
> exige — nombre propio, modulo propio, pureza, testeable en unidad, tabla antes que codigo — lo
> cumple el modulo. Si el verificador quiere el simbolo literal, esta nota es el sitio donde discutirlo.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: La tabla de casos, en ROJO y antes que el codigo</name>
  <files>apps/backend/tests/test_chapter_recognition.py</files>

  <read_first>
    - `apps/backend/nyanko_api/sources/local_archive.py:242-244` — `_chapter_number`. Las tres lineas que este plan jubila. Miralas antes de escribir un caso: la tabla existe para describir lo que ESTO no hace.
    - `apps/backend/nyanko_api/sources/local_archive.py:106-132` — `chapters()`. Aqui se ve el reparto que NO cambia: si `ComicInfo.xml` trae `Number`, manda el (RD-08); si no, el nombre del fichero.
    - `apps/backend/nyanko_api/sources/local_archive.py:29-60` — `_ARCHIVE_MEMBER_BOUNDARY`, `_DIGITS` y `_natural_key`. Dos cosas: (a) el estilo de comentario de esta casa (en espanol, explica el POR QUE y cita el bug real, no el QUE); (b) `_natural_key` es ORDENACION de paginas y NO se toca — no es reconocimiento de capitulo.
    - `apps/backend/tests/test_manga_api.py:110-170` — el estilo de test de esta casa: nombres en espanol, `@pytest.mark.parametrize` donde hay tabla.
  </read_first>

  <action>
    Crear `apps/backend/tests/test_chapter_recognition.py` con la tabla de casos como
    `@pytest.mark.parametrize` sobre `recognize_chapter`, importado de
    `nyanko_api.chapter_recognition`. El modulo NO existe todavia: eso es el punto. El test debe
    fallar con `ModuleNotFoundError` / `ImportError`, y ese rojo hay que verlo antes de la Task 2.

    Casos OBLIGATORIOS de la tabla (entrada -> esperado), los tres primeros nombrados por el ROADMAP
    y literales:

    - `"Ch.12 extra"` -> `12.99` — el sufijo `extra` vale .99
    - `"Ch.12 omake"` -> `12.98` — el sufijo `omake` vale .98
    - `"12a"` -> `12.1` — sufijo alfabetico: la n-esima letra vale n/10, luego `a` -> .1
    - `"12b"` -> `12.2` — la misma regla, segunda letra
    - `"Ch.12 special"` -> `12.97` — completa la terna de sufijos nombrados
    - `"Cap 12"` -> `12.0` — el caso simple
    - `"Chapter 10.5"` -> `10.5` — el decimal explicito manda sobre cualquier sufijo
    - `"Cap 004"` -> `4.0` — ceros a la izquierda
    - `"Vol.2 Ch.15"` -> `15.0` — el volumen NO es el capitulo. Hoy `local_archive.py:242-244` devuelve `2.0` aqui: este caso es una correccion de bug, no una feature
    - `"Berserk 12"` con `series_title="Berserk"` -> `12.0` — el titulo de la serie se descuenta antes de buscar numeros
    - `"Blue Period 2"` con `series_title="Blue Period"` -> `2.0` — el titulo lleva un numero dentro (`Period`... no; el caso real es el titulo que contiene digitos). Usar `series_title="Mob Psycho 100"` y nombre `"Mob Psycho 100 5"` -> `5.0`: sin descontar el titulo, el `100` gana
    - `"Prologo"` -> `None` — sin numero NO se inventa numero
    - `""` -> `None` — cadena vacia
    - `"12z"` -> `12.0` — la letra 26 daria 2.6 y pisaria el capitulo 14; fuera de rango se ignora el sufijo (regla: solo letra unica y n/10 < 1.0)

    Cada caso lleva su `id` de parametrize en espanol y, los que corrigen un bug real
    (`Vol.2 Ch.15`, `Mob Psycho 100 5`, `Ch.12 extra`), un comentario de una linea diciendo que
    devuelve HOY `local_archive.py:242-244`. Un caso sin ese comentario es un caso que nadie sabe si
    protege algo.

    Anadir tambien un test que afirme la pureza: `recognize_chapter` no recibe ni devuelve rutas, y
    el modulo de test no usa `tmp_path`, ni `TestClient`, ni fixtures de BD.
  </action>

  <verify>
    <automated>cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_chapter_recognition.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Asercion de fuente (lo unico que el executor puede cerrar por si mismo): `tests/test_chapter_recognition.py` existe y contiene los 14 casos listados, con los tres nombrados por el ROADMAP exactos: `12.99` para `extra`, `12.98` para `omake`, `12.1` para `12a`.
    - Asercion de fuente: el fichero no importa `pathlib`, `tmp_path`, `TestClient` ni `Database`.
    - Asercion de fuente: `nyanko_api/chapter_recognition.py` NO existe todavia al terminar esta task. Esa ausencia ES el rojo, y es comprobable sin ejecutar nada.
    - Asercion de fuente: los casos `Vol.2 Ch.15`, `Mob Psycho 100 5` y `Ch.12 extra` llevan comentario con lo que devuelve hoy la implementacion vieja.
    - DUENO DEL ROJO — ORQUESTADOR, no executor: entre esta task y la Task 2, el orquestador ejecuta `cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_chapter_recognition.py -q` FUERA del sandbox, comprueba que falla con `ModuleNotFoundError: No module named 'nyanko_api.chapter_recognition'` (o `ImportError` sobre `recognize_chapter`), y commitea el rojo. El executor escribe `unknown` en el SUMMARY (CODEX-RULES reglas 2 y 5) y no afirma ningun resultado de tests. El `<automated>` de arriba es EL COMANDO DEL ORQUESTADOR y aqui se espera ROJO, no verde: es el unico sitio de la fase donde un exit != 0 es el resultado correcto.
    - Gate determinista del «antes» (lo cierra el orquestador con `git log`, no con prosa): `git log --oneline --diff-filter=A -- apps/backend/tests/test_chapter_recognition.py apps/backend/nyanko_api/chapter_recognition.py` lista el commit del TEST antes que el del MODULO. Ese orden es el criterio 3; una frase en el SUMMARY no lo es.
  </acceptance_criteria>

  <done>La tabla existe en el arbol, el modulo NO existe, y el orquestador puede commitear el rojo y verlo en `git log` antes del verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: El componente — que la tabla se ponga verde</name>
  <files>apps/backend/nyanko_api/chapter_recognition.py</files>

  <read_first>
    - `apps/backend/tests/test_chapter_recognition.py` — la tabla de la Task 1. Es la especificacion; si un caso incomoda, se discute, no se cambia el test para que pase.
    - `apps/backend/nyanko_api/sources/local_archive.py:29-51` — `_ARCHIVE_MEMBER_BOUNDARY`. El precedente de esta casa para una regex con `re.IGNORECASE | re.ASCII` y su comentario explicando por que `.lower()` es una trampa (`'I'.lower()` puede alargar la cadena). El mismo cuidado aplica aqui.
    - `apps/backend/nyanko_api/normalizer.py` — `normalize_title`. Ya existe y es lo que usa `matcher.py` para comparar titulos. NO se usa aqui: `recognize_chapter` es puro y de stdlib, y `normalize_title` arrastra dependencias. Miralo para no reimplementarlo por accidente ni importarlo.
    - `apps/backend/nyanko_api/progress.py` — `next_progress`. El precedente de «falla cerrado»: sin valor bueno devuelve `None`, no un 0 de relleno.
  </read_first>

  <action>
    Crear `apps/backend/nyanko_api/chapter_recognition.py` con `recognize_chapter(name, series_title=None)
    -> float | None`, puro y solo con `re` de stdlib. Algoritmo (es el de Mihon `ChapterRecognition`,
    que es el precedente publico de este problema exacto; se escribe aqui como dato, no se copia
    codigo Kotlin):

    1. `name.casefold()`. Si `series_title` viene, quitar su forma `casefold()` del nombre y hacer
       `strip()`. Ese paso es lo que hace que `"Mob Psycho 100 5"` con serie `"Mob Psycho 100"` de
       `5.0` y no `100.0`.
    2. Sustituir `,` y `-` por `.`.
    3. Quitar el espacio que precede a `extra`, `special` u `omake`, para que el sufijo quede pegado
       al numero y lo capture el mismo match.
    4. Quitar los marcadores de volumen/temporada: `v`, `ver`, `vol`, `version`, `volume`, `season`,
       `s` seguidos de un separador opcional no alfabetico y digitos. Eso es lo que resuelve
       `"Vol.2 Ch.15"` -> `15.0`.
    5. Caso base: si hay un `ch.` (o `cap.`/`capitulo`, que es lo que produce esta biblioteca en
       espanol), tomar el numero que le sigue.
    6. Si no, y hay EXACTAMENTE UNA aparicion de numero en el nombre, tomarla. Dos o mas sin `ch.`
       es ambiguo -> `None`. Preferir devolver `None` a acertar por suerte.
    7. Del match, componer: parte entera + decimal explicito si lo hay; si no hay decimal pero hay
       sufijo alfabetico, aplicar `extra` -> .99, `omake` -> .98, `special` -> .97, y si es una sola
       letra `a`..`i`, su posicion / 10 (`a` -> .1, `b` -> .2). Una letra que diera >= 1.0 (de la `j`
       en adelante) se ignora: sumar 2.6 al 12 pisaria el capitulo 14, que es un capitulo real de
       otra gente.
    8. Sin numero -> `None`.

    Las tres constantes de sufijo (.99/.98/.97) van con comentario en espanol explicando que el
    numero no es arbitrario: es el orden — un extra va DESPUES del capitulo 12 y ANTES del 13, y
    `.99` es lo que lo coloca ahi sin colisionar con un `12.5` real que si existe.

    `re.ASCII` en los patrones de letra, por el mismo motivo que
    `local_archive.py:29-41` documenta.
  </action>

  <verify>
    <automated>cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_chapter_recognition.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Asercion de comportamiento: los 14 casos de la tabla pasan. `recognize_chapter("Ch.12 extra")` devuelve `12.99`, `recognize_chapter("Ch.12 omake")` devuelve `12.98`, `recognize_chapter("12a")` devuelve `12.1`.
    - Asercion de comportamiento: `recognize_chapter("Prologo")` devuelve `None` y `recognize_chapter("")` devuelve `None`. Ningun caso devuelve `0.0` en lugar de `None`.
    - Asercion de comportamiento: `recognize_chapter("Mob Psycho 100 5", series_title="Mob Psycho 100")` devuelve `5.0`.
    - Asercion de fuente: `chapter_recognition.py` no contiene `import pathlib`, `import zipfile`, `import httpx`, `from .database`, `from .models` ni `from .normalizer`.
    - Asercion de fuente: las constantes .99/.98/.97 llevan comentario en espanol explicando el criterio de ORDEN.
    - Comando: `cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_chapter_recognition.py -q` sale 0.
  </acceptance_criteria>

  <done>La tabla escrita en la Task 1 esta en verde sin haber tocado ni un caso.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: El motor delega — que no queden dos implementaciones</name>
  <files>apps/backend/nyanko_api/sources/local_archive.py</files>

  <read_first>
    - `apps/backend/nyanko_api/sources/local_archive.py:242-244` — `_chapter_number`, el metodo a vaciar.
    - `apps/backend/nyanko_api/sources/local_archive.py:106-132` — `chapters()`: las dos llamadas a `_chapter_number` (linea 125 con el `Number` de ComicInfo, linea 127 con `path.name`). RD-08 manda: si ComicInfo trae `Number`, gana. Ese reparto se conserva TAL CUAL.
    - `apps/backend/nyanko_api/sources/local_archive.py:23-27` — `ARCHIVE_EXTENSIONS` / `UNSUPPORTED_ARCHIVE_EXTENSIONS`. Relevante para el paso a `path.stem`.
    - `apps/backend/tests/test_local_archive.py` (si existe) y `apps/backend/tests/test_manga_api.py:110-170` — los tests vivos de `chapters()`. Si alguno afirma un `number`, este cambio los toca y este plan es su dueno.
  </read_first>

  <action>
    En `local_archive.py`: importar `recognize_chapter` de `..chapter_recognition` y dejar
    `_chapter_number` como una delegacion. Se borra la regex local; no se deja una «por si acaso».

    Dos precisiones concretas del cableado:

    - En la llamada del nombre de fichero (`local_archive.py:127`), pasar `path.stem` cuando `path`
      es un fichero y `path.name` cuando es un directorio. La extension no es parte del numero:
      `"Cap 12a.cbz"` con `.name` obliga al componente a analizar `.cbz` como sufijo alfabetico. El
      `is_dir()` ya se ha calculado unas lineas antes (`local_archive.py:111`) — reusar esa variable,
      no volver a tocar el disco.
    - Pasar `series_title` = el nombre de la carpeta de la serie (`series_path.name`), que es lo que
      hace que `"Berserk 12"` dentro de la carpeta `Berserk` de `12.0` y no dependa de la suerte.

    La llamada del `Number` de ComicInfo (`local_archive.py:125`) tambien pasa por
    `recognize_chapter`, sin `series_title`: `Number` puede venir como `"12.5"` o como `"12a"`, y
    tener DOS parsers para el mismo concepto es justo lo que el criterio 3 prohibe.

    Si un test vivo de `chapters()` afirma un `number` que cambia (p. ej. una serie con `Vol` en el
    nombre), este plan lo posee: se actualiza la asercion y se comenta en el mismo diff por que el
    valor viejo era el bug.
  </action>

  <verify>
    <automated>cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_chapter_recognition.py tests/test_manga_api.py tests/test_source_api.py -q</automated>
  </verify>

  <acceptance_criteria>
    - Asercion de fuente: el cuerpo de `_chapter_number` en `apps/backend/nyanko_api/sources/local_archive.py` llama a `recognize_chapter` y no contiene `re.search`.
    - Asercion de fuente: `local_archive.py` importa `recognize_chapter` desde `..chapter_recognition`. `_natural_key` y `_DIGITS` siguen intactos (son ordenacion de paginas, no reconocimiento de capitulo).
    - Asercion de comportamiento: un CBZ llamado `Cap 12a.cbz` en la carpeta `Berserk` produce `MangaChapter.number == 12.1` (no `12.0`, ni un fallo por analizar `.cbz` como sufijo).
    - Asercion de comportamiento: un `ComicInfo.xml` con `<Number>12.5</Number>` sigue mandando sobre el nombre del fichero (RD-08 intacto).
    - Asercion de comportamiento: una serie cuyo capitulo se llama `Vol.2 Cap 15.cbz` produce `number == 15.0`. Antes de este plan producia `2.0`.
    - Comando: `cd apps/backend; .venv/Scripts/python.exe -m pytest tests/test_chapter_recognition.py tests/test_manga_api.py tests/test_source_api.py -q` sale 0.
  </acceptance_criteria>

  <done>Existe UNA implementacion del reconocimiento de capitulo en el arbol, tiene nombre y tiene tabla.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nombre de fichero del disco -> `recognize_chapter` | El nombre lo controla el usuario, pero tambien un CBZ descargado de un tercero (Fase 8). Es entrada no confiable que acaba, via Fase 5, en una escritura al AniList real. |
| `ComicInfo.xml` -> `recognize_chapter` | XML dentro de un archivo de terceros. Ya esta acotado a 1 MB (`COMIC_INFO_MAX_BYTES`, `local_archive.py:27`) y parseado por la Fase 3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-01 | Tampering | `recognize_chapter` -> numero -> (Fase 5) progreso en AniList | high | mitigate | Falla cerrado: sin numero reconocible devuelve `None`, nunca un valor de relleno. Dos numeros ambiguos sin `ch.` tambien devuelven `None`. Casos `"Prologo"` y `""` en la tabla. |
| T-04-01-02 | Denial of Service | regex sobre nombre de fichero | low | accept | Los patrones no tienen anidamiento con cuantificadores solapados (sin backtracking catastrofico), y la entrada esta acotada por `MAX_PATH` de Windows. Un nombre de fichero no es una entrada de red. |
| T-04-01-03 | Tampering | `re` + `casefold()` sobre nombres no-ASCII | medium | mitigate | `re.ASCII` en los patrones de letra, por el precedente medido en `local_archive.py:29-41` (`'I'.lower()` son DOS caracteres y desplaza el corte). |
| T-04-01-SC | Tampering | npm/pip/cargo installs | low | accept | Este plan no instala ni un paquete: `re` es stdlib. No hay superficie de cadena de suministro y por tanto no hay tabla de legitimidad que exigir. |
</threat_model>

<verification>
- `cd apps/backend; .venv/Scripts/python.exe -m pytest -q` — la suite entera (461 passed antes de este plan) sigue verde, con los casos nuevos sumados.
- `cd apps/backend; .venv/Scripts/python.exe -m ruff check nyanko_api/chapter_recognition.py tests/test_chapter_recognition.py`
- EL ROJO DE LA TASK 1 LO CORRE Y LO COMMITEA EL ORQUESTADOR, entre la Task 1 y la Task 2, fuera del sandbox del executor (CODEX-RULES regla 2: el executor tiene prohibido correr pytest; regla 5: escribe `unknown`). Sin ese paso, el criterio 3 no esta verificado — pero el sitio donde se comprueba NO es el SUMMARY.
- GATE DEL CRITERIO 3, determinista y obtenible: `git log --oneline --diff-filter=A -- apps/backend/tests/test_chapter_recognition.py apps/backend/nyanko_api/chapter_recognition.py` muestra el commit del test ANTES del commit del modulo. El orden de commit es un hecho de `git`, no una afirmacion de nadie. Precedente: `01-04-SUMMARY` («Commit rojo 7f229e3, verde b456463»).
</verification>

<success_criteria>
- Criterio 3 del ROADMAP verdadero: componente propio (`nyanko_api/chapter_recognition.py`), puro (solo stdlib), unitariamente testeable (la tabla lo llama directo), con `extra` = .99, `omake` = .98 y `12a` -> 12.1 en verde.
- El «antes que el codigo» del criterio 3 verificado por ORDEN DE COMMIT en `git log`, con el rojo ejecutado y registrado por el orquestador — no por una frase en el SUMMARY que el executor no puede sostener.
- Una sola implementacion del reconocimiento en el arbol: `local_archive.py` delega.
</success_criteria>

<output>
Create `.planning/phases/04-identidad-y-v-nculo-fuente-entrada-del-tracker/04-01-SUMMARY.md` when done
</output>
