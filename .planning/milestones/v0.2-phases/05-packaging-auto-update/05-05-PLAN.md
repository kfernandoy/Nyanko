---
phase: 05-packaging-auto-update
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/desktop/electron/main/compat-paths.ts
  - apps/desktop/electron/main/compat-paths.test.ts
  - apps/desktop/electron/main/index.ts
  - apps/desktop/electron/main/tray.ts
autonomous: true
requirements: [PKG-01]
user_setup: []

must_haves:
  truths:
    - "`iconPath()` resuelve a `resources/icon.png` bajo empaquetado y a `build/icon.png` en dev, con self-check en las dos ramas."
    - "Ni `index.ts` ni `tray.ts` conservan la ruta colgante a `build/icon.png` (que en el NSIS no existe)."
  artifacts:
    - apps/desktop/electron/main/compat-paths.ts
  key_links:
    - "`extraResources` del Plan 01 (`build/icon.png` → `resources/icon.png`) → `iconPath()`: sin esta función el icono empaquetado sigue colgando y la bandeja sale vacía sin error ni log."
---

<objective>
Cerrar el riesgo diferido de la Fase 4: `index.ts:40` y `tray.ts:69` resuelven el icono contra
`apps/desktop/build/`, que es el `buildResources` de electron-builder y **NO se empaqueta dentro
de la app**. En el NSIS ambas rutas cuelgan y la bandeja sale sin icono, sin error y sin log.

Purpose: PKG-01 (el paquete tiene que salir con su icono). Es un plan de una sola tarea, separado
del updater (Plan 02) porque no comparte con él ni un fichero ni una causa: son dos bugs distintos
que solo coinciden en que ambos se manifiestan al empaquetar.
Output: `iconPath()` en `compat-paths.ts` (+ self-check) y los dos call sites recableados.

**Verificación visual.** El self-check automático prueba la resolución de rutas. La confirmación
de que el gatito SALE en la bandeja del NSIS instalado la hace el checkpoint del **Plan 02**
(wave 3), que ya instala el paquete — no se monta una segunda instalación solo para mirar un
icono.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-auto-update/05-CONTEXT.md
@.planning/phases/05-packaging-auto-update/05-PATTERNS.md
@.planning/phases/05-packaging-auto-update/05-01-PLAN.md
</context>

<tasks>

<!-- planner-discipline-allow: build/icon.png -->
<!-- El literal aparece a propósito en <action>: es el cuerpo de la rama dev de iconPath(), que se
     escribe en compat-paths.ts. El gate negativo solo lo prohíbe en index.ts y tray.ts. -->

<task type="auto" tdd="true">
  <name>Task 1: `iconPath()` — el icono se resuelve por `process.resourcesPath` bajo empaquetado</name>
  <read_first>
    - apps/desktop/electron/main/compat-paths.ts (24 líneas; ELECTRON-FREE a propósito — su self-check corre bajo Node plano, así que la función nueva recibe `isPackaged` como parámetro, nunca importa `app`)
    - apps/desktop/electron/main/compat-paths.test.ts (forma de los asserts a extender)
    - apps/desktop/electron/main/sidecar.ts (líneas 35-39: `resolveSidecarExe()` — el patrón canónico de resolución bajo empaquetado; se copia su forma, no se inventa otra)
    - apps/desktop/electron/main/index.ts (línea 40: `icon: join(__dirname, "../../build/icon.png")`)
    - apps/desktop/electron/main/tray.ts (línea 69: `nativeImage.createFromPath(join(__dirname, "../../build/icon.png"))`)
    - .planning/phases/05-packaging-auto-update/05-01-PLAN.md §Tarea 3 (`extraResources` ya copia `build/icon.png` → `resources/icon.png`)
    - .planning/phases/04-native-feature-parity/04-VERIFICATION.md (frontmatter `deferred:` — este es el riesgo que se cierra)
  </read_first>
  <behavior>
    - `iconPath(true, "C:\\...\\resources", cualquierDir)` → la ruta apunta a `icon.png` dentro de ese `resources`.
    - `iconPath(false, cualquierResources, mainDir)` → la ruta apunta al `build/icon.png` del repo, relativa a `mainDir`.
    - `compat-paths.ts` sigue sin importar nada de `electron` (su test corre bajo Node plano).
  </behavior>
  <action>
    Añadir a `compat-paths.ts` un helper PURO (el módulo se mantiene Electron-free — su test corre
    bajo Node plano sin mockear electron):
    `iconPath(isPackaged: boolean, resourcesPath: string, mainDir: string): string`
    → empaquetado: `join(resourcesPath, "icon.png")`; dev: `join(mainDir, "../../build/icon.png")`.
    Es el mismo patrón que `resolveSidecarExe()`, con la única desviación de que el icono se
    necesita en AMBOS caminos (dev y prod), de ahí la rama por `isPackaged` — dejarlo dicho en el
    comentario, citando D-07 y la discreción de CONTEXT.md.

    Extender `compat-paths.test.ts` con los dos asserts de `<behavior>`.

    Cambiar los dos call sites a `iconPath(app.isPackaged, process.resourcesPath, __dirname)`
    (`tray.ts` ya importa `app`; `index.ts` también). Borrar los `join(...)` colgantes y sus
    comentarios ya obsoletos.
  </action>
  <verify>
    <automated>cd apps/desktop && npm run test:datadir && npx tsc --noEmit && node -e "const c=require('fs').readFileSync('electron/main/compat-paths.ts','utf8'); const a=[]; if(!/export function iconPath/.test(c))a.push('falta iconPath'); if(/from \"electron\"/.test(c))a.push('compat-paths dejo de ser electron-free'); for(const f of ['electron/main/index.ts','electron/main/tray.ts']){const s=require('fs').readFileSync(f,'utf8'); if(!/iconPath\(/.test(s))a.push(f+' no usa iconPath'); if(/build\/icon\.png/.test(s))a.push(f+' sigue con la ruta colgante');} if(a.length){console.error(a.join('\n'));process.exit(1)} console.log('ICON OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `compat-paths.ts` exporta `iconPath(isPackaged, resourcesPath, mainDir)` y sigue sin
      importar nada de `electron` (el módulo es Electron-free por contrato).
    - `npm run test:datadir` pasa con los dos asserts nuevos (rama empaquetada y rama dev).
    - Ni `index.ts` ni `tray.ts` contienen ya una ruta literal al PNG bajo `build`; ambos llaman a
      `iconPath(app.isPackaged, process.resourcesPath, __dirname)`.
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>
  <done>El icono se resuelve por `process.resourcesPath` en el paquete y por el repo en dev, con el mismo mecanismo que ya usa el sidecar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paquete NSIS → proceso main | El main lee un asset (`icon.png`) de una ruta derivada de `process.resourcesPath`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-18 | Tampering | `iconPath()` → `nativeImage.createFromPath` | low | accept | La ruta se compone de `process.resourcesPath` (que fija Electron, no el usuario) + un literal. No entra nada del renderer ni de la red. Un atacante con escritura en el directorio de instalación ya puede reemplazar el propio `Nyanko.exe`: el icono no añade superficie. Aceptado. |
| T-05-19 | Denial of Service | icono ausente en el paquete | low | mitigate | `nativeImage.createFromPath` sobre una ruta inexistente devuelve una imagen VACÍA sin lanzar: el fallo es silencioso, que es exactamente por qué llevaba una fase entero sin detectarse. Mitigación: el self-check de rutas (Tarea 1) + la comprobación de que `resources/icon.png` está en el paquete (Plan 01, Tarea 3) + la confirmación visual del checkpoint del Plan 02. |
</threat_model>

<artifacts_this_phase_produces>
| Artefacto | Tipo | Detalle |
|---|---|---|
| `iconPath()` en `compat-paths.ts` | helper puro (nuevo) | `(isPackaged: boolean, resourcesPath: string, mainDir: string) => string` |
</artifacts_this_phase_produces>

<verification>
1. `cd apps/desktop && npm run test:datadir` → los dos asserts de `iconPath` pasan.
2. `npx tsc --noEmit` → 0.
3. Confirmación visual sobre el NSIS instalado: checkpoint del Plan 02 (wave 3).
</verification>

<success_criteria>
- El riesgo diferido de la Fase 4 (icono no empaquetado) queda cerrado y con self-check.
- `compat-paths.ts` sigue siendo Electron-free.
</success_criteria>

<output>
Create `.planning/phases/05-packaging-auto-update/05-05-SUMMARY.md` when done
</output>
