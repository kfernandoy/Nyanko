---
phase: 05-packaging-auto-update
plan: 04
type: execute
wave: 5
depends_on: ["05-01", "05-02", "05-03", "05-05"]
files_modified:
  - apps/desktop/scripts/publish-bridge.mjs
  - apps/desktop/scripts/publish-bridge.test.mjs
  - apps/desktop/package.json
  - docs/extra/RELEASING.md
autonomous: false
requirements: [PKG-01, PKG-02]

user_setup:
  - service: github
    why: "electron-builder sube el instalador y `latest.yml` al release, y el puente sube `latest.json` + la firma. Ninguno de los dos usa el CLI `gh` (no está instalado): van por la API REST con un token."
    env_vars:
      - name: GH_TOKEN
        source: "GitHub → Settings → Developer settings → Personal access tokens → Fine-grained, con permiso Contents: Read and write sobre kfernandoy/Nyanko"

must_haves:
  truths:
    - "El release v0.2.0 lleva CUATRO artefactos: `Nyanko-Setup-0.2.0.exe`, `latest.yml` (electron-updater), `Nyanko-Setup-0.2.0.exe.sig` y `latest.json` (updater Tauri de 0.1.15)."
    - "La firma minisign de 0.2.0 verifica contra la MISMA pubkey que está horneada en los binarios 0.1.15 que ya están en el campo (D-01)."
    - "Una instalación real de 0.1.15 encuentra el update, lo descarga, verifica la firma y aterriza en Nyanko 0.2.0 (Electron) con la biblioteca intacta — con o sin asistente por medio (ver `<flags_del_updater_tauri>`)."
    - "El puente NO se publica si la firma no verifica: el script aborta antes de subir nada."
  artifacts:
    - apps/desktop/scripts/publish-bridge.mjs
    - apps/desktop/scripts/publish-bridge.test.mjs
  key_links:
    - "`~/.tauri/nyanko-updater.key` → firma → pubkey horneada en 0.1.15: si la firma no verifica contra ESA pubkey, TODO el parque instalado queda varado para siempre."
    - "`latest.json.signature` = el CONTENIDO del `.sig` (no una ruta, no un hash) — el error clásico de este flujo."
    - "`latest.json.url` → el `browser_download_url` del .exe que electron-builder acaba de subir: firmar un fichero y apuntar a otro es indetectable hasta que falla en el cliente."
---

<objective>
Publicar 0.2.0 y tender el puente de un solo uso que saca a los usuarios de Tauri 0.1.15 hacia
Electron sin que hagan nada: firmar el instalador de electron-builder con la MISMA clave minisign
que 0.1.15 verifica, y publicar un último `latest.json` en el esquema de Tauri junto al
`latest.yml` de electron-updater.

Purpose: D-01 + D-08 + PKG-02 (canal de publicación). Si se publicara solo `latest.yml`, cada
instalación 0.1.15 del mundo seguiría sondeando `latest.json`, no vería nunca una versión nueva,
y quedaría varada para siempre — sin ningún error visible para el usuario ni para nosotros.
Output: `apps/desktop/scripts/publish-bridge.mjs` (+ self-check), el script `dist:publish`,
`docs/extra/RELEASING.md` reescrito para el flujo Electron, y el release v0.2.0 publicado y
verificado desde una instalación 0.1.15 real.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-auto-update/05-CONTEXT.md
@.planning/phases/05-packaging-auto-update/05-PATTERNS.md
@.planning/phases/05-packaging-auto-update/05-01-PLAN.md
</context>

<hechos_ya_verificados>
Comprobado en esta máquina el 2026-07-11 — el ejecutor NO tiene que re-descubrirlo:

1. **La clave existe y es LA clave.** `~/.tauri/nyanko-updater.key` (348 B, sin contraseña) y
   `~/.tauri/nyanko-updater.key.pub` (152 B). El contenido del `.pub` es **byte a byte el mismo
   string base64** que `plugins.updater.pubkey` de `tauri.conf.json` en 0.1.15
   (`a50659c^`), es decir la pubkey horneada en cada binario que hay hoy en el campo. Decodifica a:
   `untrusted comment: minisign public key: BACAB6947028F6D4` + la línea
   `RWTU9ihwlLbKuoo7p70uAhks5Yh2W+JuNrCVmT2UtcWcwsBRuDXmS8aA`.
   → Firmar con esa clave produce, por construcción, firmas que 0.1.15 acepta.

2. **No hay binario `minisign` instalado. No hay CLI `gh` instalado.** Node es v24.
3. El CLI de Tauri (que era `@tauri-apps/cli@^2.8.4` antes de que la Fase 3 lo eliminara) tiene
   el subcomando `signer sign` y NO necesita toolchain de Rust: se distribuye como binario
   preconstruido en npm.

**Restricción dura: `@tauri-apps/cli` NO vuelve a `package.json`.** SHELL-02 («se eliminan las
deps `@tauri-apps/*`») está Completo y no se regresa. El puente es de un solo uso (D-01, CONTEXT
§specifics): se invoca vía `npx` con la versión PINEADA, y muere con él.
</hechos_ya_verificados>

<formato_minisign>
Lo que el ejecutor necesita saber para no adivinar (y no hay que re-derivar):

- El fichero `.pub` de Tauri es **base64 del fichero .pub de minisign** (2 líneas: comentario +
  clave). La línea 2, decodificada de base64, son 42 bytes: 2 de algoritmo, 8 de key id, 32 de
  clave pública Ed25519.
- El fichero `.sig` que produce `tauri signer sign` es **base64 del fichero .sig de minisign**
  (4 líneas). La línea 2, decodificada, son 74 bytes: 2 de algoritmo (`Ed` = legacy, firma el
  contenido; `ED` = prehashed, firma el BLAKE2b-512 del contenido), 8 de key id, 64 de firma.
- El campo `signature` de `latest.json` es **el CONTENIDO del fichero `.sig`** (ese base64 de una
  línea), no una ruta y no un hash. Es el error clásico de este flujo, y está escrito así en
  `docs/extra/RELEASING.md:41`.
- `node:crypto` de Node 24 verifica Ed25519 sin dependencias: envolver los 32 bytes crudos en
  SPKI (prefijo DER `302a300506032b6570032100` + los 32 bytes) → `crypto.createPublicKey` →
  `crypto.verify(null, mensaje, clave, firma)`. El hash BLAKE2b-512 también está en `node:crypto`
  (`createHash("blake2b512")`).
</formato_minisign>

<flags_del_updater_tauri>
**Lo que D-01 SÍ garantiza y lo que NO.** El updater de Tauri descarga el NSIS, verifica la firma
minisign y lo EJECUTA. Eso es indiferente al framework que generó el NSIS: la parte de D-01 que
importa (los 0.1.15 ven el update y acaban corriendo nuestro instalador) es sólida.

Lo que NO está garantizado es la EXPERIENCIA de esa instalación. `tauri.conf.json` en `a50659c^`
declara `plugins.updater` con solo `endpoints` + `pubkey`: sin `windows.installMode`, Tauri v2 usa
el modo **passive** por defecto e invoca el instalador con SUS flags (los de la familia
passive/restart del NSIS que genera Tauri). El NSIS asistido de electron-builder (`oneClick: false`,
D-03) implementa otros flags (`/S`, `/D=`, `/currentuser`); los que no reconoce, NSIS simplemente
los ignora.

**Resultado realista, y es aceptable:** al usuario de 0.1.15 le aparece el **asistente completo**
(selector de idioma + EULA + directorio) en vez de una barra de progreso, y puede que la app **no
se relance sola** al terminar. La migración se completa igual — solo que con un par de clics.
Lo que NO puede pasar es que el instalador falle o que la biblioteca se pierda.

Este plan **no asume** el camino silencioso: el checkpoint de la Tarea 3 registra cuál de los dos
ocurrió, y ambos son APROBABLES. El camino de escape, si sale mal de verdad (el instalador ni
siquiera arranca), está en el paso 5-bis de esa tarea: las notas de release llevan la instrucción
de descarga manual, y esas notas las ve el usuario 0.1.15 en el diálogo de confirmación del propio
updater de Tauri, ANTES de aceptar.
</flags_del_updater_tauri>

<tasks>

<task type="auto">
  <name>Task 1: `publish-bridge.mjs` — firmar, VERIFICAR y subir el puente (D-01), + RELEASING.md</name>
  <read_first>
    - docs/extra/RELEASING.md (las 99 líneas; el esquema de `latest.json` está en las líneas 41-55 y el flujo manual de Tauri en 15-60 — es el precedente PROCEDIMENTAL exacto que este script automatiza)
    - apps/desktop/package.json (tras el Plan 01: `version: 0.2.0`, script `dist` con `--publish never`)
    - apps/desktop/electron-builder.yml (Plan 01: `artifactName: Nyanko-Setup-${version}.${ext}`, `directories.output: release`, bloque `publish` github/kfernandoy/Nyanko)
    - apps/desktop/electron/main/compat-paths.test.ts (la forma de self-check del repo: `node:test` + `node:assert/strict`, asserts sobre helpers PUROS)
    - .planning/phases/05-packaging-auto-update/05-PATTERNS.md §`scripts/publish-bridge.mjs` (no hay analog en el árbol; el precedente es el procedimiento de RELEASING.md)
  </read_first>
  <action>
    **`apps/desktop/scripts/publish-bridge.mjs`** (ESM, Node puro, cero dependencias nuevas en
    `package.json`). Comentarios en español citando D-01/D-08; el script y las notas de release,
    en inglés (convención del repo desde 2026-07-06). Se ejecuta DESPUÉS de que electron-builder
    haya subido el `.exe` y el `latest.yml`, y lee la versión de `apps/desktop/package.json`.

    Constante en cabecera: `EMBEDDED_PUBKEY_B64`, con el string base64 EXACTO que está horneado en
    los binarios 0.1.15 (el que aparece en `<hechos_ya_verificados>`). No se lee del disco: es el
    ancla de confianza, y tenerlo literal en el script es lo que permite detectar que alguien
    firmó con otra clave.

    Estructura — **helpers puros arriba (testables bajo Node plano), efectos abajo**, igual que
    hacen los módulos del main:

    Puros:
    - `parseMinisignKey(pubB64)` → `{ algorithm, keyId, publicKey }` (decodifica el base64 externo,
      toma la línea 2, decodifica, corta 2/8/32).
    - `parseMinisignSig(sigB64)` → `{ algorithm, keyId, signature }` (mismo desglose, 2/8/64).
    - `verifyMinisign(fileBytes, sigB64, pubB64)` → `boolean`. Falla si los key id no coinciden.
      Elige el mensaje según el algoritmo del `.sig`: prehashed (`ED`) → BLAKE2b-512 del fichero;
      legacy (`Ed`) → el fichero. Verifica Ed25519 con `node:crypto` (envoltorio SPKI descrito en
      `<formato_minisign>`). No verifica la firma global del trusted comment: lo que protege el
      binario es la firma del payload. Dejarlo dicho con un comentario `ponytail:`.
    - `buildLatestJson({ version, notes, signature, url })` → el objeto EXACTO del esquema Tauri
      de `RELEASING.md:43-55` (`version`, `notes`, `pub_date` ISO, `platforms["windows-x86_64"]`
      con `signature` y `url`). `signature` es el CONTENIDO del `.sig`.

    Efectos, en este orden y abortando (exit != 0) en cuanto algo no cuadre:
    1. Exigir `GH_TOKEN` en el entorno. **Nunca imprimirlo, ni imprimir el contenido de la clave
       privada** (T-05-13).
    2. Comprobar que existe `apps/desktop/release/Nyanko-Setup-<version>.exe`.
    3. Leer `~/.tauri/nyanko-updater.key.pub` y **abortar si no es idéntico a
       `EMBEDDED_PUBKEY_B64`**. Es el chequeo que impide firmar con una clave equivocada, que es
       el fallo irrecuperable (varía a todo el parque instalado y no se nota hasta que un usuario
       se queja).
    4. Firmar: `npx --yes @tauri-apps/cli@2.8.4 signer sign -f <ruta de la clave> --password ""
       <ruta del exe>` (versión PINEADA — ver T-05-SC-2). Produce `<exe>.sig`.
    5. **Verificar la firma recién creada con `verifyMinisign()` contra `EMBEDDED_PUBKEY_B64`.
       Si no verifica, abortar SIN subir nada.** Una firma mala es indistinguible de una buena
       hasta que falla silenciosamente en cada cliente.
    6. Localizar el release por tag: `GET /repos/kfernandoy/Nyanko/releases` autenticado (este
       endpoint SÍ devuelve los borradores; `GET /releases/tags/...` no) y quedarse con el de
       `tag_name === "v" + version`. electron-builder lo habrá creado como BORRADOR — subir ahí
       es justo lo que queremos: el release se publica al final con los cuatro artefactos ya
       dentro, sin ventana de tiempo en la que `releases/latest/download/latest.json` devuelva 404
       a los clientes 0.1.15.
    7. Subir `Nyanko-Setup-<version>.exe.sig` y `latest.json` a ese release (POST al `upload_url`,
       `Content-Type: application/octet-stream`, `Authorization: Bearer`). Si ya existe un asset
       con ese nombre, borrarlo antes (DELETE) para que re-ejecutar el script sea idempotente.
       La `url` de `latest.json` es el `browser_download_url` del `.exe` que devuelve la API para
       ESE release — no una URL compuesta a mano.
    8. Imprimir un resumen: versión, los cuatro assets del release, y el recordatorio de publicar
       el borrador.

    Las notas de release salen de `process.argv[2]`; si falta, `Nyanko <version>`. Van al
    `latest.json`, y el updater de Tauri se las ENSEÑA al usuario de 0.1.15 en el diálogo de
    confirmación, antes de que acepte: es el único canal que tenemos hacia el parque instalado.
    Por eso las notas de v0.2.0 deben incluir, en inglés, (a) que esta versión cambia el motor a
    Electron y el instalador puede pedir unos clics, y (b) el enlace de descarga manual del
    instalador por si el proceso automático falla. Es el camino de escape de D-01
    (ver `<flags_del_updater_tauri>`), y cuesta una frase.

    **`apps/desktop/scripts/publish-bridge.test.mjs`** (self-check, `node:test` + `node:assert/strict`):
    - `buildLatestJson()` produce las claves del esquema Tauri y mete el contenido del `.sig` en
      `platforms["windows-x86_64"].signature`.
    - Round-trip de `verifyMinisign()`: generar un par Ed25519 con `crypto.generateKeyPairSync`,
      componer a mano un `.pub` y un `.sig` minisign válidos (prehashed) sobre un buffer de
      prueba, y comprobar que verifica → `true`. Alterar un byte del buffer → `false`. Alterar el
      key id → `false`.
    - `EMBEDDED_PUBKEY_B64` decodifica a un fichero de clave pública minisign de 2 líneas.
    Registrar el script `test:publish` en `apps/desktop/package.json`, junto a los `test:*` que ya
    existen: `node --test scripts/publish-bridge.test.mjs`.

    **`apps/desktop/package.json`**: añadir el script `dist:publish` =
    `electron-vite build && electron-builder --publish always`. El `dist` del Plan 01
    (`--publish never`) se queda como está: son dos verbos distintos y mezclarlos es cómo se
    publica un release por accidente durante una prueba local.

    **`docs/extra/RELEASING.md`**: reescribir «Requisitos» y «Pasos por release» para el flujo
    Electron, conservando la advertencia de backup de la clave privada y la sección de la
    extensión y las Notas (que siguen siendo válidas). El flujo nuevo:
    1. Subir `version` en `apps/desktop/package.json` (semver estricto, sin sufijos).
    2. `GH_TOKEN` en el entorno.
    3. Raíz: `npm run build:extension && npm run build:sidecar`.
    4. `npm run dist:publish --workspace @nyanko/desktop` → sube el `.exe` + `latest.yml` a un
       release BORRADOR `v<version>`.
    5. `node apps/desktop/scripts/publish-bridge.mjs "<release notes en inglés>"` → firma,
       verifica y sube `.sig` + `latest.json` al mismo borrador.
    6. Publicar el borrador en GitHub (no puede quedarse en draft:
       `releases/latest/download/...` solo resuelve releases publicados).
    7. Probar desde una instalación 0.1.15: Ajustes → Acerca de → Buscar actualizaciones.
    Y una nota nueva y explícita: **el puente (`latest.json` + minisign) es de un solo uso.** En
    cuanto el parque instalado esté en 0.2.x manda `latest.yml` y el puente se puede borrar —
    incluida la clave. No hay que mantenerlo vivo por inercia.
  </action>
  <verify>
    <automated>cd apps/desktop && npm run test:publish && node -e "const s=require('fs').readFileSync('scripts/publish-bridge.mjs','utf8'); const a=[]; if(!/EMBEDDED_PUBKEY_B64/.test(s))a.push('sin ancla de confianza EMBEDDED_PUBKEY_B64'); if(!/verifyMinisign/.test(s))a.push('no verifica la firma antes de subir'); if(!/blake2b512/.test(s))a.push('sin la rama prehashed'); if(!/GH_TOKEN/.test(s))a.push('no exige GH_TOKEN'); if(!/2\.8\.4/.test(s))a.push('la version del CLI de firma no esta pineada'); const p=require('./package.json'); if(!p.scripts['dist:publish']||!p.scripts['dist:publish'].includes('--publish always'))a.push('falta dist:publish'); if(!p.scripts.dist.includes('--publish never'))a.push('el dist local dejo de ser --publish never'); if(p.devDependencies&&p.devDependencies['@tauri-apps/cli'])a.push('SHELL-02: @tauri-apps/cli NO puede volver a package.json'); if(a.length){console.error(a.join('\n'));process.exit(1)} console.log('BRIDGE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:publish` pasa: round-trip de `verifyMinisign` (acepta la firma buena, rechaza
      el buffer alterado y el key id alterado) y forma de `buildLatestJson`.
    - `publish-bridge.mjs` aborta con exit != 0 si falta `GH_TOKEN`, si falta el `.exe`, si la
      pubkey del disco no coincide con `EMBEDDED_PUBKEY_B64`, o si la firma no verifica —
      **antes de subir nada** (comprobable leyendo el orden de las guardas en el fuente).
    - El script no imprime ni el token ni el contenido de la clave privada en ninguna rama.
    - `@tauri-apps/cli` no aparece en `dependencies` ni en `devDependencies` de ningún
      `package.json` del repo (SHELL-02 sigue Completo); se invoca por `npx` con `2.8.4` pineado.
    - `apps/desktop/package.json` tiene `dist:publish` (`--publish always`) y conserva `dist`
      (`--publish never`).
    - `docs/extra/RELEASING.md` describe los 7 pasos del flujo Electron, ya no menciona
      `tauri.conf.json` ni `Cargo.toml`, y dice explícitamente que el puente es de un solo uso.
  </acceptance_criteria>
  <done>El puente existe, se prueba solo, y se niega a publicar una firma que los clientes 0.1.15 no aceptarían.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: gate de legitimidad (T-05-SC-2) — `npx @tauri-apps/cli@2.8.4` + GH_TOKEN</name>
  <what-built>
    El puente está escrito y su self-check pasa, pero todavía no se ha ejecutado. Dos cosas
    bloquean su primera ejecución y ninguna la puede hacer Claude:

    1. `publish-bridge.mjs` invoca por `npx` un paquete que **ejecuta código** en esta máquina y
       produce la firma de la que depende TODO el parque instalado. No hay RESEARCH.md en esta
       fase → política de fallback: se trata como **[ASSUMED]** y se verifica a mano antes de
       ejecutarlo. (Precedente: T-05-SC del Plan 01, T-04-SC de la Fase 4.)
    2. `GH_TOKEN` es un secreto: lo crea y lo exporta el usuario.
  </what-built>
  <how-to-verify>
    1. Abrir https://npmjs.com/package/@tauri-apps/cli y confirmar:
       - Es el CLI OFICIAL de Tauri (repo `tauri-apps/tauri`), no un typosquat (`tauri-cli`,
         `@tauri_apps/cli`, homoglifos…).
       - La versión **2.8.4** existe y es de la línea 2.x. Es exactamente el paquete y la línea de
         versión que este repo ya tenía como dependencia (`@tauri-apps/cli@^2.8.4`) y con el que
         se firmaron TODOS los releases 0.1.x que hay hoy en el campo. No es un proveedor nuevo:
         es el mismo de siempre, invocado una última vez.
       - Que el `2.8.4` del plan coincide carácter a carácter con el de la ficha.
    2. Crear el token: GitHub → Settings → Developer settings → Personal access tokens →
       Fine-grained → repositorio `kfernandoy/Nyanko`, permiso **Contents: Read and write**.
       Exportarlo como `GH_TOKEN` en la terminal desde la que se hará el release (Git Bash:
       `export GH_TOKEN=...`). No pegarlo en ningún fichero del repo.
    3. Confirmar que existe un **backup fuera de esta máquina** de
       `~/.tauri/nyanko-updater.key`. Sin esa clave no se puede firmar ningún puente y los
       usuarios 0.1.15 quedan huérfanos para siempre (ya está advertido en RELEASING.md).
  </how-to-verify>
  <resume-signal>Escribe "approved" cuando el paquete esté verificado y `GH_TOKEN` exportado, o describe el problema.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: publicar v0.2.0 y probar la migración real desde una instalación 0.1.15</name>
  <what-built>
    Todo: instalador (Plan 01), updater (Plan 02), migración del instalador (Plan 03), icono
    (Plan 05) y el puente (Tarea 1). Este checkpoint es la única prueba que vale de **D-01**, y no
    se puede automatizar: exige un release publicado de verdad y una instalación 0.1.15 de verdad.

    **NO cierra PKG-02.** Aquí el que actualiza es el updater de **Tauri** (minisign +
    `latest.json`). El de electron-updater (SHA512 + `latest.yml` + matar sidecar +
    `quitAndInstall`) solo llega hasta la rama «estás al día» (paso 7). Su camino feliz completo
    lo prueba el **Plan 06**, que publica un segundo release.

    **Estado de máquina requerido al entrar: M1** (ver `<coreografia_del_estado_de_la_maquina>` en
    05-03-PLAN.md). La máquina viene de los Planes 03 y 02, así que está en **M2** (0.2.0
    instalada): **hay que restaurar M1 explícitamente**, es el paso 5-pre. **Estado al salir: M2**
    — el que el Plan 06 necesita.
  </what-built>
  <how-to-verify>
    **Publicar** (desde la raíz, con `GH_TOKEN` exportado — ver `docs/extra/RELEASING.md`):

    1. `npm run build:extension && npm run build:sidecar`
    2. `npm run dist:publish --workspace @nyanko/desktop`
       → sube `Nyanko-Setup-0.2.0.exe` + `latest.yml` (+ `.blockmap`) a un release BORRADOR `v0.2.0`.
    3. `node apps/desktop/scripts/publish-bridge.mjs "<notas en inglés: engine swap a Electron, el
       instalador puede pedir unos clics, y el enlace de descarga manual como plan B>"`
       → firma, verifica y sube `Nyanko-Setup-0.2.0.exe.sig` + `latest.json` al mismo borrador.
       Si el script aborta, NO seguir: el mensaje dice qué guarda falló.
    4. En GitHub, abrir el borrador `v0.2.0` y comprobar que están los CUATRO artefactos
       (`.exe`, `latest.yml`, `.exe.sig`, `latest.json`) — y solo entonces **Publish release**.
       Un borrador no resuelve por `releases/latest/download/...`.

    **5-pre. Restaurar la máquina a M1.** Ahora mismo tiene la 0.2.0 instalada (la puso el e2e del
    Plan 03 y la reinstaló el checkpoint del Plan 02). Para probar D-01 hace falta una 0.1.15 de
    verdad:
    a. Desinstalar Nyanko 0.2.0 desde Agregar/quitar programas. **Su desinstalador NO borra
       `%APPDATA%\app.nyanko.desktop`** (`deleteAppDataOnUninstall: false`, DATA-01, verificado en
       el Plan 01) — pero comprobarlo, y si no, restaurar el data dir desde el backup.
    b. Instalar **Nyanko 0.1.15** desde https://github.com/kfernandoy/Nyanko/releases.
    c. Abrirlo, confirmar que la biblioteca carga, y anotar el tamaño de `nyanko.db`.
    La máquina está en M1.

    **La prueba que da fe (D-01):**

    5. Abrir Nyanko 0.1.15 → Ajustes → Acerca de → **Buscar actualizaciones**.
       Debe: encontrar 0.2.0, mostrar las notas, pedir confirmación, descargar, **verificar la
       firma minisign** y ejecutar el instalador.
       **Los DOS desenlaces siguientes son APROBABLES** (ver `<flags_del_updater_tauri>`) — anotar
       en el SUMMARY cuál ocurrió, porque es un hecho sobre el parque instalado:
       - **(i) silencioso:** barra de progreso, instala y relanza solo. Ideal.
       - **(ii) con asistente:** aparece el asistente NSIS completo (idioma + EULA + directorio);
         el usuario le da a Siguiente hasta el final; puede que la app no se relance sola y haya
         que abrirla desde el acceso directo. **Aceptable**: el flag passive de Tauri no lo
         entiende el NSIS de electron-builder y NSIS lo ignora. Es un par de clics, no un fallo.
         Si sale este desenlace, **dejar el directorio de instalación por defecto, sea el que sea, y
         no tocarlo**. El porqué depende de la rama que el gate D-02 del Plan 03 haya seleccionado —
         consultar el SUMMARY del Plan 03 antes de este checkpoint:
         - **rama B** (instalar encima): el directorio por defecto viene sembrado del
           `InstallLocation` de Tauri por el `preInit`. Cambiarlo a mano deja el `Nyanko.exe` viejo
           sin sobrescribir y por tanto lanzable (T-05-17).
         - **rama A** (uninstall silencioso): no hay `preInit` ni directorio sembrado; el default es
           el de electron-builder y la 0.1.15 ya ha sido desinstalada. Cambiarlo no rompe nada, pero
           no aporta nada y desvía del camino que se está probando.
       En AMBOS casos, al abrir: **es la app Electron** (Acerca de → Versión = 0.2.0).
    5-bis. **FALLO real** = el instalador ni siquiera arranca, o arranca y falla, o la app no abre
       después. Entonces NO se aprueba el checkpoint. Camino de escape para el usuario, que ya está
       publicado y no hay que improvisarlo: las notas de release (paso 3) llevan el enlace de
       descarga manual del `.exe`, y el updater de Tauri se las enseña ANTES de que acepte. Reportar
       el fallo con el log del updater de 0.1.15 en vez de aprobar.
    6. Comprobar tras la migración:
       - la biblioteca carga con el mismo contenido y `nyanko.db` conserva su tamaño (DATA-01);
       - Agregar/quitar programas: UNA sola entrada «Nyanko» (Plan 03);
       - los accesos directos del menú Inicio abren la 0.2.0, no la 0.1.15 (Plan 03, D-02);
       - la bandeja muestra el icono de marca (Plan 05);
       - no queda ningún `nyanko-api.exe` huérfano en el Administrador de tareas (D-05).
    7. Y ahora la rama «al día» del updater NUEVO: en esa 0.2.0 recién migrada, Ajustes → Acerca de
       → Buscar actualizaciones → debe decir **«Nyanko está al día»** (ya no el error de
       `latest.yml` ausente que se vio en el checkpoint del Plan 02: ahora existe y coincide con
       la versión instalada). El camino feliz — que encuentre, descargue e INSTALE — es el Plan 06.

    Si el paso 5 falla en la verificación de firma, es la firma o la URL de `latest.json`: descargar
    `latest.json` del release y confirmar que su `signature` es el contenido literal del `.sig`
    publicado y que su `url` apunta al `.exe` de ESE release.
  </how-to-verify>
  <resume-signal>Escribe "approved" cuando una instalación 0.1.15 real haya aterrizado en 0.2.0 con la biblioteca intacta, diciendo si fue el desenlace (i) silencioso o el (ii) con asistente. Si el instalador falló de verdad, describe el fallo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clave privada minisign (`~/.tauri/`) → firma → 20 000 binarios en el campo | La firma es lo ÚNICO que autoriza a 0.1.15 a ejecutar un binario descargado. |
| npm registry → máquina de release | `npx @tauri-apps/cli` ejecuta código de terceros en la máquina que guarda la clave privada. |
| Máquina de release → GitHub Releases | `GH_TOKEN` tiene permiso de escritura sobre el repo. |
| GitHub Releases → clientes 0.1.15 | Canal público, HTTPS; la integridad la aporta la firma, no el transporte. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC-2 | Tampering | `npx @tauri-apps/cli@2.8.4 signer sign` | high | mitigate | Paquete tratado como [ASSUMED] (sin RESEARCH.md): checkpoint humano bloqueante (Tarea 2) contra npmjs.com antes de la primera ejecución, y **versión pineada** en el script (nunca `@latest`: un `latest` comprometido correría con acceso a la clave privada). Es el mismo paquete y la misma línea de versión con la que este repo firmó todos sus releases 0.1.x. No auto-aprobable. |
| T-05-11 | Spoofing / Tampering | firma minisign de `Nyanko-Setup-0.2.0.exe` | critical | mitigate | Dos guardas ANTES de subir nada: (a) la pubkey del disco debe ser idéntica a `EMBEDDED_PUBKEY_B64`, el ancla horneada en los binarios 0.1.15 → detecta «firmado con otra clave»; (b) `verifyMinisign()` verifica en Ed25519 la firma recién creada contra esa misma pubkey → detecta firma corrupta, truncada o de otro fichero. Sin esto, una firma mala es indistinguible de una buena hasta que falla, en silencio, en cada cliente instalado. |
| T-05-12 | Tampering | `latest.json` apuntando a un asset que no es el firmado | high | mitigate | La `url` sale del `browser_download_url` que devuelve la API para el asset que electron-builder acaba de subir a ESE release, y la `signature` es el contenido del `.sig` de ESE mismo fichero. Nada se compone a mano (que es como se escribía antes: `RELEASING.md:41-55`, paso manual). |
| T-05-13 | Information disclosure | `GH_TOKEN` y la clave privada | high | mitigate | El token solo vive en el entorno de la terminal (nunca en un fichero del repo) y el script no lo imprime en ninguna rama. La clave privada vive fuera del repo (`~/.tauri/`), el script la pasa como ruta al firmador y jamás vuelca su contenido. Criterio de aceptación de la Tarea 1. |
| T-05-14 | Denial of Service | release publicado sin `latest.json` | high | mitigate | El puente sube al BORRADOR y el release se publica con los cuatro artefactos dentro (Tarea 3, paso 4). Publicar primero y firmar después abriría una ventana en la que `releases/latest/download/latest.json` devuelve 404 a todo el parque 0.1.15. |
| T-05-15 | Repudiation | instalador sin code-signing | medium | accept | Ya aceptado en T-05-03 (Plan 01) y en REQUIREMENTS.md §v2/Deferred. Las ACTUALIZACIONES por updater no pasan por SmartScreen; solo la primera descarga manual. |
| T-05-20 | Denial of Service | el NSIS de electron-builder ignora los flags passive/restart del updater de Tauri | medium | accept | La migración D-01 puede acabar enseñando el asistente completo en vez de una barra de progreso, y puede no relanzar la app sola. El binario está firmado y verificado igual, y la instalación se completa igual: el coste es un par de clics del usuario. Reescribir el NSIS de electron-builder para hablar los flags de Tauri es un parche a un instalador de terceros para un puente **de un solo uso**. Aceptado y VERIFICADO en el checkpoint (Tarea 3, paso 5, desenlace (ii)), con las notas de release llevando el enlace de descarga manual como camino de escape. |
</threat_model>

<artifacts_this_phase_produces>
| Artefacto | Tipo | Detalle |
|---|---|---|
| `apps/desktop/scripts/publish-bridge.mjs` | script (nuevo) | Exports puros: `parseMinisignKey`, `parseMinisignSig`, `verifyMinisign`, `buildLatestJson`. Constante `EMBEDDED_PUBKEY_B64`. Efectos: firma (npx tauri signer), verifica, sube `.sig` + `latest.json` al release borrador |
| `apps/desktop/scripts/publish-bridge.test.mjs` | self-check (nuevo) | `node:test`: round-trip de `verifyMinisign` (positivo + dos negativos) y forma de `buildLatestJson` |
| script npm `dist:publish` (`@nyanko/desktop`) | script | `electron-vite build && electron-builder --publish always` |
| script npm `test:publish` (`@nyanko/desktop`) | script | `node --test scripts/publish-bridge.test.mjs` |
| `latest.json` (asset del release v0.2.0) | artefacto de release | Esquema Tauri; puente de UN SOLO USO para el parque 0.1.15 |
| `Nyanko-Setup-0.2.0.exe.sig` | artefacto de release | Firma minisign verificable contra la pubkey horneada en 0.1.15 |
| `docs/extra/RELEASING.md` | doc (reescrito) | Flujo Electron de 7 pasos; el flujo Tauri desaparece |
</artifacts_this_phase_produces>

<verification>
1. `cd apps/desktop && npm run test:publish` → los helpers puros del puente pasan su self-check.
2. `npm run dist:publish --workspace @nyanko/desktop` sube `.exe` + `latest.yml` al borrador v0.2.0.
3. `node apps/desktop/scripts/publish-bridge.mjs "..."` firma, VERIFICA y sube `.sig` + `latest.json`.
4. El release v0.2.0 publicado tiene los cuatro artefactos.
5. Una instalación 0.1.15 real (M1, restaurada en el paso 5-pre) se actualiza a 0.2.0 (Electron)
   con la biblioteca intacta — desenlace (i) o (ii), ambos aprobables.
6. La 0.2.0 migrada dice «Nyanko está al día» al buscar actualizaciones.
</verification>

<success_criteria>
- D-01: el puente firmado existe, verifica contra la pubkey del parque instalado, y una 0.1.15
  real ha aterrizado en 0.2.0.
- D-08: `latest.yml` lo genera electron-builder (SHA512 y tamaño por construcción), nunca a mano.
- PKG-02 (canal de publicación): el feed de electron-updater existe en GitHub Releases y la
  instalación 0.2.0 lo lee y se reconoce al día. **El camino feliz de PKG-02 (descargar, verificar
  SHA512, matar el sidecar, instalar, relanzar) se cierra en el Plan 06** — necesita un SEGUNDO
  release y no se puede probar contra la versión que uno mismo tiene instalada.
- PKG-01 completo: el instalador publicado es el que reciben tanto los usuarios nuevos como los
  que vienen de Tauri.
</success_criteria>

<output>
Create `.planning/phases/05-packaging-auto-update/05-04-SUMMARY.md` when done
</output>
