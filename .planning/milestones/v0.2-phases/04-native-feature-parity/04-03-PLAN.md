---
phase: 04-native-feature-parity
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - apps/desktop/package.json
  - apps/desktop/electron/main/discord.ts
  - apps/desktop/electron/main/index.ts
  - apps/desktop/electron/main/ipc.ts
  - apps/desktop/electron/preload/index.ts
  - apps/desktop/src/native.ts
autonomous: false
user_setup: []
requirements: [NATIVE-05, NATIVE-06]
must_haves:
  truths:
    - "setDiscordActivity shows Rich Presence using client id 1521045260342525962 (or NYANKO_DISCORD_CLIENT_ID) with details/state/optional start_timestamp"
    - "When Discord is not running or unconfigured, set/clear activity is a silent no-op (no crash)"
    - "clearDiscordActivity clears the presence"
    - "A second launch focuses the live instance instead of opening a new window"
    - "Autostart registers/removes the login item and launches with --minimized"
    - "Notifications, open-external, and folder dialog work (Phase-3 wiring verified intact)"
  artifacts:
    - apps/desktop/electron/main/discord.ts
    - "@xhayper/discord-rpc dependency in package.json"
    - "discord:set-activity / discord:clear-activity + autostart:get / autostart:set IPC handlers"
    - "filled native.ts setDiscordActivity/clearDiscordActivity/getAutostart/setAutostart bodies"
  key_links:
    - "native.setDiscordActivity -> discord:set-activity IPC -> lazy-connected @xhayper client (catch+swallow on failure)"
    - "app.requestSingleInstanceLock -> second-instance event -> show+focus mainWindow"
    - "native.setAutostart -> autostart:set IPC -> app.setLoginItemSettings({openAtLogin, args:['--minimized']})"
---

<objective>
Deliver NATIVE-05 (Discord Rich Presence) and NATIVE-06 (single-instance, autostart, and
verification of the already-wired notifications/opener/dialog). Mirror the deleted Rust discord.rs
and lib.rs behavior exactly (D-01/D-02/D-03, D-08).

Purpose: these are the last native ops still stubbed as no-ops. Discord RP must connect lazily and
fail silently; a second launch must focus the running instance; autostart must launch minimized.
notify/openExternal/openPath/openFolderDialog were wired in Phase 3 — this plan verifies they are
intact, not rebuild.
Output: discord.ts main module, the @xhayper/discord-rpc dependency, single-instance + autostart
wiring, filled native bodies, and a green self-check with no remaining Phase-4 stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-native-feature-parity/04-CONTEXT.md

# Parity source of truth (D-08) — read via git BEFORE implementing:
#   git show a50659c^:apps/desktop/src-tauri/src/discord.rs  (client id 1521045260342525962, NYANKO_DISCORD_CLIENT_ID, lazy connect, Activity details/state/Timestamps.start, drop client + no-op on error)
#   git show a50659c^:apps/desktop/src-tauri/src/lib.rs      (single_instance -> show/unminimize/focus; autostart plugin with ['--minimized']; opener/dialog/notification/process plugins)
@apps/desktop/src/native.ts
@apps/desktop/electron/main/index.ts
@apps/desktop/electron/main/ipc.ts
@apps/desktop/electron/preload/index.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package-legitimacy gate for @xhayper/discord-rpc (T-04-SC)</name>
  <action>Blocking human checkpoint (never auto-approvable): verify @xhayper/discord-rpc on npmjs.com before it is installed. No code is written until approved.</action>
  <what-built>Nothing yet — this is the mandatory package-legitimacy gate before installing a new npm dependency (T-04-SC). D-01 locked `@xhayper/discord-rpc` (actively-maintained TypeScript fork of the archived discord-rpc).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@xhayper/discord-rpc
    2. Confirm it is the intended package (scope `@xhayper`, a Discord RPC client), has recent
       publishes, and is not a typosquat of the archived `discord-rpc`.
    3. Confirm the exact spelling `@xhayper/discord-rpc` before it is added to apps/desktop/package.json.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or name a different package.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: discord.ts main module + @xhayper/discord-rpc dep + IPC + native bodies</name>
  <files>apps/desktop/package.json, apps/desktop/electron/main/discord.ts, apps/desktop/electron/main/ipc.ts, apps/desktop/electron/preload/index.ts, apps/desktop/src/native.ts</files>
  <read_first>
    - git show a50659c^:apps/desktop/src-tauri/src/discord.rs (behavior contract: lazy connect, no-op on failure, drop client on set error, Timestamps.start)
    - apps/desktop/src/native.ts (setDiscordActivity/clearDiscordActivity no-op stubs ~76-83; DiscordActivity type ~16-20)
    - apps/desktop/electron/preload/index.ts
    - apps/desktop/electron/main/ipc.ts
  </read_first>
  <action>
    Add `@xhayper/discord-rpc` to apps/desktop/package.json dependencies and install (only after the
    checkpoint is approved). Create apps/desktop/electron/main/discord.ts holding a module-level
    client slot (lazily created). Client id = `process.env.NYANKO_DISCORD_CLIENT_ID ||
    "1521045260342525962"` (D-02). `setDiscordActivity({details, state, start_timestamp})`: lazily
    construct+connect the @xhayper client on first call; build an activity with details/state only
    when non-empty (Discord rejects empty strings) and set startTimestamp when start_timestamp is
    present; wrap the entire connect+setActivity in try/catch and SILENTLY swallow errors (D-03 —
    Discord not running / unconfigured), dropping the client slot on failure so the next call
    reconnects. `clearDiscordActivity()`: if a client exists, clear presence, ignoring errors.
    Register `ipcMain.handle("discord:set-activity", (_e, payload) => setDiscordActivity(payload))`
    and `ipcMain.handle("discord:clear-activity", () => clearDiscordActivity())`. Add preload methods
    `setDiscordActivity: (payload) => ipcRenderer.invoke("discord:set-activity", payload)` and
    `clearDiscordActivity: () => ipcRenderer.invoke("discord:clear-activity")`. In native.ts fill the
    two bodies to `return window.nyanko?.setDiscordActivity(payload) ?? Promise.resolve();` and the
    clear analog (web fallback stays a silent no-op, matching the old ignore-on-error). Do NOT change
    NATIVE_OPS, do NOT touch checkForUpdates, do NOT change discord.ts/windowPrefs.ts renderer
    re-exports or App.tsx signature/timestamp logic (D-03). Comments in Spanish.
    ponytail: no reconnect/backoff loop — lazy connect + swallow + drop-on-error is the whole 0.1.15 contract.
  </action>
  <verify>
    <automated>cd apps/desktop &amp;&amp; npm run test:native &amp;&amp; npm run check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@xhayper/discord-rpc' apps/desktop/package.json` >= 1 and the package installs.
    - `grep -c '1521045260342525962\|NYANKO_DISCORD_CLIENT_ID' apps/desktop/electron/main/discord.ts` >= 1.
    - `grep -c 'discord:set-activity\|discord:clear-activity' apps/desktop/electron/preload/index.ts` >= 2.
    - `grep -c 'setDiscordActivity\|clearDiscordActivity' apps/desktop/src/native.ts` shows the bodies route through window.nyanko (no `Promise.resolve()`-only stub left).
    - `npm run test:native` 2/2; `npm run check` + `npm run build` exit 0 (build succeeds with Discord closed — proving no top-level connect crash).
  </acceptance_criteria>
  <done>Discord Rich Presence set/clear works with the locked client id and is a silent no-op when Discord is absent; native bodies are real and the self-check stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Single-instance + autostart + verify notifications/opener/dialog intact</name>
  <files>apps/desktop/electron/main/index.ts, apps/desktop/electron/main/ipc.ts, apps/desktop/electron/preload/index.ts, apps/desktop/src/native.ts</files>
  <read_first>
    - git show a50659c^:apps/desktop/src-tauri/src/lib.rs (single_instance handler: show+unminimize+focus; autostart args ['--minimized'])
    - apps/desktop/electron/main/index.ts (app.whenReady/before-quit; where to add requestSingleInstanceLock and the mainWindow ref from Plan 02)
    - apps/desktop/electron/main/ipc.ts (existing notify/openExternal/openPath/openFolderDialog handlers — verify unchanged, Phase 3)
    - apps/desktop/src/native.ts (getAutostart/setAutostart no-op stubs ~56-63)
  </read_first>
  <action>
    In electron/main/index.ts, before app.whenReady: call `app.requestSingleInstanceLock()`; if it
    returns false, `app.quit()` and return immediately; otherwise register
    `app.on("second-instance", () => { show + unminimize + focus mainWindow })` (mirror lib.rs — a
    relaunch brings the live instance to front). Autostart: register
    `ipcMain.handle("autostart:get", () => app.getLoginItemSettings().openAtLogin)` and
    `ipcMain.handle("autostart:set", (_e, enabled) => app.setLoginItemSettings({ openAtLogin:
    Boolean(enabled), args: ["--minimized"] }))` (D: `--minimized`, Claude's discretion via
    setLoginItemSettings). Add preload methods `getAutostart: () => ipcRenderer.invoke("autostart:get")`
    and `setAutostart: (enabled) => ipcRenderer.invoke("autostart:set", enabled)`. In native.ts fill
    getAutostart -> `return window.nyanko?.getAutostart() ?? Promise.resolve(false);` and setAutostart
    -> `return window.nyanko?.setAutostart(enabled) ?? Promise.resolve();`. VERIFY (do not rebuild)
    that the Phase-3 notify/openExternal (http(s)-only)/openPath/revealItemInDir/openFolderDialog
    handlers in ipc.ts are still present and their validation is not weakened — NATIVE-06 counts them
    as already delivered. Do NOT change NATIVE_OPS. Comments in Spanish.
    ponytail: single-instance is `requestSingleInstanceLock` + one focus handler — no custom IPC broker.
  </action>
  <verify>
    <automated>cd apps/desktop &amp;&amp; npm run test:native &amp;&amp; npm run check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'requestSingleInstanceLock\|second-instance' apps/desktop/electron/main/index.ts` >= 2.
    - `grep -c 'autostart:get\|autostart:set' apps/desktop/electron/main/ipc.ts` >= 2 and `setLoginItemSettings` appears with `--minimized`.
    - `grep -c 'getAutostart\|setAutostart' apps/desktop/electron/preload/index.ts` >= 2.
    - `grep -c 'throw new Error' apps/desktop/src/native.ts` returns 1 (only checkForUpdates / Phase 5 remains a throw-stub; all Phase-4 stubs are real).
    - `grep -c 'openExternal' apps/desktop/electron/main/ipc.ts` >= 1 and its `^https?` check is intact (Phase-3 opener not weakened).
    - `npm run test:native` 2/2; `npm run check` + `npm run build` exit 0.
  </acceptance_criteria>
  <done>Single-instance focuses the live window, autostart registers with --minimized, and the Phase-3 notifications/opener/dialog handlers are confirmed intact — every Phase-4 native op is now real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main (IPC) | Renderer sets Discord activity + autostart via new channels |
| main -> Discord IPC | Local Discord socket via @xhayper/discord-rpc |
| supply chain | New npm dependency @xhayper/discord-rpc |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | @xhayper/discord-rpc install | high | mitigate | Blocking human checkpoint verifies the package on npmjs.com before install; exact-spelling confirmation guards against typosquats |
| T-04-08 | Denial of Service | discord.ts lazy connect | medium | mitigate | connect+setActivity wrapped in try/catch and swallowed; a closed/absent Discord never crashes the main process (matches 0.1.15) |
| T-04-09 | Tampering | discord client id | low | accept | Client id from env/const only, never renderer-supplied; renderer supplies only details/state/timestamp strings |
| T-04-10 | Tampering | preload bridge | medium | mitigate | Only named typed methods (setDiscordActivity/clearDiscordActivity/getAutostart/setAutostart) exposed; no raw ipcRenderer |
| T-04-11 | Elevation of Privilege | Phase-3 opener/dialog | medium | mitigate | Verify openExternal keeps http(s)-only and openPath keeps the `://` scheme rejection — not weakened while extending the boundary |
</threat_model>

<verification>
- `cd apps/desktop && npm run test:native` 2/2; `npm run check` + `npm run build` exit 0 with Discord closed.
- `grep 'throw new Error' apps/desktop/src/native.ts` returns exactly 1 (only the Phase-5 updater stub remains).
- Manual (carried to phase verify): Discord open -> presence appears; Discord closed -> no crash; launch a second instance -> the live window focuses; toggle autostart -> login item registered with --minimized.
</verification>

<success_criteria>
NATIVE-05 (Discord RP set/clear, silent no-op) and NATIVE-06 (single-instance focus, autostart
--minimized, notifications/opener/dialog) work as 0.1.15 equivalents; no Phase-4 stub remains.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces
- New file: `apps/desktop/electron/main/discord.ts`.
- New dependency: `@xhayper/discord-rpc` in apps/desktop/package.json.
- New IPC channels: `discord:set-activity`, `discord:clear-activity`, `autostart:get`, `autostart:set`.
- New `window.nyanko` bridge methods: `setDiscordActivity`, `clearDiscordActivity`, `getAutostart`, `setAutostart`.
- Filled `native.*` bodies: `setDiscordActivity`, `clearDiscordActivity`, `getAutostart`, `setAutostart`.
- index.ts: `requestSingleInstanceLock` + `second-instance` focus handler.
- Verified intact (Phase 3): `notify`, `openExternal`, `openPath`, `revealItemInDir`, `openFolderDialog`.
- Untouched: `checkForUpdates` (Phase 5 stub). NATIVE_OPS unchanged; native.test.ts stays 2/2.
</artifacts_produced>

<output>
Create `.planning/phases/04-native-feature-parity/04-03-SUMMARY.md` when done.
</output>
